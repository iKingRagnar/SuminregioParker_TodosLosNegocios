# CÃ³digo para Claude â€” Suminregio Parker ERP

Copia este archivo y los 5 archivos listados abajo (abre cada uno y copia todo el contenido) y pÃ¡saselos a Claude en texto plano.

---

## LO QUE AÃšN NO SE HA PODIDO SOLUCIONAR (decirle esto a Claude)

- **ventas.html:** Las grÃ¡ficas **Ventas Diarias** y **Cotizaciones Diarias** siguen sin mostrar datos reales (o se ven vacÃ­as / ejes raros). Se aÃ±adieron fallbacks con 30 dÃ­as en cero cuando la API devuelve vacÃ­o; el usuario reporta que las grÃ¡ficas "no se ven".
- **cobradas.html:** La pÃ¡gina se queda en "Cargando" o muestra "Sin datos de facturas cobradas" aunque existan datos. Se usÃ³ fwt() con timeout y siempre renderAll(); el problema persiste.
- **vendedores.html:** **Tendencia Diaria del Equipo (30 dÃ­as)** y **TENDENCIA DE COBROS (6 MESES)** siguen sin mostrar datos en las grÃ¡ficas; solo "% Cumplimiento vs Meta Mensual" muestra datos.
- **resultados.html:** El P&L sigue sin dar datos Ãºtiles: Costo de ventas "Sin datos", Utilidad Bruta = Ventas Netas, Margen al 100%. En el servidor se aÃ±adiÃ³ costo desde DOCTOS_IN con TIPO_DOCTO STARTING WITH 'S' y PRECIO_UNITARIO*UNIDADES; si en Microsip el esquema es distinto (p. ej. TIPO_MOV en vez de TIPO_DOCTO, o costo en otra tabla), hay que ajustar la consulta.

**Base de datos:** Firebird 3.0. Contexto en el mensaje del usuario (tablas DOCTOS_VE, DOCTOS_PV, DOCTOS_IN, IMPORTES_DOCTOS_CC, etc.). **No usar defaultPreset:'mes'** en initFilters para no filtrar por mes actual en la primera carga.

---

## Archivos con el cÃ³digo (copiar todo el contenido de cada uno)

1. **ventas.html** â€” ruta: `ventas.html`
2. **cobradas.html** â€” ruta: `cobradas.html`
3. **vendedores.html** â€” ruta: `vendedores.html`
4. **inventario.html** â€” ruta: `inventario.html`
5. **server_corregido.js** â€” ruta: `server_corregido.js`

Abre cada archivo en el editor, selecciona todo (Ctrl+A) y copia. Pega en el chat con Claude despuÃ©s de la nota de arriba.
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Suminregio Parker ? Ventas</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Mono:ital,wght@0,300;0,400;0,500;1,300&display=swap" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<script src="filters.js"></script>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --bg:#060E1A;--s1:#0C1B29;--s2:#112233;--s3:#172D42;--s4:#1D3654;
  --border:rgba(255,255,255,.06);--border2:rgba(255,255,255,.11);
  --green:#00E5A0;--green-dim:rgba(0,229,160,.1);
  --yellow:#FFB800;--yellow-dim:rgba(255,184,0,.12);
  --orange:#FF8C42;--orange-dim:rgba(255,140,66,.1);
  --red:#FF4566;--red-dim:rgba(255,69,102,.12);
  --blue:#4DA6FF;--blue-dim:rgba(77,166,255,.1);
  --purple:#9B6DFF;--purple-dim:rgba(155,109,255,.1);
  --text:#E8EFF8;--text2:#B0BECE;--muted:#5A7090;--dim:#243348;
}
html{font-size:16px;-webkit-font-smoothing:antialiased}
body{background:var(--bg);color:var(--text);font-family:'Syne',sans-serif;min-height:100vh}
.bg-orbs{position:fixed;inset:0;pointer-events:none;z-index:0;overflow:hidden}
.orb{position:absolute;border-radius:50%;filter:blur(130px);opacity:.12;animation:drift 20s ease-in-out infinite alternate}
.orb-1{width:600px;height:600px;background:radial-gradient(circle,#1E7FD9,transparent 70%);top:-150px;left:-100px}
.orb-2{width:500px;height:500px;background:radial-gradient(circle,#00E5A0,transparent 70%);bottom:-100px;right:-100px;animation-delay:-7s}
@keyframes drift{0%{transform:translate(0,0)}100%{transform:translate(20px,15px)}}
header{position:sticky;top:0;z-index:100;background:rgba(5,11,20,.85);backdrop-filter:blur(20px);border-bottom:1px solid var(--border)}
.header-inner{max-width:1600px;margin:0 auto;height:64px;display:flex;align-items:center;justify-content:space-between;gap:1.5rem;padding:0 2rem}
.logo{display:flex;align-items:center;gap:.75rem;text-decoration:none}
.logo-icon{width:36px;height:36px;background:linear-gradient(135deg,var(--blue),var(--green));border-radius:10px;display:grid;place-items:center;flex-shrink:0}
.logo-icon svg{width:20px;height:20px;fill:white}
.logo-text{font-size:.9rem;font-weight:700;color:var(--text)}
.logo-sub{font-size:.62rem;font-family:'DM Mono',monospace;color:var(--muted);letter-spacing:.08em;text-transform:uppercase}
nav{display:flex;align-items:center;gap:.25rem}
.nav-link{display:flex;align-items:center;gap:.4rem;padding:.4rem .85rem;border-radius:8px;font-size:.8rem;font-weight:500;color:var(--muted);text-decoration:none;transition:all .2s}
.nav-link:hover{color:var(--text);background:var(--s2)}
.nav-link.active{color:var(--text);background:var(--s3);border:1px solid var(--border2)}
.nav-link svg{width:14px;height:14px;fill:currentColor}
.header-right{display:flex;align-items:center;gap:1rem}
.live-pill{display:flex;align-items:center;gap:.5rem;background:var(--green-dim);border:1px solid rgba(0,229,160,.2);border-radius:99px;padding:.3rem .75rem;font-family:'DM Mono',monospace;font-size:.68rem;color:var(--green);letter-spacing:.06em}
.live-dot{width:6px;height:6px;border-radius:50%;background:var(--green);box-shadow:0 0 8px var(--green);animation:pulse 2s ease-in-out infinite}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.4}}
.clock{font-family:'DM Mono',monospace;font-size:.78rem;color:var(--muted)}
.page{position:relative;z-index:1;max-width:1600px;margin:0 auto;padding:2rem}
.page-header{margin-bottom:2rem;display:flex;align-items:flex-start;justify-content:space-between;gap:1rem;flex-wrap:wrap}
.page-title{font-size:1.75rem;font-weight:800;background:linear-gradient(135deg,var(--text) 30%,var(--orange));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.page-sub{font-size:.85rem;color:var(--muted);margin-top:.35rem}
.source-tabs{display:flex;gap:.5rem;margin-bottom:1.5rem;flex-wrap:wrap;align-items:center}
.tab-btn{padding:.45rem 1.1rem;border-radius:8px;font-size:.8rem;font-weight:600;cursor:pointer;border:1px solid var(--border2);background:var(--s1);color:var(--muted);transition:all .2s;font-family:'Syne',sans-serif}
.tab-btn:hover{color:var(--text);border-color:var(--blue)}
.tab-all.active{background:var(--green-dim);color:var(--green);border-color:var(--green)}
.tab-ve.active{background:var(--blue-dim);color:var(--blue);border-color:var(--blue)}
.tab-pv.active{background:var(--purple-dim);color:var(--purple);border-color:var(--purple)}
.kpi-grid{display:grid;grid-template-columns:repeat(5,1fr);gap:1rem;margin-bottom:1.5rem}
@media(max-width:1200px){.kpi-grid{grid-template-columns:repeat(3,1fr)}}
@media(max-width:700px){.kpi-grid{grid-template-columns:repeat(2,1fr)}}
.kpi-card{background:var(--s1);border:1px solid var(--border);border-radius:16px;padding:1.25rem 1.5rem;position:relative;overflow:hidden;transition:all .3s}
.kpi-card::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,var(--kpi-c),transparent);opacity:.8}
.kpi-card:hover{border-color:var(--border2);transform:translateY(-2px);box-shadow:0 12px 40px rgba(0,0,0,.3)}
.kpi-card.blue{--kpi-c:var(--blue)}.kpi-card.green{--kpi-c:var(--green)}.kpi-card.yellow{--kpi-c:var(--yellow)}.kpi-card.orange{--kpi-c:var(--orange)}.kpi-card.purple{--kpi-c:var(--purple)}
.kpi-label{font-family:'DM Mono',monospace;font-size:.62rem;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-bottom:.4rem}
.kpi-value{font-family:'DM Mono',monospace;font-size:1.4rem;font-weight:500;letter-spacing:-.02em;line-height:1}
.kpi-card.blue .kpi-value{color:var(--blue)}.kpi-card.green .kpi-value{color:var(--green)}.kpi-card.yellow .kpi-value{color:var(--yellow)}.kpi-card.orange .kpi-value{color:var(--orange)}.kpi-card.purple .kpi-value{color:var(--purple)}
.kpi-sub{margin-top:.4rem;font-family:'DM Mono',monospace;font-size:.65rem;color:var(--muted)}
.meta-bar-wrap{margin-top:.6rem}.meta-bar-bg{height:4px;background:var(--s3);border-radius:99px;overflow:hidden}.meta-bar-fill{height:100%;border-radius:99px;background:var(--kpi-c);transition:width 1.2s cubic-bezier(.4,0,.2,1)}.meta-pct{font-family:'DM Mono',monospace;font-size:.62rem;color:var(--muted);margin-top:.2rem}
.tag{display:inline-block;padding:.1rem .4rem;border-radius:4px;font-size:.6rem;font-weight:500}
.tag.up{background:var(--green-dim);color:var(--green)}.tag.down{background:var(--red-dim);color:var(--red)}.tag.warn{background:var(--yellow-dim);color:var(--yellow)}.tag.ok{background:var(--blue-dim);color:var(--blue)}
.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:1.25rem;margin-bottom:1.25rem}
@media(max-width:1100px){.grid-2{grid-template-columns:1fr}}
.card{background:var(--s1);border:1px solid var(--border);border-radius:16px;overflow:hidden}
.card-head{padding:1.25rem 1.5rem;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:.5rem}
.card-title{font-size:.88rem;font-weight:600;color:var(--text);display:flex;align-items:center;gap:.6rem}
.card-title svg{width:16px;height:16px;fill:var(--muted)}
.card-badge{font-family:'DM Mono',monospace;font-size:.63rem;padding:.2rem .6rem;border-radius:99px;border:1px solid var(--border2);color:var(--muted);background:transparent;cursor:default}
.card-badge.btn{cursor:pointer}.card-badge.btn:hover{border-color:var(--blue);color:var(--blue)}
#mensuales-periodo{display:flex;gap:2px;padding:0}.periodo-btn{border:1px solid var(--border2);background:transparent;color:var(--muted);font-family:'DM Mono',monospace;font-size:.6rem;padding:.15rem .45rem;border-radius:4px;cursor:pointer}
.periodo-btn:hover{border-color:var(--blue);color:var(--blue)}.periodo-btn.active{background:var(--blue);border-color:var(--blue);color:#fff}
.card-body{padding:1.5rem}.chart-wrap{position:relative;height:220px}
.tbl{width:100%;border-collapse:collapse;font-size:.78rem}
.tbl th{font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;padding:.6rem 1rem;text-align:left;border-bottom:1px solid var(--border)}
.tbl td{padding:.65rem 1rem;border-bottom:1px solid var(--border);color:var(--text2)}
.tbl tr:last-child td{border:none}.tbl tr:hover td{background:var(--s2);color:var(--text)}
.tbl .num{text-align:right;font-family:'DM Mono',monospace;font-size:.75rem;color:var(--text)}
.tbl .lbl{font-weight:600;color:var(--text)}
.rank-num{display:inline-block;width:22px;height:22px;border-radius:6px;background:var(--s3);font-family:'DM Mono',monospace;font-size:.65rem;text-align:center;line-height:22px;color:var(--muted);margin-right:.4rem}
.vendor-list{display:flex;flex-direction:column;gap:.85rem}
.vendor-item{display:flex;flex-direction:column;gap:.3rem}
.vendor-head{display:flex;align-items:center;justify-content:space-between}
.vendor-name{font-size:.82rem;font-weight:600;color:var(--text)}
.vendor-amount{font-family:'DM Mono',monospace;font-size:.85rem;color:var(--green)}
.vendor-pct{font-family:'DM Mono',monospace;font-size:.65rem;color:var(--muted);margin-left:.5rem}
.vendor-bar-bg{height:5px;background:var(--s3);border-radius:99px;overflow:hidden}
.vendor-bar-fill{height:100%;border-radius:99px;background:linear-gradient(90deg,var(--blue),var(--green));transition:width 1.2s cubic-bezier(.4,0,.2,1)}
.vendor-split{display:flex;gap:.5rem;margin-top:.15rem;font-family:'DM Mono',monospace;font-size:.6rem}
.sk{display:inline-block;background:linear-gradient(90deg,var(--s2) 25%,var(--s3) 50%,var(--s2) 75%);background-size:200%;animation:shimmer 1.5s infinite;border-radius:4px}
@keyframes shimmer{0%{background-position:200% 0}100%{background-position:-200% 0}}
.tipo-badge{display:inline-block;padding:.1rem .4rem;border-radius:4px;font-size:.6rem;font-weight:600;font-family:'DM Mono',monospace}
.tipo-ve{background:var(--blue-dim);color:var(--blue)}.tipo-pv{background:var(--purple-dim);color:var(--purple)}
.split-bar{display:flex;height:8px;border-radius:99px;overflow:hidden;margin-top:.5rem}
.split-bar-ve{background:var(--blue);transition:width .8s}.split-bar-pv{background:var(--purple);transition:width .8s}
</style>
</head>
<body>
<div class="bg-orbs"><div class="orb orb-1"></div><div class="orb orb-2"></div></div>

<header>
  <div class="header-inner">
    <a href="index.html" class="logo">
      <div class="logo-icon"><svg viewBox="0 0 24 24"><path d="M3 13h8V3H3v10zm0 8h8v-6H3v6zm10 0h8V11h-8v10zm0-18v6h8V3h-8z"/></svg></div>
      <div><div class="logo-text">Suminregio Parker</div><div class="logo-sub">ERP Dashboard</div></div>
    </a>
    <nav>
      <a href="index.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"/></svg>Inicio</a>
      <a href="director.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M16 11c1.66 0 2.99-1.34 2.99-3S17.66 5 16 5c-1.66 0-3 1.34-3 3s1.34 3 3 3zm-8 0c1.66 0 2.99-1.34 2.99-3S9.66 5 8 5C6.34 5 5 6.34 5 8s1.34 3 3 3zm0 2c-2.33 0-7 1.17-7 3.5V19h14v-2.5c0-2.33-4.67-3.5-7-3.5z"/></svg>Director</a>
      <a href="ventas.html" class="nav-link active"><svg viewBox="0 0 24 24"><path d="M7 18c-1.1 0-1.99.9-1.99 2S5.9 22 7 22s2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.59-1.35 2.45c-.16.28-.25.61-.25.96C5 16.1 6.9 18 9 18h12v-2H9.42c-.14 0-.25-.11-.25-.25l.03-.12.9-1.63H19c.75 0 1.41-.41 1.75-1.03l3.58-6.49A1 1 0 0023.46 5H5.21l-.94-2H1zm16 16c-1.1 0-1.99.9-1.99 2s.89 2 1.99 2 2-.9 2-2-.9-2-2-2z"/></svg>Ventas</a>
      <a href="cxc.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M11.8 10.9c-2.27-.59-3-1.2-3-2.15 0-1.09 1.01-1.85 2.7-1.85 1.78 0 2.44.85 2.5 2.1h2.21c-.07-1.72-1.12-3.3-3.21-3.81V3h-3v2.16c-1.94.42-3.5 1.68-3.5 3.61 0 2.31 1.91 3.46 4.7 4.13 2.5.6 3 1.48 3 2.41 0 .69-.49 1.79-2.7 1.79-2.06 0-2.87-.92-2.98-2.1h-2.2c.12 2.19 1.76 3.42 3.68 3.83V21h3v-2.15c1.95-.37 3.5-1.5 3.5-3.55 0-2.84-2.43-3.81-4.7-4.4z"/></svg>CXC</a>
      <a href="inventario.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M20 7H4c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V9c0-1.1-.9-2-2-2zm-8 9c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3zM4 3h16v2H4z"/></svg>Inventario</a>
    </nav>
    <div class="header-right">
      <div class="live-pill"><div class="live-dot"></div>LIVE</div>
      <div class="clock" id="clock">--:--:--</div>
    </div>
  </div>
</header>

<div class="page">
  <div class="page-header">
    <div>
      <div class="page-title">Ventas</div>
      <div class="page-sub" id="page-sub">Cargando datos...</div>
    </div>
  </div>

  <!-- Barra de filtros compartida -->
  <div id="filter-bar"></div>

  <div class="source-tabs">
    <button class="tab-btn tab-all active" onclick="setTipo('')" id="tab-all">Todos</button>
    <button class="tab-btn tab-ve" onclick="setTipo('VE')" id="tab-ve">Industrial (VE)</button>
    <button class="tab-btn tab-pv" onclick="setTipo('PV')" id="tab-pv">Mostrador (PV)</button>
    <span style="margin-left:auto;font-family:'DM Mono',monospace;font-size:.65rem;color:var(--muted)" id="split-info"></span>
  </div>

  <div class="kpi-grid">
    <div class="kpi-card blue">
      <div class="kpi-label">Hoy</div>
      <div class="kpi-value" id="kpi-hoy">?</div>
      <div class="kpi-sub" id="kpi-hoy-sub">&nbsp;</div>
      <div class="meta-bar-wrap"><div class="meta-bar-bg"><div class="meta-bar-fill" id="bar-hoy" style="width:0%"></div></div><div class="meta-pct" id="pct-hoy">&nbsp;</div></div>
    </div>
    <div class="kpi-card green">
      <div class="kpi-label">Mes Actual</div>
      <div class="kpi-value" id="kpi-mes">?</div>
      <div class="kpi-sub" id="kpi-mes-sub">&nbsp;</div>
      <div class="meta-bar-wrap"><div class="meta-bar-bg"><div class="meta-bar-fill" id="bar-mes" style="width:0%"></div></div><div class="meta-pct" id="pct-mes">&nbsp;</div></div>
    </div>
    <div class="kpi-card yellow">
      <div class="kpi-label">Meta Diaria</div>
      <div class="kpi-value" id="kpi-meta">?</div>
      <div class="kpi-sub" id="kpi-meta-sub">Base / Ideal (+10%)</div>
    </div>
    <div class="kpi-card orange">
      <div class="kpi-label">Facturas Mes</div>
      <div class="kpi-value" id="kpi-facturas">?</div>
      <div class="kpi-sub" id="kpi-facturas-sub">&nbsp;</div>
    </div>
    <div class="kpi-card purple">
      <div class="kpi-label">Prom. Diario</div>
      <div class="kpi-value" id="kpi-prom">?</div>
      <div class="kpi-sub" id="kpi-prom-sub">D?as transcurridos</div>
    </div>
  </div>

  <!-- Split VE/PV -->
  <div class="card" style="margin-bottom:1.25rem">
    <div class="card-head">
      <div class="card-title"><svg viewBox="0 0 24 24"><path d="M3 3h8v8H3zm10 0h8v8h-8zM3 13h8v8H3zm10 0h8v8h-8z"/></svg>Industrial vs Mostrador ? Mes Actual</div>
      <div class="card-badge" id="split-badge">Cargando...</div>
    </div>
    <div class="card-body" style="padding:1rem 1.5rem">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:.5rem">
        <div style="display:flex;gap:1.5rem">
          <div><div class="kpi-label" style="margin:0">Industrial (VE)</div><div style="font-family:'DM Mono',monospace;font-size:1rem;color:var(--blue)" id="split-ve-amt">?</div></div>
          <div><div class="kpi-label" style="margin:0">Mostrador (PV)</div><div style="font-family:'DM Mono',monospace;font-size:1rem;color:var(--purple)" id="split-pv-amt">?</div></div>
        </div>
        <div style="text-align:right"><div class="kpi-label" style="margin:0">Total Combinado</div><div style="font-family:'DM Mono',monospace;font-size:1rem;color:var(--green)" id="split-total">?</div></div>
      </div>
      <div class="split-bar"><div class="split-bar-ve" id="split-bar-ve" style="width:50%"></div><div class="split-bar-pv" id="split-bar-pv" style="width:50%"></div></div>
      <div style="display:flex;justify-content:space-between;margin-top:.3rem;font-family:'DM Mono',monospace;font-size:.62rem;color:var(--muted)">
        <span id="split-ve-pct">VE ?%</span><span id="split-pv-pct">PV ?%</span>
      </div>
    </div>
  </div>

  <div class="grid-2">
    <div class="card">
      <div class="card-head">
        <div class="card-title"><svg viewBox="0 0 24 24"><path d="M3.5 18.49l6-6.01 4 4L22 6.92l-1.41-1.41-7.09 7.97-4-4L2 16.99z"/></svg>Ventas Diarias</div>
        <div style="display:flex;gap:.4rem">
          <button onclick="setDias(14)" class="card-badge btn" id="btn-14d">14d</button>
          <button onclick="setDias(30)" class="card-badge btn" id="btn-30d" style="border-color:var(--blue);color:var(--blue)">30d</button>
          <button onclick="setDias(60)" class="card-badge btn" id="btn-60d">60d</button>
        </div>
      </div>
      <div class="card-body"><div class="chart-wrap"><canvas id="chartDiarias"></canvas></div></div>
    </div>
    <div class="card">
      <div class="card-head">
        <div class="card-title"><svg viewBox="0 0 24 24"><path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-7 3c1.93 0 3.5 1.57 3.5 3.5S13.93 13 12 13s-3.5-1.57-3.5-3.5S10.07 6 12 6zm7 13H5v-.23c0-.62.28-1.2.76-1.58C7.47 15.82 9.64 15 12 15s4.53.82 6.24 2.19c.48.38.76.97.76 1.58V19z"/></svg>Por Vendedor (mes actual)</div>
        <div class="card-badge" id="vendedor-badge">?</div>
      </div>
      <div class="card-body" id="vendedores-list"><div style="text-align:center;padding:2rem;color:var(--muted)">Cargando...</div></div>
    </div>
  </div>

  <div class="grid-2">
    <div class="card">
      <div class="card-head"><div class="card-title"><svg viewBox="0 0 24 24"><path d="M16 6l2.29 2.29-4.88 4.88-4-4L2 16.59 3.41 18l6-6 4 4 6.3-6.29L22 12V6z"/></svg>Ventas Mensuales (VE + PV)</div><div class="card-badge" id="mensuales-periodo"><button type="button" class="periodo-btn" data-meses="3">3m</button><button type="button" class="periodo-btn" data-meses="6">6m</button><button type="button" class="periodo-btn active" data-meses="12">12m</button></div></div>
      <div class="card-body"><div class="chart-wrap"><canvas id="chartMensuales"></canvas></div></div>
    </div>
    <div class="card">
      <div class="card-head"><div class="card-title"><svg viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-6h2v6zm0-8h-2V7h2v2z"/></svg>Top 10 Clientes</div><div class="card-badge">90 d?as</div></div>
      <div class="card-body">
        <table class="tbl" id="tbl-clientes">
          <thead><tr><th>#</th><th>Cliente</th><th class="num">Total</th><th class="num">Docs</th></tr></thead>
          <tbody><tr><td colspan="4" style="text-align:center;color:var(--muted)">Cargando...</td></tr></tbody>
        </table>
      </div>
    </div>
  </div>

  <!-- COTIZACIONES -->
  <div id="coti-section" style="padding-top:.5rem">
    <div style="margin-bottom:1rem;padding-top:.5rem;border-top:1px solid var(--border)">
      <div style="font-size:1.1rem;font-weight:700;color:var(--text);margin-bottom:.25rem">Cotizaciones</div>
      <div style="font-size:.8rem;color:var(--muted)">Solo m?dulo Industrial ? DOCTOS_VE, TIPO_DOCTO = C</div>
    </div>

    <div class="kpi-grid" style="grid-template-columns:repeat(4,1fr)">
      <div class="kpi-card yellow">
        <div class="kpi-label">Cotiz. Hoy</div>
        <div class="kpi-value" id="kc-hoy">?</div>
        <div class="kpi-sub" id="kc-hoy-imp">&nbsp;</div>
      </div>
      <div class="kpi-card orange">
        <div class="kpi-label">Cotiz. Mes</div>
        <div class="kpi-value" id="kc-mes">?</div>
        <div class="kpi-sub" id="kc-mes-imp">&nbsp;</div>
      </div>
      <div class="kpi-card blue">
        <div class="kpi-label">Importe Mes</div>
        <div class="kpi-value" id="kc-imp">?</div>
        <div class="kpi-sub">Meta: <span id="kc-meta">?</span></div>
        <div class="meta-bar-wrap"><div class="meta-bar-bg"><div class="meta-bar-fill" id="bar-coti" style="width:0%"></div></div><div class="meta-pct" id="pct-coti">&nbsp;</div></div>
      </div>
      <div class="kpi-card green">
        <div class="kpi-label">Conv. Estimada</div>
        <div class="kpi-value" id="kc-conv">?</div>
        <div class="kpi-sub">Ventas / Cotizaciones</div>
      </div>
    </div>

    <div class="grid-2">
      <div class="card">
        <div class="card-head"><div class="card-title"><svg viewBox="0 0 24 24"><path d="M9 17H7v-7h2v7zm4 0h-2V7h2v10zm4 0h-2v-4h2v4z"/></svg>Cotizaciones Diarias</div><div class="card-badge">30 d?as</div></div>
        <div class="card-body"><div class="chart-wrap"><canvas id="chartCotiDiarias"></canvas></div></div>
      </div>
      <div class="card">
        <div class="card-head"><div class="card-title"><svg viewBox="0 0 24 24"><path d="M16 11c1.66 0 2.99-1.34 2.99-3S17.66 5 16 5c-1.66 0-3 1.34-3 3s1.34 3 3 3zm-8 0c1.66 0 2.99-1.34 2.99-3S9.66 5 8 5C6.34 5 5 6.34 5 8s1.34 3 3 3zm0 2c-2.33 0-7 1.17-7 3.5V19h14v-2.5c0-2.33-4.67-3.5-7-3.5z"/></svg>Cotizaciones por Vendedor</div><div class="card-badge">Mes actual</div></div>
        <div class="card-body" id="coti-vendedores-list"><div style="text-align:center;padding:2rem;color:var(--muted)">Cargando...</div></div>
      </div>
    </div>
  </div>

  <!-- -- An?lisis Cotizaciones vs Ventas ----------------------- -->
  <div class="card" style="margin-top:1.25rem;margin-bottom:1.25rem">
    <div class="card-head">
      <div class="card-title">
        <svg viewBox="0 0 24 24"><path d="M18 20V10M12 20V4M6 20v-6"/></svg>
        Conversi?n Cotizaciones ? Ventas
      </div>
      <div class="card-badge" id="conv-badge">6 meses</div>
    </div>
    <div class="card-body">
      <!-- KPI strip -->
      <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:.85rem;margin-bottom:1.25rem">
        <div style="background:var(--s2);border-radius:12px;padding:.9rem 1rem;border:1px solid var(--border)">
          <div style="font-family:'DM Mono',monospace;font-size:.55rem;color:var(--muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.25rem">Total Cotizado</div>
          <div id="cv-total-coti" style="font-family:'DM Mono',monospace;font-size:1.25rem;font-weight:500;color:var(--amber)">?</div>
          <div id="cv-num-coti" style="font-family:'DM Mono',monospace;font-size:.65rem;color:var(--muted);margin-top:.15rem">? cotizaciones</div>
        </div>
        <div style="background:var(--s2);border-radius:12px;padding:.9rem 1rem;border:1px solid var(--border)">
          <div style="font-family:'DM Mono',monospace;font-size:.55rem;color:var(--muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.25rem">Total Vendido</div>
          <div id="cv-total-ventas" style="font-family:'DM Mono',monospace;font-size:1.25rem;font-weight:500;color:var(--blue)">?</div>
          <div id="cv-num-ventas" style="font-family:'DM Mono',monospace;font-size:.65rem;color:var(--muted);margin-top:.15rem">? documentos</div>
        </div>
        <div style="background:var(--s2);border-radius:12px;padding:.9rem 1rem;border:1px solid var(--border)">
          <div style="font-family:'DM Mono',monospace;font-size:.55rem;color:var(--muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.25rem">Tasa Conversi?n $</div>
          <div id="cv-tasa" style="font-family:'DM Mono',monospace;font-size:1.55rem;font-weight:500;color:var(--green)">?</div>
          <div style="font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted);margin-top:.15rem">de lo cotizado se vende</div>
        </div>
        <div style="background:var(--s2);border-radius:12px;padding:.9rem 1rem;border:1px solid var(--border)">
          <div style="font-family:'DM Mono',monospace;font-size:.55rem;color:var(--muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.25rem">Tasa Docs (# piezas)</div>
          <div id="cv-tasa-docs" style="font-family:'DM Mono',monospace;font-size:1.55rem;font-weight:500;color:var(--orange)">?</div>
          <div style="font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted);margin-top:.15rem">cotizaciones convertidas</div>
        </div>
      </div>
      <!-- Intelligence bar -->
      <div id="cv-insight" style="background:var(--s3);border:1px solid var(--border);border-left:3px solid var(--orange);border-radius:8px;padding:.75rem 1rem;font-family:'DM Mono',monospace;font-size:.67rem;color:var(--text2);line-height:1.7;margin-bottom:1.1rem">Analizando conversi?n...</div>
      <!-- Chart -->
      <div style="height:220px"><canvas id="chartConversion"></canvas></div>
    </div>
  </div>

  <div class="card" style="margin-top:1rem">
    <div class="card-head"><div class="card-title"><svg viewBox="0 0 24 24"><path d="M13 3c-4.97 0-9 4.03-9 9H1l3.89 3.89.07.14L9 12H6c0-3.87 3.13-7 7-7s7 3.13 7 7-3.13 7-7 7c-1.93 0-3.68-.79-4.94-2.06l-1.42 1.42C8.27 19.99 10.51 21 13 21c4.97 0 9-4.03 9-9s-4.03-9-9-9zm-1 5v5l4.28 2.54.72-1.21-3.5-2.08V8H12z"/></svg>Documentos Recientes</div><div class="card-badge" id="rec-badge">?ltimos 20</div></div>
    <div class="card-body" style="padding:0">
      <table class="tbl" id="tbl-recientes">
        <thead><tr><th>Folio</th><th>Tipo</th><th>Fuente</th><th>Cliente</th><th>Vendedor</th><th class="num">Total</th><th>Fecha</th></tr></thead>
        <tbody><tr><td colspan="7" style="text-align:center;color:var(--muted);padding:1.5rem">Cargando...</td></tr></tbody>
      </table>
    </div>
  </div>
</div>

<script>
const API = '';
function fwt(url,fallback,ms=10000){return Promise.race([fetch(url).then(r=>r.json()).catch(()=>fallback),new Promise(r=>setTimeout(()=>r(fallback),ms))]);}
let chartDiarias, chartMensuales, chartCotiDiarias, chartConvInst;
let currentTipo = '';
let currentDias = 30;
let currentMeses = 12;
let metas = {};

// -- Helper: construye URL con filtros activos + params extra ---------------
function buildApiUrl(path, extraParams) {
  const qs    = window.filterBuildQS ? filterBuildQS() : '';
  const extra = extraParams ? String(extraParams).replace(/^[?&]+/, '') : '';
  const combined = [qs, extra].filter(Boolean).join('&');
  return API + path + (combined ? '?' + combined : '');
}

// -- Conversi?n Cotizaciones ? Ventas ----------------------------------
async function loadConversion(){
  try{
    const data = await fwt(API+'/api/ventas/vs-cotizaciones?meses=6', {}).catch(()=>({}));
    const ventasArr = Array.isArray(data.ventas) ? data.ventas : [];
    const cotiArr   = Array.isArray(data.cotizaciones) ? data.cotizaciones : [];
    if(!ventasArr.length && !cotiArr.length) return;

    // Build month key map
    const mapa = {};
    cotiArr.forEach(c=>{
      const k=`${c.ANIO}-${String(c.MES).padStart(2,'0')}`;
      mapa[k]={ ANIO:c.ANIO, MES:c.MES, COTI:+c.TOTAL_COTI||0, NUM_COTI:+c.NUM_COTI||0, VENTAS:0, NUM_VEN:0 };
    });
    ventasArr.forEach(v=>{
      const k=`${v.ANIO}-${String(v.MES).padStart(2,'0')}`;
      if(mapa[k]){ mapa[k].VENTAS=+v.TOTAL_VENTAS||0; mapa[k].NUM_VEN=+v.NUM_DOCS||0; }
      else mapa[k]={ ANIO:v.ANIO, MES:v.MES, COTI:0, NUM_COTI:0, VENTAS:+v.TOTAL_VENTAS||0, NUM_VEN:+v.NUM_DOCS||0 };
    });
    const rows = Object.values(mapa).sort((a,b)=>a.ANIO!==b.ANIO?a.ANIO-b.ANIO:a.MES-b.MES);

    const MESES_ES=['','Ene','Feb','Mar','Abr','May','Jun','Jul','Ago','Sep','Oct','Nov','Dic'];
    const labels  = rows.map(r=>(MESES_ES[+r.MES]||r.MES)+' '+(+r.ANIO||''));
    const cotiVals= rows.map(r=>r.COTI);
    const venVals = rows.map(r=>r.VENTAS);
    const tasas   = rows.map(r=>r.COTI>0?Math.round(r.VENTAS/r.COTI*100):0);

    const totCoti = cotiVals.reduce((s,v)=>s+v,0);
    const totVen  = venVals.reduce((s,v)=>s+v,0);
    const numCoti = cotiArr.reduce((s,r)=>s+(+r.NUM_COTI||0),0);
    const numVen  = ventasArr.reduce((s,r)=>s+(+r.NUM_DOCS||0),0);
    const tasaAmt = totCoti>0?Math.round(totVen/totCoti*100):0;
    const tasaDocs= numCoti>0?Math.round(numVen/numCoti*100):0;

    const setTxt=(id,v)=>{const e=document.getElementById(id);if(e)e.textContent=v;};
    const setH  =(id,v)=>{const e=document.getElementById(id);if(e)e.innerHTML=v;};

    setTxt('cv-total-coti', fmtK(totCoti));
    setTxt('cv-num-coti',   numCoti+' cotizaciones');
    setTxt('cv-total-ventas', fmtK(totVen));
    setTxt('cv-num-ventas', numVen+' documentos');
    setTxt('cv-tasa', tasaAmt+'%');
    setTxt('cv-tasa-docs', tasaDocs+'%');

    // Color conversion rate
    const tasaEl = document.getElementById('cv-tasa');
    if(tasaEl) tasaEl.style.color = tasaAmt>=40?'var(--green)':tasaAmt>=25?'var(--amber)':'var(--red)';

    // Insight
    const msg = tasaAmt>=40
      ? `<span style="color:var(--green)">&#10003; Tasa de conversi&#243;n saludable</span>: por cada $100 cotizados se venden $${tasaAmt}. `
      : tasaAmt>=25
      ? `<span style="color:var(--amber)">&#9888; Tasa moderada</span>: por cada $100 cotizados se venden $${tasaAmt}. Oportunidad de mejorar el seguimiento. `
      : `<span style="color:var(--red)">&#10005; Tasa baja</span>: solo $${tasaAmt} de cada $100 cotizados se convierten en venta. Revisar proceso comercial. `;
    const lastTasa = tasas[tasas.length-1]||0;
    const prevTasa = tasas[tasas.length-2]||0;
    const tendMsg  = lastTasa>=prevTasa
      ? `<span style="color:var(--green)">&#8593; mejora en el &#250;ltimo mes (${lastTasa}% vs ${prevTasa}% anterior)</span>.`
      : `<span style="color:var(--red)">&#8595; baja en el &#250;ltimo mes (${lastTasa}% vs ${prevTasa}% anterior)</span>.`;
    setH('cv-insight', msg+' Tendencia reciente: '+tendMsg
      +`<br><span style="color:var(--muted)">Total 6 meses: ${fmtK(totCoti)} cotizado, ${fmtK(totVen)} vendido (${numCoti} cotizaciones, ${numVen} facturas)</span>`);

    // Chart
    const ctx = document.getElementById('chartConversion');
    if(ctx){
      if(chartConvInst) chartConvInst.destroy();
      chartConvInst = new Chart(ctx, {
        data:{labels,datasets:[
          {type:'bar',label:'Cotizaciones',data:cotiVals,backgroundColor:'rgba(255,184,0,.28)',borderColor:'#FFB800',borderWidth:1.5,borderRadius:4},
          {type:'bar',label:'Ventas',data:venVals,backgroundColor:'rgba(30,127,217,.32)',borderColor:'#1E7FD9',borderWidth:1.5,borderRadius:4},
          {type:'line',label:'% Conversi\u00f3n',data:tasas,borderColor:'#F57C00',backgroundColor:'rgba(245,124,0,.1)',borderWidth:2.5,pointRadius:5,pointBackgroundColor:'#F57C00',fill:true,tension:.35,yAxisID:'y2'}
        ]},
        options:{responsive:true,maintainAspectRatio:false,
          plugins:{legend:{labels:{color:'#4D6E8A',font:{size:11,family:"'DM Mono'"}}},
            tooltip:{backgroundColor:'#112233',borderColor:'rgba(255,255,255,.1)',borderWidth:1,titleColor:'#EDF4FF',bodyColor:'#93B4CC',
              callbacks:{label:c=>c.dataset.label+': '+(c.datasetIndex===2?c.raw+'%':fmtK(c.raw))}}},
          scales:{
            x:{grid:{color:'rgba(255,255,255,.04)'},ticks:{color:'#4D6E8A',font:{size:10,family:"'DM Mono'"}}},
            y:{grid:{color:'rgba(255,255,255,.04)'},ticks:{color:'#4D6E8A',font:{size:10,family:"'DM Mono'"},callback:v=>fmtK(v)}},
            y2:{position:'right',min:0,max:120,grid:{display:false},ticks:{color:'#F57C00',font:{size:10,family:"'DM Mono'"},callback:v=>v+'%'}}
          }}
      });
    }
  }catch(e){console.warn('loadConversion error:',e);}
}

const MESES = ['Ene','Feb','Mar','Abr','May','Jun','Jul','Ago','Sep','Oct','Nov','Dic'];
const fmt  = n => isNaN(+n)||n==null?'?':'$'+Number(n).toLocaleString('es-MX',{minimumFractionDigits:0,maximumFractionDigits:0});
const fmtK = n => {
  if(isNaN(+n)||n==null) return '?';
  n = +n;
  if(Math.abs(n)>=1e6) return '$'+(n/1e6).toFixed(1)+'M';
  if(Math.abs(n)>=1e3) return '$'+(n/1e3).toFixed(0)+'k';
  return '$'+n.toFixed(0);
};
const setText = (id,v) => { const e=document.getElementById(id); if(e) e.textContent=v; };
const setWid  = (id,p) => { const e=document.getElementById(id); if(e) e.style.width=Math.min(+p||0,100)+'%'; };

setInterval(()=>{ document.getElementById('clock').textContent=new Date().toLocaleTimeString('es-MX'); },1000);

function setTipo(t) {
  currentTipo = t;
  ['tab-all','tab-ve','tab-pv'].forEach(id => document.getElementById(id).classList.remove('active'));
  document.getElementById(t===''?'tab-all':t==='VE'?'tab-ve':'tab-pv').classList.add('active');
  loadAll();
}
function setDias(d) {
  currentDias = d;
  ['btn-14d','btn-30d','btn-60d'].forEach(id => { const e=document.getElementById(id); if(e){e.style.borderColor='';e.style.color='';} });
  const bid = d===14?'btn-14d':d===30?'btn-30d':'btn-60d';
  const be = document.getElementById(bid); if(be){be.style.borderColor='var(--blue)';be.style.color='var(--blue)';}
  loadDiarias();
}
const tipoQ = () => currentTipo ? `&tipo=${currentTipo}` : '';

const chartDefaults = () => ({
  responsive:true, maintainAspectRatio:false,
  plugins:{ legend:{ display:true, labels:{ color:'#B0BECE', font:{size:10}, boxWidth:10 } },
    tooltip:{ backgroundColor:'#0D1D35', borderColor:'rgba(255,255,255,.1)', borderWidth:1,
      callbacks:{ label: ctx => ' '+fmt(ctx.raw) } } },
  scales:{
    x:{ grid:{color:'rgba(255,255,255,.04)'}, ticks:{color:'#5A7090',font:{size:9}} },
    y:{ grid:{color:'rgba(255,255,255,.04)'}, ticks:{color:'#5A7090',font:{size:9},callback:v=>fmtK(v)} }
  }
});

async function loadAll() {
  try {
    const m = await fwt(`${API}/api/config/metas`, {});
    metas = m;
    setText('kpi-meta', fmtK(m.META_TOTAL_DIARIA));
    setText('kpi-meta-sub', `Ideal (+10%): ${fmtK(m.META_IDEAL_TOTAL)}`);
    setText('kc-meta', fmtK(m.META_COTI_TOTAL));
  } catch(e){}

  await Promise.all([loadResumen(), loadSplit(), loadDiarias(), loadMensuales(), loadVendedores(), loadClientes(), loadCotizaciones(), loadRecientes(), loadConversion()]);
  const params = window.filterGetParams ? filterGetParams() : {};
  const labelParts = [];
  if(params.desde && params.hasta) labelParts.push(params.desde === params.hasta ? params.desde : `${params.desde} ? ${params.hasta}`);
  else if(params.anio && params.mes) labelParts.push(`${['Ene','Feb','Mar','Abr','May','Jun','Jul','Ago','Sep','Oct','Nov','Dic'][params.mes-1]} ${params.anio}`);
  else if(params.anio) labelParts.push(`A?o ${params.anio}`);
  labelParts.push(currentTipo||'Todos');
  setText('page-sub', `Actualizado: ${new Date().toLocaleTimeString('es-MX')} ? ${labelParts.join(' ? ')}`);
}

async function loadResumen() {
  try {
    const d = await fwt(buildApiUrl('/api/ventas/resumen', tipoQ()), {});
    const hoy=+(d.HOY||0), mes=+(d.MES_ACTUAL||0), docs=+(d.FACTURAS_MES||0);
    const diasMes = new Date().getDate();
    const meta = +(metas.META_TOTAL_DIARIA||5650);
    const ideal = +(metas.META_IDEAL_TOTAL||6215);
    setText('kpi-hoy', fmt(hoy));
    setText('kpi-hoy-sub', `Meta: ${fmtK(meta)}`);
    setText('kpi-mes', fmt(mes));
    setText('kpi-mes-sub', `${docs} docs`);
    setText('kpi-facturas', docs.toLocaleString());
    setText('kpi-prom', fmtK(diasMes>0?mes/diasMes:0));
    setText('kpi-prom-sub', `${diasMes} d?as transcurridos`);
    setWid('bar-hoy', (hoy/meta)*100);
    setWid('bar-mes', (mes/(ideal*diasMes))*100);
    setText('pct-hoy', `${((hoy/meta)*100).toFixed(0)}% de meta ? ${((hoy/ideal)*100).toFixed(0)}% de ideal`);
    setText('pct-mes', `${((mes/(ideal*diasMes))*100).toFixed(0)}% vs meta ideal acumulada`);
  } catch(e){ console.error('resumen',e); }
}

async function loadSplit() {
  try {
    const [dVE,dPV] = await Promise.all([
      fwt(buildApiUrl('/api/ventas/resumen', 'tipo=VE'), {}),
      fwt(buildApiUrl('/api/ventas/resumen', 'tipo=PV'), {}),
    ]);
    const ve=+(dVE.MES_ACTUAL||0), pv=+(dPV.MES_ACTUAL||0), total=ve+pv;
    setText('split-ve-amt', fmt(ve));
    setText('split-pv-amt', fmt(pv));
    setText('split-total', fmt(total));
    setText('split-badge', `Total: ${fmt(total)}`);
    if(total>0){
      const vp=(ve/total*100), pp=(pv/total*100);
      setWid('split-bar-ve', vp); setWid('split-bar-pv', pp);
      setText('split-ve-pct', `VE ${vp.toFixed(0)}%`);
      setText('split-pv-pct', `PV ${pp.toFixed(0)}%`);
      setText('split-info', `VE: ${vp.toFixed(0)}% | PV: ${pp.toFixed(0)}%`);
    }
  } catch(e){}
}

async function loadDiarias() {
  try {
    const rows = await fwt(buildApiUrl('/api/ventas/diarias', `dias=${currentDias}${tipoQ()}`), []);
    const meta = +(metas.META_TOTAL_DIARIA||5650);
    const ideal = +(metas.META_IDEAL_TOTAL||6215);
    let labels, metaL, idealL, datasets;
    if (!rows.length) {
      const today = new Date();
      labels = []; const data0 = [];
      for (let i = currentDias - 1; i >= 0; i--) {
        const d = new Date(today); d.setDate(d.getDate() - i);
        labels.push(`${d.getDate()}/${MESES[d.getMonth()]}`);
        data0.push(0);
      }
      metaL = data0.map(() => meta);
      idealL = data0.map(() => ideal);
      datasets = currentTipo === '' ? [
        {label:'Industrial (VE)', data: data0.slice(), borderColor:'#4DA6FF', borderWidth:2, fill:true, tension:.4},
        {label:'Mostrador (PV)', data: data0.slice(), borderColor:'#9B6DFF', borderWidth:2, fill:true, tension:.4},
        {label:'Meta', data: metaL, borderColor:'rgba(255,184,0,.5)', borderWidth:1, borderDash:[4,4], pointRadius:0, fill:false},
        {label:'Ideal (+10%)', data: idealL, borderColor:'rgba(0,229,160,.35)', borderWidth:1, borderDash:[2,4], pointRadius:0, fill:false},
      ] : [
        {label:'Ventas', data: data0, borderColor: currentTipo==='VE'?'#4DA6FF':'#9B6DFF', borderWidth:2, fill:true, tension:.4},
        {label:'Meta', data: metaL, borderColor:'rgba(255,184,0,.5)', borderWidth:1, borderDash:[4,4], pointRadius:0, fill:false},
      ];
    } else {
      labels = rows.map(r=>{ const d = new Date(r.DIA); return `${d.getDate()}/${MESES[d.getMonth()]}`; });
      metaL = rows.map(()=>meta);
      idealL = rows.map(()=>ideal);
      datasets = currentTipo==='' ? [
        {label:'Industrial (VE)', data:rows.map(r=>r.VENTAS_VE||0), backgroundColor:'rgba(77,166,255,.15)', borderColor:'#4DA6FF', borderWidth:2, fill:true, tension:.4},
        {label:'Mostrador (PV)', data:rows.map(r=>r.VENTAS_PV||0), backgroundColor:'rgba(155,109,255,.15)', borderColor:'#9B6DFF', borderWidth:2, fill:true, tension:.4},
        {label:'Meta', data:metaL, borderColor:'rgba(255,184,0,.5)', borderWidth:1, borderDash:[4,4], pointRadius:0, fill:false},
        {label:'Ideal (+10%)', data:idealL, borderColor:'rgba(0,229,160,.35)', borderWidth:1, borderDash:[2,4], pointRadius:0, fill:false},
      ] : [
        {label:'Ventas', data:rows.map(r=>r.TOTAL_VENTAS||0), backgroundColor:currentTipo==='VE'?'rgba(77,166,255,.15)':'rgba(155,109,255,.15)', borderColor:currentTipo==='VE'?'#4DA6FF':'#9B6DFF', borderWidth:2, fill:true, tension:.4},
        {label:'Meta', data:metaL, borderColor:'rgba(255,184,0,.5)', borderWidth:1, borderDash:[4,4], pointRadius:0, fill:false},
      ];
    }
    const opts = chartDefaults();
    opts.scales.y.min = 0;
    if (meta > 0) opts.scales.y.suggestedMax = Math.max(meta * 1.2, 1000);
    if(chartDiarias) chartDiarias.destroy();
    chartDiarias = new Chart(document.getElementById('chartDiarias').getContext('2d'), {type:'line', data:{labels, datasets}, options:opts});
  } catch(e){ console.error('diarias',e); }
}

async function loadMensuales() {
  try {
    const rows = await fwt(buildApiUrl('/api/ventas/mensuales', `meses=${currentMeses}`), {});
    const labels = rows.map(r=>`${MESES[r.MES-1]} ${r.ANIO}`);
    if(chartMensuales) chartMensuales.destroy();
    const opts = {...chartDefaults(), scales:{...chartDefaults().scales, x:{...chartDefaults().scales.x, stacked:true}, y:{...chartDefaults().scales.y, stacked:true}}};
    chartMensuales = new Chart(document.getElementById('chartMensuales').getContext('2d'), {type:'bar', data:{labels, datasets:[
      {label:'Industrial (VE)', data:rows.map(r=>r.VENTAS_VE||0), backgroundColor:'rgba(77,166,255,.55)', borderColor:'#4DA6FF', borderWidth:1},
      {label:'Mostrador (PV)', data:rows.map(r=>r.VENTAS_PV||0), backgroundColor:'rgba(155,109,255,.55)', borderColor:'#9B6DFF', borderWidth:1},
    ]}, options:opts});
  } catch(e){}
}

async function loadVendedores() {
  try {
    const rows = await fwt(buildApiUrl('/api/ventas/por-vendedor', tipoQ()), []);
    const max = rows.reduce((m,r)=>Math.max(m,+(r.VENTAS_MES||0)),0);
    const el = document.getElementById('vendedores-list');
    el.innerHTML = rows.length ? `<div class="vendor-list">${rows.map((r,i)=>{
      const pct = max>0?((+(r.VENTAS_MES||0))/max*100).toFixed(0):0;
      return `<div class="vendor-item">
        <div class="vendor-head">
          <span class="vendor-name"><span class="rank-num">${i+1}</span>${r.VENDEDOR}</span>
          <span><span class="vendor-amount">${fmtK(r.VENTAS_MES)}</span><span class="vendor-pct">${pct}%</span></span>
        </div>
        <div class="vendor-bar-bg"><div class="vendor-bar-fill" style="width:${pct}%"></div></div>
        <div class="vendor-split">
          <span style="color:var(--blue)">VE: ${fmtK(r.VENTAS_MES_VE||0)}</span>
          <span style="color:var(--muted)">|</span>
          <span style="color:var(--purple)">PV: ${fmtK(r.VENTAS_MES_PV||0)}</span>
        </div>
      </div>`;
    }).join('')}</div>` : '<div style="text-align:center;color:var(--muted);padding:1.5rem">Sin datos</div>';
    setText('vendedor-badge', `${rows.length} vendedores`);
  } catch(e){}
}

async function loadClientes() {
  try {
    const rows = await fwt(buildApiUrl('/api/ventas/top-clientes', `limit=10&dias=90${tipoQ()}`), []);
    document.querySelector('#tbl-clientes tbody').innerHTML = rows.map((r,i)=>`<tr>
      <td><span class="rank-num">${i+1}</span></td>
      <td class="lbl">${r.CLIENTE}</td>
      <td class="num">${fmt(r.TOTAL_VENTAS)}</td>
      <td class="num">${r.NUM_FACTURAS}</td>
    </tr>`).join('') || '<tr><td colspan="4" style="text-align:center;color:var(--muted)">Sin datos</td></tr>';
  } catch(e){}
}

async function loadCotizaciones() {
  try {
    const [res, diarias, vends, ventasVE] = await Promise.all([
      fwt(buildApiUrl('/api/ventas/cotizaciones/resumen'), {}),
      fwt(buildApiUrl('/api/ventas/cotizaciones/diarias','dias=30'), []),
      fwt(buildApiUrl('/api/ventas/por-vendedor/cotizaciones'), []),
      fwt(buildApiUrl('/api/ventas/resumen','tipo=VE'), {}),
    ]);
    const resObj = res && typeof res === 'object' ? res : {};
    setText('kc-hoy', resObj.COTIZACIONES_HOY||0);
    setText('kc-hoy-imp', fmt(resObj.HOY));
    setText('kc-mes', resObj.COTIZACIONES_MES||0);
    setText('kc-mes-imp', fmt(resObj.MES_ACTUAL));
    setText('kc-imp', fmt(resObj.MES_ACTUAL));
    const metaC = +(metas.META_COTI_TOTAL||10000);
    const dias = new Date().getDate();
    setWid('bar-coti', ((+(resObj.MES_ACTUAL||0))/(metaC*Math.max(dias,1)))*100);
    setText('pct-coti', `${(((+(resObj.MES_ACTUAL||0))/(metaC*Math.max(dias,1)))*100).toFixed(0)}% de meta acumulada`);
    const conv = +(resObj.MES_ACTUAL||0)>0?(+(ventasVE.MES_ACTUAL||0)/+(resObj.MES_ACTUAL||0)*100).toFixed(0):0;
    setText('kc-conv', `${conv}%`);

    const arr = Array.isArray(diarias) ? diarias : [];
    let lbls, vals, metaL;
    if (!arr.length) {
      const today = new Date();
      lbls = []; vals = [];
      for (let i = 29; i >= 0; i--) {
        const d = new Date(today); d.setDate(d.getDate() - i);
        lbls.push(`${d.getDate()}/${MESES[d.getMonth()]}`);
        vals.push(0);
      }
      metaL = vals.map(() => metaC);
    } else {
      lbls = arr.map(r=>{ const d = new Date(r.DIA); return `${d.getDate()}/${MESES[d.getMonth()]}`; });
      vals = arr.map(r=>r.TOTAL_COTIZACIONES||0);
      metaL = arr.map(()=>metaC);
    }
    const opts = chartDefaults();
    opts.scales.y.min = 0;
    if (metaC > 0) opts.scales.y.suggestedMax = Math.max(metaC * 1.2, 1000);
    if(chartCotiDiarias) chartCotiDiarias.destroy();
    chartCotiDiarias = new Chart(document.getElementById('chartCotiDiarias').getContext('2d'), {type:'bar', data:{labels:lbls, datasets:[
      {label:'Cotizaciones', data:vals, backgroundColor:'rgba(255,184,0,.3)', borderColor:'#FFB800', borderWidth:1},
      {label:'Meta', data:metaL, type:'line', borderColor:'rgba(255,140,66,.5)', borderWidth:1, borderDash:[4,4], pointRadius:0, fill:false},
    ]}, options:opts});

    const maxV = vends.reduce((m,r)=>Math.max(m,+(r.COTIZACIONES_MES||0)),0);
    document.getElementById('coti-vendedores-list').innerHTML = vends.length ?
      `<div class="vendor-list">${vends.map((r,i)=>{
        const pct = maxV>0?((+(r.COTIZACIONES_MES||0))/maxV*100).toFixed(0):0;
        return `<div class="vendor-item">
          <div class="vendor-head">
            <span class="vendor-name"><span class="rank-num">${i+1}</span>${r.VENDEDOR}</span>
            <span><span class="vendor-amount" style="color:var(--yellow)">${fmtK(r.COTIZACIONES_MES)}</span><span class="vendor-pct">${r.NUM_COTI_MES} coti</span></span>
          </div>
          <div class="vendor-bar-bg"><div style="height:5px;border-radius:99px;background:linear-gradient(90deg,var(--yellow),var(--orange));width:${pct}%;transition:width 1.2s"></div></div>
        </div>`;
      }).join('')}</div>` : '<div style="text-align:center;color:var(--muted);padding:1.5rem">Sin datos</div>';
  } catch(e){ console.error('coti',e); }
}

async function loadRecientes() {
  try {
    const rows = await fwt(buildApiUrl('/api/ventas/recientes', `limit=20${tipoQ()}`), []);
    document.querySelector('#tbl-recientes tbody').innerHTML = rows.map(r=>{
      const src = r.TIPO_SRC==='PV'?'<span class="tipo-badge tipo-pv">PV</span>':'<span class="tipo-badge tipo-ve">VE</span>';
      const tipo = r.TIPO_DOCTO==='F'?'<span class="tag ok">Factura</span>':'<span class="tag warn">Venta</span>';
      const fecha = r.FECHA ? new Date(r.FECHA).toLocaleDateString('es-MX') : '?';
      return `<tr><td class="lbl" style="font-family:'DM Mono',monospace">${r.FOLIO||'?'}</td><td>${tipo}</td><td>${src}</td><td>${r.CLIENTE||'?'}</td><td style="color:var(--muted)">${r.VENDEDOR||'?'}</td><td class="num">${fmt(r.TOTAL!=null?r.TOTAL:r.IMPORTE_NETO)}</td><td style="color:var(--muted);font-family:'DM Mono',monospace;font-size:.72rem">${fecha}</td></tr>`;
    }).join('') || '<tr><td colspan="7" style="text-align:center;color:var(--muted);padding:1.5rem">Sin datos</td></tr>';
    setText('rec-badge', `${rows.length} documentos`);
  } catch(e){}
}

// Carga inicial inmediata ? no depende de filtros
loadAll();
// Barra de filtros: recarga al cambiar selecci?n
initFilters({ containerId:'filter-bar', showVendedor:true, onChange:()=>loadAll() });
document.getElementById('mensuales-periodo')?.addEventListener('click', function(e){
  const btn = e.target.closest('.periodo-btn');
  if(!btn) return;
  const m = parseInt(btn.dataset.meses,10);
  if(isNaN(m)) return;
  currentMeses = m;
  this.querySelectorAll('.periodo-btn').forEach(b=>b.classList.toggle('active', parseInt(b.dataset.meses,10)===m));
  loadMensuales();
});
setInterval(loadAll, 60000);
</script>
<script src="nav.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Suminregio Parker &mdash; Facturas Cobradas</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Mono:ital,wght@0,300;0,400;0,500&display=swap" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<script src="filters.js"></script>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --bg:#060E1A;--s1:#0C1B29;--s2:#112233;--s3:#172D42;--s4:#1D3654;
  --border:rgba(255,255,255,.06);--border2:rgba(255,255,255,.11);
  --green:#00E5A0;--green-dim:rgba(0,229,160,.1);
  --yellow:#FFB800;--yellow-dim:rgba(255,184,0,.12);
  --orange:#FF8C42;--orange-dim:rgba(255,140,66,.1);
  --red:#FF4566;--red-dim:rgba(255,69,102,.12);
  --blue:#4DA6FF;--blue-dim:rgba(77,166,255,.1);
  --purple:#9B6DFF;--purple-dim:rgba(155,109,255,.1);
  --cyan:#00D4FF;--cyan-dim:rgba(0,212,255,.1);
  --text:#E8EFF8;--text2:#B0BECE;--muted:#5A7090;--dim:#243348;
}
html{font-size:16px;-webkit-font-smoothing:antialiased}
body{background:var(--bg);color:var(--text);font-family:'Syne',sans-serif;min-height:100vh}
.bg-orbs{position:fixed;inset:0;pointer-events:none;z-index:0;overflow:hidden}
.orb{position:absolute;border-radius:50%;filter:blur(130px);opacity:.10;animation:drift 20s ease-in-out infinite alternate}
.orb-1{width:700px;height:700px;background:radial-gradient(circle,#00E5A0,transparent 70%);top:-200px;left:-150px}
.orb-2{width:600px;height:600px;background:radial-gradient(circle,#1E7FD9,transparent 70%);bottom:-150px;right:-150px;animation-delay:-7s}
.orb-3{width:400px;height:400px;background:radial-gradient(circle,#F57C00,transparent 70%);top:40%;left:45%;transform:translate(-50%,-50%);animation-delay:-12s;opacity:.06}
@keyframes drift{0%{transform:translate(0,0)}100%{transform:translate(20px,15px)}}
header{position:sticky;top:0;z-index:100;background:rgba(5,11,20,.88);backdrop-filter:blur(24px);border-bottom:1px solid var(--border)}
.header-inner{max-width:1800px;margin:0 auto;height:64px;display:flex;align-items:center;justify-content:space-between;gap:1.5rem;padding:0 2rem}
.logo{display:flex;align-items:center;gap:.75rem;text-decoration:none}
.logo-icon{width:38px;height:38px;background:linear-gradient(135deg,var(--green),var(--blue));border-radius:10px;display:grid;place-items:center;flex-shrink:0;box-shadow:0 0 20px rgba(0,229,160,.3)}
.logo-icon svg{width:22px;height:22px;fill:white}
.logo-text{font-size:.92rem;font-weight:800;color:var(--text)}
.logo-sub{font-size:.6rem;font-family:'DM Mono',monospace;color:var(--muted);letter-spacing:.1em;text-transform:uppercase}
nav{display:flex;align-items:center;gap:.2rem}
.nav-link{display:flex;align-items:center;gap:.45rem;padding:.4rem .85rem;border-radius:8px;font-size:.78rem;font-weight:600;color:var(--muted);text-decoration:none;transition:all .2s}
.nav-link:hover{color:var(--text);background:var(--s2)}
.nav-link.active{color:var(--text);background:var(--s3);border:1px solid var(--border2)}
.nav-link svg{width:14px;height:14px;fill:currentColor;flex-shrink:0}
.header-right{display:flex;align-items:center;gap:1rem}
.live-pill{display:flex;align-items:center;gap:.5rem;background:var(--green-dim);border:1px solid rgba(0,229,160,.25);border-radius:99px;padding:.3rem .8rem;font-family:'DM Mono',monospace;font-size:.65rem;color:var(--green);letter-spacing:.08em;text-transform:uppercase}
.live-dot{width:6px;height:6px;border-radius:50%;background:var(--green);box-shadow:0 0 8px var(--green);animation:pulse 2s ease-in-out infinite}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.35}}
.clock{font-family:'DM Mono',monospace;font-size:.78rem;color:var(--muted);letter-spacing:.05em}
.page{position:relative;z-index:1;max-width:1800px;margin:0 auto;padding:2rem}
.hero{margin-bottom:2rem;display:flex;align-items:flex-end;justify-content:space-between;gap:1rem;flex-wrap:wrap}
.hero-eyebrow{font-family:'DM Mono',monospace;font-size:.65rem;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:.5rem;display:flex;align-items:center;gap:.6rem}
.hero-eyebrow::before{content:'';display:inline-block;width:20px;height:1px;background:var(--green);opacity:.6}
.hero-title{font-size:2.4rem;font-weight:800;line-height:1;letter-spacing:-.03em;background:linear-gradient(135deg,var(--text) 30%,var(--green));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.hero-sub{font-size:.85rem;color:var(--muted);margin-top:.5rem;font-family:'DM Mono',monospace}

/* KPI Grid */
.kpi-grid{display:grid;grid-template-columns:repeat(5,1fr);gap:1rem;margin-bottom:1.5rem}
@media(max-width:1300px){.kpi-grid{grid-template-columns:repeat(3,1fr)}}
@media(max-width:700px){.kpi-grid{grid-template-columns:repeat(2,1fr)}}
.kpi{background:var(--s1);border:1px solid var(--border);border-radius:16px;padding:1.2rem 1.4rem;position:relative;overflow:hidden;transition:all .3s}
.kpi::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,var(--kc),transparent)}
.kpi::after{content:'';position:absolute;top:0;right:0;width:60px;height:60px;background:radial-gradient(circle at top right,var(--kc),transparent 70%);opacity:.07}
.kpi.green{--kc:var(--green)}.kpi.blue{--kc:var(--blue)}.kpi.yellow{--kc:var(--yellow)}.kpi.orange{--kc:var(--orange)}.kpi.purple{--kc:var(--purple)}
.kpi-mod{font-family:'DM Mono',monospace;font-size:.55rem;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:.3rem}
.kpi-label{font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted);letter-spacing:.06em;text-transform:uppercase;margin-bottom:.5rem}
.kpi-val{font-family:'DM Mono',monospace;font-size:1.5rem;font-weight:500;letter-spacing:-.02em;line-height:1}
.kpi.green .kpi-val{color:var(--green)}.kpi.blue .kpi-val{color:var(--blue)}.kpi.yellow .kpi-val{color:var(--yellow)}.kpi.orange .kpi-val{color:var(--orange)}.kpi.purple .kpi-val{color:var(--purple)}
.kpi-sub{margin-top:.4rem;font-family:'DM Mono',monospace;font-size:.63rem;color:var(--muted)}

/* Section divider */
.sd{display:flex;align-items:center;gap:1rem;margin:2rem 0 1.25rem}
.sd-label{font-family:'DM Mono',monospace;font-size:.65rem;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;white-space:nowrap}
.sd-line{flex:1;height:1px;background:var(--border)}

/* Grid layout */
.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:1.25rem;margin-bottom:1.5rem}
@media(max-width:1000px){.grid-2{grid-template-columns:1fr}}
.grid-3{display:grid;grid-template-columns:2fr 1fr;gap:1.25rem;margin-bottom:1.5rem}
@media(max-width:1000px){.grid-3{grid-template-columns:1fr}}

/* Cards */
.card{background:var(--s1);border:1px solid var(--border);border-radius:16px;padding:1.4rem;position:relative;overflow:hidden}
.card-head{display:flex;align-items:center;justify-content:space-between;gap:1rem;margin-bottom:1.2rem}
.card-title{font-size:.82rem;font-weight:700;color:var(--text);letter-spacing:-.01em;display:flex;align-items:center;gap:.5rem}
.card-title svg{width:15px;height:15px;fill:var(--green)}
.card-badge{font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted);background:var(--s2);border:1px solid var(--border);padding:.2rem .6rem;border-radius:99px}

/* Comision table */
.comision-table{width:100%;border-collapse:collapse}
.comision-table th{font-family:'DM Mono',monospace;font-size:.58rem;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;padding:.6rem .75rem;text-align:left;border-bottom:1px solid var(--border)}
.comision-table td{font-size:.8rem;padding:.7rem .75rem;border-bottom:1px solid rgba(255,255,255,.04);color:var(--text2);transition:background .15s}
.comision-table tr:hover td{background:var(--s2)}
.comision-table .num{text-align:right;font-family:'DM Mono',monospace;font-size:.78rem}
.rank-badge{display:inline-flex;align-items:center;justify-content:center;width:22px;height:22px;border-radius:50%;font-size:.65rem;font-weight:700;font-family:'DM Mono',monospace}
.rank-1{background:linear-gradient(135deg,#FFB800,#FF8C42);color:#050B14}
.rank-2{background:linear-gradient(135deg,#B0BECE,#7A9ABE);color:#050B14}
.rank-3{background:linear-gradient(135deg,#CD7F32,#A05C20);color:#E8EFF8}
.rank-n{background:var(--s3);color:var(--muted)}

/* Progress bar */
.prog-wrap{width:100%;background:var(--s3);border-radius:99px;height:5px;overflow:hidden;margin-top:4px}
.prog-fill{height:100%;border-radius:99px;transition:width 1.2s cubic-bezier(.4,0,.2,1)}

/* Donut chart */
.chart-wrap{position:relative;width:160px;height:160px;flex-shrink:0}
.chart-center{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;pointer-events:none}
.chart-center-val{font-family:'DM Mono',monospace;font-size:1.2rem;font-weight:500;color:var(--green)}
.chart-center-lbl{font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted)}

/* Legends */
.legend{display:flex;flex-direction:column;gap:.6rem;flex:1}
.legend-item{display:flex;align-items:center;gap:.6rem;font-family:'DM Mono',monospace;font-size:.72rem}
.legend-dot{width:8px;height:8px;border-radius:50%;flex-shrink:0}
.legend-lbl{color:var(--text2);flex:1}
.legend-val{color:var(--text);font-weight:500}

/* Tabs */
.tabs{display:flex;gap:.3rem;margin-bottom:1.2rem}
.tab{font-family:'DM Mono',monospace;font-size:.68rem;padding:.35rem .85rem;border-radius:8px;border:1px solid var(--border);color:var(--muted);cursor:pointer;background:none;transition:all .2s;letter-spacing:.04em}
.tab.active{color:var(--green);border-color:rgba(0,229,160,.3);background:var(--green-dim)}

/* Detalle facturas */
.fact-list{display:flex;flex-direction:column;gap:.4rem;max-height:380px;overflow-y:auto}
.fact-item{display:grid;grid-template-columns:auto 1fr auto auto;align-items:center;gap:.75rem;padding:.65rem .75rem;background:var(--s2);border-radius:8px;border:1px solid var(--border);font-size:.77rem}
.fact-folio{font-family:'DM Mono',monospace;font-size:.7rem;color:var(--muted)}
.fact-cliente{color:var(--text2);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.fact-monto{font-family:'DM Mono',monospace;color:var(--blue);font-size:.76rem;white-space:nowrap}
.fact-comision{font-family:'DM Mono',monospace;color:var(--green);font-size:.76rem;white-space:nowrap}

/* Bar chart rows */
.bar-row{display:flex;align-items:center;gap:.75rem;padding:.5rem 0}
.bar-row:not(:last-child){border-bottom:1px solid rgba(255,255,255,.04)}
.bar-name{font-size:.78rem;color:var(--text2);width:140px;flex-shrink:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.bar-outer{flex:1;background:var(--s3);border-radius:99px;height:7px;overflow:hidden}
.bar-inner{height:100%;border-radius:99px;transition:width 1.2s cubic-bezier(.4,0,.2,1)}
.bar-val{font-family:'DM Mono',monospace;font-size:.7rem;width:90px;text-align:right;flex-shrink:0}
.bar-com{font-family:'DM Mono',monospace;font-size:.68rem;color:var(--green);width:72px;text-align:right;flex-shrink:0}

/* Period selector */
.period-btns{display:flex;gap:.4rem}
.period-btn{font-family:'DM Mono',monospace;font-size:.65rem;padding:.28rem .7rem;border-radius:6px;border:1px solid var(--border);color:var(--muted);cursor:pointer;background:none;transition:all .2s}
.period-btn.active{color:var(--green);border-color:rgba(0,229,160,.3);background:var(--green-dim)}

/* Empty state */
.empty{text-align:center;padding:2rem;color:var(--muted);font-family:'DM Mono',monospace;font-size:.78rem}
</style>
</head>
<body>
<div class="bg-orbs"><div class="orb orb-1"></div><div class="orb orb-2"></div><div class="orb orb-3"></div></div>

<header>
  <div class="header-inner">
    <a href="index.html" class="logo">
      <div class="logo-icon">
        <svg viewBox="0 0 24 24"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
      </div>
      <div><div class="logo-text">Suminregio Parker</div><div class="logo-sub">ERP Dashboard</div></div>
    </a>
    <nav>
      <a href="index.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>Inicio</a>
      <a href="ventas.html" class="nav-link"><svg viewBox="0 0 24 24"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>Ventas</a>
      <a href="cobradas.html" class="nav-link active"><svg viewBox="0 0 24 24"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>Cobradas</a>
      <a href="vendedores.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>Vendedores</a>
      <a href="cxc.html" class="nav-link"><svg viewBox="0 0 24 24"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>CxC</a>
      <a href="clientes.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>Clientes</a>
      <a href="inventario.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 2 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/></svg>Inventario</a>
      <a href="resultados.html" class="nav-link"><svg viewBox="0 0 24 24"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>Resultados</a>
    </nav>
    <div class="header-right">
      <div class="live-pill"><div class="live-dot"></div>Live</div>
      <div class="clock" id="reloj">--:--:--</div>
    </div>
  </div>
</header>

<div class="page">
  <!-- Hero -->
  <div class="hero">
    <div>
      <div class="hero-eyebrow">Facturas Cobradas &bull; Comisiones</div>
      <div class="hero-title">Cobradas &amp; Comisiones</div>
      <div class="hero-sub" id="heroSub">Cargando datos...</div>
    </div>
  </div>

  <!-- Barra de filtros compartida -->
  <div id="filter-bar"></div>

  <!-- KPIs principales -->
  <div class="kpi-grid">
    <div class="kpi green">
      <div class="kpi-mod">Cobros</div>
      <div class="kpi-label">Total cobrado del mes</div>
      <div class="kpi-val" id="kpiTotalCobrado">--</div>
      <div class="kpi-sub" id="kpiTotalCobradoSub">-- facturas</div>
    </div>
    <div class="kpi blue">
      <div class="kpi-mod">Facturaci&oacute;n</div>
      <div class="kpi-label">Total facturado (base cobros)</div>
      <div class="kpi-val" id="kpiTotalVenta">--</div>
      <div class="kpi-sub" id="kpiEficiencia">-- de conversi&oacute;n</div>
    </div>
    <div class="kpi yellow">
      <div class="kpi-mod">Comisiones</div>
      <div class="kpi-label">Total comisiones 8% cobro</div>
      <div class="kpi-val" id="kpiTotalComision">--</div>
      <div class="kpi-sub">8% sobre monto cobrado</div>
    </div>
    <div class="kpi orange">
      <div class="kpi-mod">Promedio</div>
      <div class="kpi-label">Ticket promedio cobrado</div>
      <div class="kpi-val" id="kpiTicketProm">--</div>
      <div class="kpi-sub" id="kpiTicketSub">-- facturas en periodo</div>
    </div>
    <div class="kpi purple">
      <div class="kpi-mod">Mejor</div>
      <div class="kpi-label">Vendedor top del mes</div>
      <div class="kpi-val" id="kpiTopVend">--</div>
      <div class="kpi-sub" id="kpiTopVendSub">-- en comisi&oacute;n</div>
    </div>
  </div>

  <!-- Section: Ranking + Donut -->
  <div class="sd"><div class="sd-label">Ranking por Vendedor</div><div class="sd-line"></div></div>
  <div class="grid-3" style="grid-template-columns:2fr 1fr">
    <!-- Ranking tabla completa -->
    <div class="card">
      <div class="card-head">
        <div class="card-title">
          <svg viewBox="0 0 24 24"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
          Comisi&oacute;n por Vendedor
        </div>
        <div class="card-badge">8% del cobro</div>
      </div>
      <table class="comision-table">
        <thead>
          <tr>
            <th>#</th>
            <th>Vendedor</th>
            <th class="num">Facturas</th>
            <th class="num">Venta Base</th>
            <th class="num">Cobrado</th>
            <th class="num">% Cobro</th>
            <th class="num">Comisi&oacute;n 8%</th>
            <th></th>
          </tr>
        </thead>
        <tbody id="rankingBody">
          <tr><td colspan="8" class="empty">Cargando...</td></tr>
        </tbody>
      </table>
    </div>

    <!-- Donut participacion -->
    <div class="card" style="display:flex;flex-direction:column;gap:1.2rem">
      <div class="card-head" style="margin-bottom:0">
        <div class="card-title">
          <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
          Distribuci&oacute;n
        </div>
      </div>
      <div style="display:flex;align-items:center;gap:1.2rem;flex-wrap:wrap">
        <div class="chart-wrap">
          <canvas id="donutChart"></canvas>
          <div class="chart-center">
            <div class="chart-center-val" id="donutPct">--</div>
            <div class="chart-center-lbl">top 1</div>
          </div>
        </div>
        <div class="legend" id="donutLegend"></div>
      </div>
    </div>
  </div>

  <!-- Section: Barras cobranza + Detalle facturas -->
  <div class="sd"><div class="sd-label">An&aacute;lisis Cobrado vs Facturado</div><div class="sd-line"></div></div>
  <div class="grid-2">
    <!-- Barras comparativas -->
    <div class="card">
      <div class="card-head">
        <div class="card-title">
          <svg viewBox="0 0 24 24"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>
          Cobrado vs Facturado por Vendedor
        </div>
        <div class="card-badge">eficiencia cobro</div>
      </div>
      <div id="barrasVend"></div>
    </div>

    <!-- Detalle facturas recientes -->
    <div class="card">
      <div class="card-head">
        <div class="card-title">
          <svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
          &Uacute;ltimas Facturas Cobradas
        </div>
        <div class="tabs">
          <button class="tab active" onclick="setFiltroVend(null,this)">Todos</button>
          <button class="tab" id="tabVend1" onclick="setFiltroVend(0,this)">--</button>
          <button class="tab" id="tabVend2" onclick="setFiltroVend(1,this)">--</button>
        </div>
      </div>
      <div class="fact-list" id="factList"></div>
    </div>
  </div>

  <!-- Section: Evolucion mensual -->
  <div class="sd"><div class="sd-label">Evoluci&oacute;n Mensual de Cobros</div><div class="sd-line"></div></div>
  <div class="card" style="margin-bottom:2rem">
    <div class="card-head">
      <div class="card-title">
        <svg viewBox="0 0 24 24"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
        Tendencia Cobros &amp; Comisiones
      </div>
    </div>
    <div style="height:220px"><canvas id="chartEvolucion"></canvas></div>
  </div>
</div>

<script>
const API = '';
function fwt(url,fallback,ms=10000){return Promise.race([fetch(url).then(r=>r.json()).catch(()=>fallback),new Promise(r=>setTimeout(()=>r(fallback),ms))]);}
const COMISION_PCT = 0.08;
let allData = [];
let currentPeriod = 'mes';
let currentVendFiltro = null;
let donutChartInst = null;
let evoChartInst = null;

// â”€â”€ Helper: URL con filtros â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
function buildApiUrl(path, extra) {
  const qs    = window.filterBuildQS ? filterBuildQS() : '';
  const e     = extra ? String(extra).replace(/^[?&]+/, '') : '';
  const parts = [qs, e].filter(Boolean).join('&');
  return API + path + (parts ? '?' + parts : '');
}

setInterval(()=>{
  const n=new Date();
  document.getElementById('reloj').textContent=
    String(n.getHours()).padStart(2,'0')+':'+String(n.getMinutes()).padStart(2,'0')+':'+String(n.getSeconds()).padStart(2,'0');
},1000);

const fmtM = v => {
  if(v===null||v===undefined||isNaN(v))return'--';
  v=+v;
  if(v>=1e6) return '$'+( v/1e6).toFixed(2)+'M';
  if(v>=1e3) return '$'+(v/1e3).toFixed(1)+'K';
  return '$'+Math.round(v).toLocaleString();
};
const pct = (a,b)=> b>0?Math.round(a/b*100):0;

function setPeriod(p){
  currentPeriod=p;
  document.querySelectorAll('.period-btn').forEach(b=>{
    b.classList.toggle('active', b.textContent.trim().toLowerCase().includes(
      p==='mes'?'este':p==='anterior'?'anterior':'trimes'
    ));
  });
  loadData();
}

function setFiltroVend(idx, btn){
  currentVendFiltro=idx;
  document.querySelectorAll('.tabs .tab').forEach(t=>t.classList.remove('active'));
  btn.classList.add('active');
  renderFactList();
}

function fwt(url, fallback, ms) { ms = ms || 12000; return Promise.race([ fetch(url).then(r=>r.json()).catch(()=>fallback), new Promise(r=>setTimeout(()=>r(fallback), ms)) ]); }
async function loadData(){
  const heroSub = document.getElementById('heroSub');
  if(heroSub) heroSub.textContent = 'Cargando datos...';
  try{
    const r = await fwt(buildApiUrl('/api/ventas/cobradas'), { vendedores: [], totalCobrado: 0, totalFacturado: 0 });
    if (r && Array.isArray(r.vendedores)) {
      allData = r.vendedores;
      totalCobradoGlobal = +r.totalCobrado || 0;
      totalFacturadoGlobal = +r.totalFacturado || 0;
    } else {
      allData = Array.isArray(r) ? r : [];
      totalCobradoGlobal = null;
      totalFacturadoGlobal = null;
    }
    renderAll();
    const ev = await fwt(buildApiUrl('/api/ventas/mensuales', 'meses=13'), []);
    renderEvolucion(Array.isArray(ev) ? ev : []);
  }catch(e){
    allData = []; totalCobradoGlobal = null; totalFacturadoGlobal = null;
    renderAll();
    if(heroSub) heroSub.textContent = 'Error conectando con el servidor';
  }
}

let totalCobradoGlobal = null;
let totalFacturadoGlobal = null;

function renderAll(){
  const d = allData;
  if(!d.length){
    document.getElementById('heroSub').textContent='Sin datos de facturas cobradas';
    return;
  }

  const totalCobrado = totalCobradoGlobal !== null ? totalCobradoGlobal : d.reduce((s,r)=>s+(+r.TOTAL_COBRADO||0),0);
  const totalVenta   = totalFacturadoGlobal !== null ? totalFacturadoGlobal : d.reduce((s,r)=>s+(+r.TOTAL_VENTA||0),0);
  const totalComision= totalCobrado * COMISION_PCT;
  const totalFacturas= d.reduce((s,r)=>s+(+r.NUM_FACTURAS||0),0);
  const ticketProm   = totalFacturas>0 ? totalCobrado/totalFacturas : 0;
  const topVend      = [...d].sort((a,b)=>(+b.TOTAL_COBRADO||0)-(+a.TOTAL_COBRADO||0))[0];

  document.getElementById('heroSub').textContent = d.length+' vendedores \u2022 '+totalFacturas+' facturas \u2022 '+fmtM(totalCobrado)+' cobrado';
  document.getElementById('kpiTotalCobrado').textContent = fmtM(totalCobrado);
  document.getElementById('kpiTotalCobradoSub').textContent = totalFacturas+' facturas cobradas';
  document.getElementById('kpiTotalVenta').textContent = fmtM(totalVenta);
  document.getElementById('kpiTotalVentaSub').textContent = totalFacturas+' facturas publicadas';
  document.getElementById('kpiEficiencia').textContent = pct(totalCobrado,totalVenta)+'% conversi\u00f3n cobro';
  document.getElementById('kpiTotalComision').textContent = fmtM(totalComision);
  document.getElementById('kpiTicketProm').textContent = fmtM(ticketProm);
  document.getElementById('kpiTicketSub').textContent = totalFacturas+' facturas en periodo';
  document.getElementById('kpiTopVend').textContent = (topVend?.VENDEDOR||topVend?.NOMBRE||'--').split(' ')[0];
  document.getElementById('kpiTopVendSub').textContent = fmtM((+topVend?.TOTAL_COBRADO||0)*COMISION_PCT)+' comisi\u00f3n';

  renderRanking(d, totalCobrado);
  renderBarras(d);
  renderDonut(d, totalCobrado);
  renderFactList();
  renderTabsVend(d);
}

function renderRanking(d, totalCobrado){
  const sorted = [...d].sort((a,b)=>(+b.TOTAL_COBRADO||0)-(+a.TOTAL_COBRADO||0));
  const cols=['#00E5A0','#4DA6FF','#9B6DFF','#FFB800','#FF8C42','#FF4566','#00D4FF'];
  const rows = sorted.map((r,i)=>{
    const cobrado   = +r.TOTAL_COBRADO||0;
    const venta     = +r.TOTAL_VENTA||0;
    const comision  = cobrado*COMISION_PCT;
    const pctCobro  = pct(cobrado,venta);
    const pctPart   = pct(cobrado,totalCobrado);
    const col       = cols[i%cols.length];
    const rankHtml  = i===0?`<span class="rank-badge rank-1">1</span>`
                    : i===1?`<span class="rank-badge rank-2">2</span>`
                    : i===2?`<span class="rank-badge rank-3">3</span>`
                    : `<span class="rank-badge rank-n">${i+1}</span>`;
    return `<tr>
      <td style="padding:.7rem .75rem">${rankHtml}</td>
      <td style="padding:.7rem .75rem;font-size:.8rem;color:var(--text)">${r.VENDEDOR||r.NOMBRE||'Vendedor '+(i+1)}</td>
      <td class="num" style="color:var(--text2)">${+r.NUM_FACTURAS||0}</td>
      <td class="num" style="color:var(--blue)">${fmtM(venta)}</td>
      <td class="num" style="color:var(--green)">${fmtM(cobrado)}</td>
      <td class="num">
        <span style="color:${pctCobro>=80?'var(--green)':pctCobro>=50?'var(--yellow)':'var(--red)'}">${pctCobro}%</span>
        <div class="prog-wrap"><div class="prog-fill" style="width:${pctCobro}%;background:${col}"></div></div>
      </td>
      <td class="num" style="color:var(--yellow);font-weight:600">${fmtM(comision)}</td>
      <td style="padding:.7rem .75rem"><span style="font-family:'DM Mono',monospace;font-size:.65rem;color:${col}">${pctPart}%</span></td>
    </tr>`;
  });
  document.getElementById('rankingBody').innerHTML = rows.join('') || '<tr><td colspan="8" class="empty">Sin datos</td></tr>';
}

function renderBarras(d){
  const sorted = [...d].sort((a,b)=>(+b.TOTAL_COBRADO||0)-(+a.TOTAL_COBRADO||0));
  const maxCob = +sorted[0]?.TOTAL_COBRADO||1;
  const cols=['#00E5A0','#4DA6FF','#9B6DFF','#FFB800','#FF8C42','#FF4566','#00D4FF'];
  const html = sorted.map((r,i)=>{
    const cob  = +r.TOTAL_COBRADO||0;
    const ven  = +r.TOTAL_VENTA||0;
    const nom  = (r.VENDEDOR||r.NOMBRE||'V'+(i+1)).split(' ')[0];
    const pCob = Math.round(cob/maxCob*100);
    const pEfi = pct(cob,ven);
    return `<div class="bar-row">
      <div class="bar-name" title="${r.VENDEDOR||r.NOMBRE}">${nom}</div>
      <div class="bar-outer"><div class="bar-inner" style="width:${pCob}%;background:${cols[i%cols.length]}"></div></div>
      <div class="bar-val" style="color:${cols[i%cols.length]}">${fmtM(cob)}</div>
      <div class="bar-com">${fmtM(cob*COMISION_PCT)}</div>
    </div>`;
  }).join('');
  document.getElementById('barrasVend').innerHTML = html || '<div class="empty">Sin datos</div>';
}

function renderDonut(d, totalCobrado){
  const sorted = [...d].sort((a,b)=>(+b.TOTAL_COBRADO||0)-(+a.TOTAL_COBRADO||0)).slice(0,6);
  const cols=['#00E5A0','#4DA6FF','#9B6DFF','#FFB800','#FF8C42','#FF4566'];
  const vals = sorted.map(r=>+r.TOTAL_COBRADO||0);
  const labels = sorted.map(r=>(r.VENDEDOR||r.NOMBRE||'?').split(' ')[0]);
  const topPct = totalCobrado>0?Math.round(vals[0]/totalCobrado*100):0;
  document.getElementById('donutPct').textContent = topPct+'%';
  const ctx = document.getElementById('donutChart');
  if(donutChartInst)donutChartInst.destroy();
  donutChartInst = new Chart(ctx,{
    type:'doughnut',
    data:{labels,datasets:[{data:vals,backgroundColor:cols,borderColor:'#0A1628',borderWidth:3,hoverBorderWidth:0}]},
    options:{cutout:'72%',plugins:{legend:{display:false},tooltip:{backgroundColor:'#0D1D35',borderColor:'rgba(255,255,255,.1)',borderWidth:1,titleColor:'#E8EFF8',bodyColor:'#B0BECE',callbacks:{label:c=>' '+fmtM(c.raw)}}},animation:{animateRotate:true,duration:1000}}
  });
  document.getElementById('donutLegend').innerHTML = sorted.map((r,i)=>`
    <div class="legend-item">
      <div class="legend-dot" style="background:${cols[i]}"></div>
      <div class="legend-lbl">${labels[i]}</div>
      <div class="legend-val" style="color:${cols[i]}">${fmtM(vals[i])}</div>
    </div>
  `).join('');
}

function renderTabsVend(d){
  const sorted=[...d].sort((a,b)=>(+b.TOTAL_COBRADO||0)-(+a.TOTAL_COBRADO||0));
  ['tabVend1','tabVend2'].forEach((id,i)=>{
    const el=document.getElementById(id);
    if(sorted[i]) el.textContent=(sorted[i].VENDEDOR||sorted[i].NOMBRE||'V'+(i+1)).split(' ')[0];
  });
}

function renderFactList(){
  if(!allData.length){document.getElementById('factList').innerHTML='<div class="empty">Sin facturas</div>';return}
  let data = allData;
  if(currentVendFiltro!==null){
    const sorted=[...data].sort((a,b)=>(+b.TOTAL_COBRADO||0)-(+a.TOTAL_COBRADO||0));
    const vend=sorted[currentVendFiltro];
    if(vend) data=[vend];
  }
  const html = data.map(r=>{
    const cobrado=(+r.TOTAL_COBRADO||0);
    const comision=cobrado*COMISION_PCT;
    const nombre=(r.VENDEDOR||r.NOMBRE||'?').split(' ').slice(0,2).join(' ');
    return `<div class="fact-item">
      <div class="fact-folio">${+r.NUM_FACTURAS||0} facts.</div>
      <div class="fact-cliente">${nombre}</div>
      <div class="fact-monto">${fmtM(cobrado)}</div>
      <div class="fact-comision">${fmtM(comision)}</div>
    </div>`;
  }).join('');
  document.getElementById('factList').innerHTML=html||'<div class="empty">Sin datos</div>';
}

function renderEvolucion(data){
  if(!Array.isArray(data)||!data.length) return;
  // /api/ventas/mensuales devuelve ANIO + MES (separados) y TOTAL_VENTAS
  const MESES_ES=['Ene','Feb','Mar','Abr','May','Jun','Jul','Ago','Sep','Oct','Nov','Dic'];
  const labels = data.map(d=>{
    if(d.ANIO && d.MES) return (MESES_ES[(+d.MES||1)-1]||d.MES)+' '+(d.ANIO||'');
    // fallback para otros formatos
    const raw = String(d.FECHA||d.DIA||'').substring(0,10);
    const p = raw.split('-');
    return p.length===3 ? p[2]+'/'+p[1] : raw;
  });
  const ventas = data.map(d=>+(d.TOTAL_VENTAS||d.TOTAL||d.VENTAS_NETAS||d.IMPORTE_NETO||0));
  const comision = ventas.map(v=>v*COMISION_PCT);
  const ctx=document.getElementById('chartEvolucion');
  if(evoChartInst)evoChartInst.destroy();
  evoChartInst=new Chart(ctx,{
    type:'bar',
    data:{
      labels,
      datasets:[
        {label:'Cobrado',data:ventas,backgroundColor:'rgba(77,166,255,.25)',borderColor:'#4DA6FF',borderWidth:1.5,borderRadius:4},
        {label:'Comisi\u00f3n 8%',data:comision,backgroundColor:'rgba(0,229,160,.3)',borderColor:'#00E5A0',borderWidth:1.5,borderRadius:4,type:'line',fill:false,tension:.3,pointRadius:3,pointBackgroundColor:'#00E5A0'}
      ]
    },
    options:{
      responsive:true,maintainAspectRatio:false,
      plugins:{legend:{position:'top',labels:{color:'#5A7090',font:{size:11,family:"'DM Mono'"}}},
        tooltip:{backgroundColor:'#0D1D35',borderColor:'rgba(255,255,255,.1)',borderWidth:1,titleColor:'#E8EFF8',bodyColor:'#B0BECE',callbacks:{label:c=>' '+c.dataset.label+': '+fmtM(c.raw)}}},
      scales:{
        x:{grid:{color:'rgba(255,255,255,.04)'},ticks:{color:'#5A7090',font:{size:10,family:"'DM Mono'"}}},
        y:{grid:{color:'rgba(255,255,255,.04)'},ticks:{color:'#5A7090',font:{size:10,family:"'DM Mono'"},callback:v=>fmtM(v)}}
      }
    }
  });
}

// Carga inicial inmediata
loadData();
initFilters({ containerId:'filter-bar', showVendedor:true, onChange:()=>loadData() });
setInterval(loadData, 120000);
</script>
<script src="nav.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Suminregio Parker &mdash; Vendedores</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Mono:ital,wght@0,300;0,400;0,500&display=swap" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<script src="filters.js"></script>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --bg:#060E1A;--s1:#0C1B29;--s2:#112233;--s3:#172D42;--s4:#1D3654;
  --border:rgba(255,255,255,.06);--border2:rgba(255,255,255,.11);
  --green:#00E5A0;--green-dim:rgba(0,229,160,.1);
  --yellow:#FFB800;--yellow-dim:rgba(255,184,0,.12);
  --orange:#FF8C42;--orange-dim:rgba(255,140,66,.1);
  --red:#FF4566;--red-dim:rgba(255,69,102,.12);
  --blue:#4DA6FF;--blue-dim:rgba(77,166,255,.1);
  --purple:#9B6DFF;--purple-dim:rgba(155,109,255,.1);
  --cyan:#00D4FF;--cyan-dim:rgba(0,212,255,.1);
  --text:#E8EFF8;--text2:#B0BECE;--muted:#5A7090;--dim:#243348;
}
html{font-size:16px;-webkit-font-smoothing:antialiased}
body{background:var(--bg);color:var(--text);font-family:'Syne',sans-serif;min-height:100vh}
.bg-orbs{position:fixed;inset:0;pointer-events:none;z-index:0;overflow:hidden}
.orb{position:absolute;border-radius:50%;filter:blur(130px);opacity:.10;animation:drift 20s ease-in-out infinite alternate}
.orb-1{width:700px;height:700px;background:radial-gradient(circle,#F57C00,transparent 70%);top:-200px;left:-150px}
.orb-2{width:600px;height:600px;background:radial-gradient(circle,#1E7FD9,transparent 70%);bottom:-150px;right:-150px;animation-delay:-7s}
.orb-3{width:400px;height:400px;background:radial-gradient(circle,#00E5A0,transparent 70%);top:40%;left:45%;transform:translate(-50%,-50%);animation-delay:-12s;opacity:.06}
@keyframes drift{0%{transform:translate(0,0)}100%{transform:translate(20px,15px)}}
header{position:sticky;top:0;z-index:100;background:rgba(5,11,20,.88);backdrop-filter:blur(24px);border-bottom:1px solid var(--border)}
.header-inner{max-width:1800px;margin:0 auto;height:64px;display:flex;align-items:center;justify-content:space-between;gap:1.5rem;padding:0 2rem}
.logo{display:flex;align-items:center;gap:.75rem;text-decoration:none}
.logo-icon{width:38px;height:38px;background:linear-gradient(135deg,var(--purple),var(--blue));border-radius:10px;display:grid;place-items:center;flex-shrink:0;box-shadow:0 0 20px rgba(155,109,255,.3)}
.logo-icon svg{width:22px;height:22px;fill:white}
.logo-text{font-size:.92rem;font-weight:800;color:var(--text)}
.logo-sub{font-size:.6rem;font-family:'DM Mono',monospace;color:var(--muted);letter-spacing:.1em;text-transform:uppercase}
nav{display:flex;align-items:center;gap:.2rem}
.nav-link{display:flex;align-items:center;gap:.45rem;padding:.4rem .85rem;border-radius:8px;font-size:.78rem;font-weight:600;color:var(--muted);text-decoration:none;transition:all .2s}
.nav-link:hover{color:var(--text);background:var(--s2)}
.nav-link.active{color:var(--text);background:var(--s3);border:1px solid var(--border2)}
.nav-link svg{width:14px;height:14px;fill:currentColor;flex-shrink:0}
.header-right{display:flex;align-items:center;gap:1rem}
.live-pill{display:flex;align-items:center;gap:.5rem;background:var(--purple-dim);border:1px solid rgba(155,109,255,.25);border-radius:99px;padding:.3rem .8rem;font-family:'DM Mono',monospace;font-size:.65rem;color:var(--purple);letter-spacing:.08em;text-transform:uppercase}
.live-dot{width:6px;height:6px;border-radius:50%;background:var(--purple);box-shadow:0 0 8px var(--purple);animation:pulse 2s ease-in-out infinite}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.35}}
.clock{font-family:'DM Mono',monospace;font-size:.78rem;color:var(--muted);letter-spacing:.05em}
.page{position:relative;z-index:1;max-width:1800px;margin:0 auto;padding:2rem}
.hero{margin-bottom:2rem;display:flex;align-items:flex-end;justify-content:space-between;gap:1rem;flex-wrap:wrap}
.hero-eyebrow{font-family:'DM Mono',monospace;font-size:.65rem;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:.5rem;display:flex;align-items:center;gap:.6rem}
.hero-eyebrow::before{content:'';display:inline-block;width:20px;height:1px;background:var(--purple);opacity:.6}
.hero-title{font-size:2.4rem;font-weight:800;line-height:1;letter-spacing:-.03em;background:linear-gradient(135deg,var(--text) 30%,var(--orange));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.hero-sub{font-size:.85rem;color:var(--muted);margin-top:.5rem;font-family:'DM Mono',monospace}

/* KPI strip */
.kpi-strip{display:grid;grid-template-columns:repeat(6,1fr);gap:1rem;margin-bottom:1.5rem}
@media(max-width:1300px){.kpi-strip{grid-template-columns:repeat(3,1fr)}}
@media(max-width:700px){.kpi-strip{grid-template-columns:repeat(2,1fr)}}
.kpi{background:var(--s1);border:1px solid var(--border);border-radius:16px;padding:1.1rem 1.3rem;position:relative;overflow:hidden}
.kpi::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,var(--kc),transparent)}
.kpi.green{--kc:var(--green)}.kpi.blue{--kc:var(--blue)}.kpi.yellow{--kc:var(--yellow)}.kpi.purple{--kc:var(--purple)}.kpi.orange{--kc:var(--orange)}.kpi.red{--kc:var(--red)}
.kpi-lbl{font-family:'DM Mono',monospace;font-size:.58rem;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;margin-bottom:.4rem}
.kpi-val{font-family:'DM Mono',monospace;font-size:1.4rem;font-weight:500;line-height:1;letter-spacing:-.02em}
.kpi.green .kpi-val{color:var(--green)}.kpi.blue .kpi-val{color:var(--blue)}.kpi.yellow .kpi-val{color:var(--yellow)}.kpi.purple .kpi-val{color:var(--purple)}.kpi.orange .kpi-val{color:var(--orange)}.kpi.red .kpi-val{color:var(--red)}
.kpi-sub{font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted);margin-top:.3rem}

/* Scorecards Grid */
.sc-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(310px,1fr));gap:1.2rem;margin-bottom:1.5rem}
.sc-card{background:var(--s1);border:1px solid var(--border);border-radius:18px;padding:1.3rem;position:relative;overflow:hidden;transition:all .3s}
.sc-card:hover{border-color:var(--border2);transform:translateY(-2px);box-shadow:0 16px 48px rgba(0,0,0,.4)}
.sc-card::before{content:'';position:absolute;top:0;left:0;right:0;height:3px;background:linear-gradient(90deg,var(--vc),transparent)}
.sc-card::after{content:'';position:absolute;top:-30px;right:-30px;width:100px;height:100px;border-radius:50%;background:radial-gradient(circle,var(--vc),transparent 70%);opacity:.06}
.sc-header{display:flex;align-items:center;gap:.8rem;margin-bottom:1rem}
.sc-avatar{width:42px;height:42px;border-radius:50%;display:grid;place-items:center;font-size:1rem;font-weight:800;flex-shrink:0;font-family:'DM Mono',monospace}
.sc-name{font-size:.9rem;font-weight:700;color:var(--text)}
.sc-id{font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted)}
.sc-kpis{display:grid;grid-template-columns:1fr 1fr 1fr;gap:.6rem;margin-bottom:.8rem}
.sc-kpi{text-align:center}
.sc-kpi-lbl{font-family:'DM Mono',monospace;font-size:.52rem;color:var(--muted);letter-spacing:.06em;text-transform:uppercase;margin-bottom:.2rem}
.sc-kpi-val{font-family:'DM Mono',monospace;font-size:.88rem;font-weight:500}
.sc-meta-row{margin-top:.3rem}
.sc-meta-lbl{display:flex;justify-content:space-between;font-family:'DM Mono',monospace;font-size:.6rem;margin-bottom:.25rem}
.sc-meta-bar{height:6px;background:var(--s3);border-radius:99px;overflow:hidden}
.sc-meta-fill{height:100%;border-radius:99px;transition:width 1.2s cubic-bezier(.4,0,.2,1)}
.sc-status{display:inline-flex;align-items:center;gap:.3rem;font-family:'DM Mono',monospace;font-size:.6rem;padding:.18rem .55rem;border-radius:99px;margin-top:.5rem}
.status-ok{background:var(--green-dim);color:var(--green);border:1px solid rgba(0,229,160,.25)}
.status-parcial{background:var(--yellow-dim);color:var(--yellow);border:1px solid rgba(255,184,0,.25)}
.status-bajo{background:var(--red-dim);color:var(--red);border:1px solid rgba(255,69,102,.25)}
.status-sm{background:var(--blue-dim);color:var(--blue);border:1px solid rgba(77,166,255,.2)}

/* Section divider */
.sd{display:flex;align-items:center;gap:1rem;margin:2rem 0 1.25rem}
.sd-label{font-family:'DM Mono',monospace;font-size:.65rem;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;white-space:nowrap}
.sd-line{flex:1;height:1px;background:var(--border)}

/* Cards */
.card{background:var(--s1);border:1px solid var(--border);border-radius:16px;padding:1.4rem}
.card-head{display:flex;align-items:center;justify-content:space-between;gap:1rem;margin-bottom:1.2rem}
.card-title{font-size:.82rem;font-weight:700;color:var(--text);letter-spacing:-.01em;display:flex;align-items:center;gap:.5rem}
.card-title svg{width:15px;height:15px;fill:var(--purple)}

/* Chart grid */
.chart-grid{display:grid;grid-template-columns:1fr 1fr;gap:1.25rem;margin-bottom:1.5rem}
@media(max-width:900px){.chart-grid{grid-template-columns:1fr}}

/* Ranking table */
.rank-table{width:100%;border-collapse:collapse}
.rank-table th{font-family:'DM Mono',monospace;font-size:.58rem;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;padding:.6rem .75rem;text-align:left;border-bottom:1px solid var(--border)}
.rank-table td{font-size:.8rem;padding:.65rem .75rem;border-bottom:1px solid rgba(255,255,255,.04);color:var(--text2)}
.rank-table tr:hover td{background:var(--s2)}
.num{text-align:right;font-family:'DM Mono',monospace;font-size:.78rem}
.prog-wrap{width:100%;background:var(--s3);border-radius:99px;height:5px;overflow:hidden;margin-top:3px}
.prog-fill{height:100%;border-radius:99px;transition:width 1.2s cubic-bezier(.4,0,.2,1)}
.empty{text-align:center;padding:2rem;color:var(--muted);font-family:'DM Mono',monospace;font-size:.78rem}
</style>
</head>
<body>
<div class="bg-orbs"><div class="orb orb-1"></div><div class="orb orb-2"></div><div class="orb orb-3"></div></div>

<header>
  <div class="header-inner">
    <a href="index.html" class="logo">
      <div class="logo-icon">
        <svg viewBox="0 0 24 24"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
      </div>
      <div><div class="logo-text">Suminregio Parker</div><div class="logo-sub">ERP Dashboard</div></div>
    </a>
    <nav>
      <a href="index.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>Inicio</a>
      <a href="ventas.html" class="nav-link"><svg viewBox="0 0 24 24"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>Ventas</a>
      <a href="cobradas.html" class="nav-link"><svg viewBox="0 0 24 24"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>Cobradas</a>
      <a href="vendedores.html" class="nav-link active"><svg viewBox="0 0 24 24"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>Vendedores</a>
      <a href="cxc.html" class="nav-link"><svg viewBox="0 0 24 24"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>CxC</a>
      <a href="clientes.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>Clientes</a>
      <a href="inventario.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 2 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/></svg>Inventario</a>
      <a href="resultados.html" class="nav-link"><svg viewBox="0 0 24 24"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>Resultados</a>
    </nav>
    <div class="header-right">
      <div class="live-pill"><div class="live-dot"></div>Live</div>
      <div class="clock" id="reloj">--:--:--</div>
    </div>
  </div>
</header>

<div class="page">
  <div class="hero">
    <div>
      <div class="hero-eyebrow">Cumplimiento &bull; Diario &bull; Mensual &bull; YTD</div>
      <div class="hero-title">Vendedores</div>
      <div class="hero-sub" id="heroSub">Conectando con el servidorâ€¦</div>
    </div>
  </div>

  <!-- Barra de filtros compartida -->
  <div id="filter-bar"></div>

  <!-- KPI Strip equipo -->
  <div class="kpi-strip">
    <div class="kpi purple"><div class="kpi-lbl">Vendedores activos</div><div class="kpi-val" id="kpiVendedores">--</div><div class="kpi-sub">en el equipo</div></div>
    <div class="kpi green"><div class="kpi-lbl">Mejor hoy</div><div class="kpi-val" id="kpiBestHoy">--</div><div class="kpi-sub" id="kpiBestHoySub">--</div></div>
    <div class="kpi blue"><div class="kpi-lbl">Total equipo hoy</div><div class="kpi-val" id="kpiTotalHoy">--</div><div class="kpi-sub" id="kpiMetaHoySub">Meta: --</div></div>
    <div class="kpi yellow"><div class="kpi-lbl">Total equipo mes</div><div class="kpi-val" id="kpiTotalMes">--</div><div class="kpi-sub" id="kpiMetaMesSub">Meta acum: --</div></div>
    <div class="kpi orange"><div class="kpi-lbl">Meta mes cumpl.</div><div class="kpi-val" id="kpiEnMetaHoy">--</div><div class="kpi-sub">vendedores</div></div>
    <div class="kpi green"><div class="kpi-lbl">Mejor mes YTD</div><div class="kpi-val" id="kpiBestMes">--</div><div class="kpi-sub" id="kpiBestMesSub">--</div></div>
  </div>

  <!-- Scorecards por vendedor -->
  <div class="sd"><div class="sd-label">Scorecards por Vendedor</div><div class="sd-line"></div></div>
  <div class="sc-grid" id="scorecardsGrid"></div>

  <!-- Charts y ranking -->
  <div class="sd"><div class="sd-label">An&aacute;lisis Comparativo</div><div class="sd-line"></div></div>
  <div class="chart-grid">
    <!-- Chart diario equipo -->
    <div class="card">
      <div class="card-head">
        <div class="card-title">
          <svg viewBox="0 0 24 24"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
          Tendencia Diaria del Equipo (30 d&iacute;as)
        </div>
      </div>
      <div style="height:200px"><canvas id="chartDiario"></canvas></div>
    </div>
    <!-- Chart radar cumplimiento -->
    <div class="card">
      <div class="card-head">
        <div class="card-title">
          <svg viewBox="0 0 24 24"><polygon points="12 2 22 8.5 22 15.5 12 22 2 15.5 2 8.5 12 2"/></svg>
          % Cumplimiento vs Meta Mensual
        </div>
      </div>
      <div style="height:200px"><canvas id="chartCumplimiento"></canvas></div>
    </div>
  </div>

  <!-- Cobros trend -->
  <div class="sd"><div class="sd-label">Tendencia de Cobros (6 meses)</div><div class="sd-line"></div></div>
  <div class="chart-grid" style="grid-template-columns:2fr 1fr;margin-bottom:1.5rem">
    <div class="card">
      <div class="card-head">
        <div class="card-title">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>
          Ventas vs Cobros Mensuales â€” Eficiencia de Cobro
        </div>
        <span id="eficBadge" style="font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted);background:var(--s2);border:1px solid var(--border);padding:.2rem .55rem;border-radius:99px">cargando...</span>
      </div>
      <div style="height:220px"><canvas id="chartCobros"></canvas></div>
    </div>
    <div class="card" style="display:flex;flex-direction:column;gap:.8rem">
      <div style="font-family:'DM Mono',monospace;font-size:.62rem;color:var(--muted);letter-spacing:.1em;text-transform:uppercase">KPIs de Cobro</div>
      <div>
        <div style="font-size:.56rem;font-family:'DM Mono',monospace;color:var(--muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.18rem">Eficiencia acumulada</div>
        <div id="kpiEfic" style="font-family:'DM Mono',monospace;font-size:1.6rem;font-weight:500;color:var(--green)">â€”</div>
      </div>
      <div>
        <div style="font-size:.56rem;font-family:'DM Mono',monospace;color:var(--muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.18rem">Total cobrado 6 meses</div>
        <div id="kpiTotalCobrado" style="font-family:'DM Mono',monospace;font-size:1.1rem;font-weight:500;color:var(--blue)">â€”</div>
      </div>
      <div>
        <div style="font-size:.56rem;font-family:'DM Mono',monospace;color:var(--muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:.18rem">Promedio mensual cobros</div>
        <div id="kpiPromCobros" style="font-family:'DM Mono',monospace;font-size:1.1rem;font-weight:500;color:var(--amber)">â€”</div>
      </div>
      <div id="cobrosInsight" style="background:var(--s2);border-radius:8px;padding:.65rem .85rem;font-family:'DM Mono',monospace;font-size:.63rem;color:var(--text2);line-height:1.65;flex:1;min-height:60px"></div>
    </div>
  </div>

  <!-- Tabla ranking completo -->
  <div class="sd"><div class="sd-label">Ranking Detallado</div><div class="sd-line"></div></div>
  <div class="card" style="margin-bottom:2rem">
    <div class="card-head">
      <div class="card-title">
        <svg viewBox="0 0 24 24"><line x1="8" y1="6" x2="21" y2="6"/><line x1="8" y1="12" x2="21" y2="12"/><line x1="8" y1="18" x2="21" y2="18"/><line x1="3" y1="6" x2="3.01" y2="6"/><line x1="3" y1="12" x2="3.01" y2="12"/><line x1="3" y1="18" x2="3.01" y2="18"/></svg>
        Tabla Completa de Cumplimiento
      </div>
    </div>
    <table class="rank-table">
      <thead>
        <tr>
          <th>#</th><th>Vendedor</th>
          <th class="num">Venta Hoy</th><th class="num">% Meta D&iacute;a</th>
          <th class="num">Venta Mes</th><th class="num">% Meta Mes</th>
          <th class="num">Venta YTD</th>
          <th class="num">Facts. Hoy</th><th class="num">Facts. Mes</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody id="rankingBody"></tbody>
    </table>
  </div>
</div>

<script>
// Mismo origen si se sirve desde el servidor (ej. http://localhost:7000); si abres por file:// usa localhost:7000
const API = (function(){ const o=typeof window!=='undefined'?window.location.origin:''; if(o.startsWith('http')&&(o.includes('7000')||o.includes('localhost'))) return ''; if(!o||o==='null'||o==='file://') return 'http://localhost:7000'; return ''; })();
function fwt(url,fallback,ms=10000){return Promise.race([fetch(url).then(r=>r.json()).catch(()=>fallback),new Promise(r=>setTimeout(()=>r(fallback),ms))]);}
let chartDiarioInst = null, chartCumplInst = null, chartCobrosInst = null;

// â”€â”€ Helper: URL con filtros â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
function buildApiUrl(path, extra) {
  const qs    = window.filterBuildQS ? filterBuildQS() : '';
  const e     = extra ? String(extra).replace(/^[?&]+/, '') : '';
  const parts = [qs, e].filter(Boolean).join('&');
  return API + path + (parts ? '?' + parts : '');
}

const fmtM = v=>{
  if(v===null||v===undefined||isNaN(+v))return'--';
  v=+v;
  if(v>=1e6)return'$'+(v/1e6).toFixed(2)+'M';
  if(v>=1e3)return'$'+(v/1e3).toFixed(1)+'K';
  return'$'+Math.round(v).toLocaleString();
};
const COLS=['#9B6DFF','#4DA6FF','#00E5A0','#FFB800','#FF8C42','#FF4566','#00D4FF'];

async function loadData(){
  try{
    const [cumplData, metas, diario, cotiVends, cxcTop] = await Promise.all([
      fwt(buildApiUrl('/api/ventas/cumplimiento'),[]),
      fwt(API+'/api/config/metas',{}),
      fwt(buildApiUrl('/api/ventas/diarias','dias=30'),[]),
      fwt(buildApiUrl('/api/ventas/por-vendedor/cotizaciones'),[]),
      fwt(API+'/api/cxc/top-deudores?limit=50',[]),
    ]);
    const vends = Array.isArray(cumplData) ? cumplData : [];

    // Merge cotizaciones por vendedor
    const cotiMap = {};
    (Array.isArray(cotiVends)?cotiVends:[]).forEach(c=>{ cotiMap[c.VENDEDOR_ID]=c; });

    // Merge CxC: mapear por nombre (no hay VENDEDOR_ID en top-deudores directamente)
    const vendsEnriched = vends.map(v => ({
      ...v,
      COTI_MES    : +(cotiMap[v.VENDEDOR_ID]?.COTIZACIONES_MES||0),
      NUM_COTI_MES: +(cotiMap[v.VENDEDOR_ID]?.NUM_COTI_MES||0),
      TICKET_PROM : v.FACTURAS_MES>0 ? Math.round(v.VENTA_MES/v.FACTURAS_MES) : 0,
    }));

    renderAll(vendsEnriched, metas);
    const dias = Array.isArray(diario) ? diario : (diario?.dias||[]);
    renderChartDiario(dias.length ? dias : null);
    renderChartCumpl(vendsEnriched);
    loadCobros();
  }catch(e){
    console.error('loadData error', e);
    const sub = document.getElementById('heroSub');
    if(sub) sub.textContent = 'Error de conexiÃ³n. Â¿EstÃ¡ corriendo el servidor? Abre http://localhost:7000';
  }
}

async function loadCobros(){
  try{
    const data = await fwt(API+'/api/resultados/pnl?meses=6', { meses: [], totales: {} });
    const meses = Array.isArray(data.meses) ? data.meses : [];
    const MESES_ES = ['','Ene','Feb','Mar','Abr','May','Jun','Jul','Ago','Sep','Oct','Nov','Dic'];
    if (!meses.length) {
      const today = new Date();
      const placeholders = [];
      for (let i = 5; i >= 0; i--) {
        const d = new Date(today.getFullYear(), today.getMonth() - i, 1);
        placeholders.push({ ANIO: d.getFullYear(), MES: d.getMonth() + 1, VENTAS_NETAS: 0, COBROS: 0 });
      }
      const labels = placeholders.map(m => (MESES_ES[m.MES]||'')+' '+(m.ANIO||''));
      const ctx = document.getElementById('chartCobros');
      if (ctx && chartCobrosInst) { chartCobrosInst.destroy(); chartCobrosInst = null; }
      if (!ctx) return;
      if (chartCobrosInst) chartCobrosInst.destroy();
      chartCobrosInst = new Chart(ctx, { data: { labels, datasets: [ { type: 'bar', label: 'Ventas Netas', data: placeholders.map(()=>0), backgroundColor: 'rgba(30,127,217,.28)', borderColor: '#1E7FD9', borderWidth: 1.5, borderRadius: 4 }, { type: 'bar', label: 'Cobros', data: placeholders.map(()=>0), backgroundColor: 'rgba(0,196,140,.32)', borderColor: '#00C48C', borderWidth: 1.5, borderRadius: 4 } ] }, options: { responsive: true, maintainAspectRatio: false, plugins: { legend: { labels: { color: '#4D6E8A', font: { size: 11 } } } }, scales: { x: { grid: { color: 'rgba(255,255,255,.04)' }, ticks: { color: '#4D6E8A', font: { size: 10 } } }, y: { grid: { color: 'rgba(255,255,255,.04)' }, ticks: { color: '#4D6E8A', font: { size: 10 }, callback: v=>fmtM(v) } } } });
      return;
    }
    const labels  = meses.map(m=>(MESES_ES[+m.MES]||m.MES)+' '+(+m.ANIO||''));
    const ventas  = meses.map(m=>+m.VENTAS_NETAS||0);
    const cobros  = meses.map(m=>+m.COBROS||0);
    const efic    = meses.map((m,i)=>ventas[i]>0?Math.round(cobros[i]/ventas[i]*100):0);
    const totVen  = ventas.reduce((s,v)=>s+v,0);
    const totCob  = cobros.reduce((s,v)=>s+v,0);
    const eficAcum= totVen>0?Math.round(totCob/totVen*100):0;
    // KPI cards
    const eficCol = eficAcum>=80?'var(--green)':eficAcum>=60?'var(--amber)':'var(--red)';
    document.getElementById('kpiEfic').textContent = eficAcum+'%';
    document.getElementById('kpiEfic').style.color = eficCol;
    document.getElementById('kpiTotalCobrado').textContent = fmtM(totCob);
    document.getElementById('kpiPromCobros').textContent = fmtM(totCob/Math.max(meses.length,1));
    document.getElementById('eficBadge').textContent = 'Efic. '+eficAcum+'%';
    // Insight
    const lastEfic = efic[efic.length-1]||0;
    const prevEfic = efic[efic.length-2]||0;
    const trend = lastEfic >= prevEfic ? '&#x2197; mejora' : '&#x2198; baja';
    const insight = eficAcum>=80?'<span style="color:var(--green)">&#10003; Excelente cobranza</span> â€” el equipo est&#225; cobrando m&#225;s del 80% de lo facturado. Mantener prioridad en cobro puntual.':
      eficAcum>=60?'<span style="color:var(--amber)">&#9888; Cobranza moderada</span> â€” se est&#225; cobrando el '+eficAcum+'% de lo facturado. Revisar los vencidos m&#225;s antiguos primero.':
      '<span style="color:var(--red)">&#10005; Cobranza baja</span> â€” solo el '+eficAcum+'% cobrado. Se recomienda acci&#243;n inmediata en la cartera vencida.';
    document.getElementById('cobrosInsight').innerHTML = insight+'<br><span style="color:var(--muted)">&#250;lt. mes: '+lastEfic+'% ('+trend+')</span>';
    // Chart
    const ctx = document.getElementById('chartCobros');
    if(!ctx) return;
    if(chartCobrosInst) chartCobrosInst.destroy();
    chartCobrosInst = new Chart(ctx, {
      data:{labels,datasets:[
        {type:'bar',label:'Ventas Netas',data:ventas,backgroundColor:'rgba(30,127,217,.28)',borderColor:'#1E7FD9',borderWidth:1.5,borderRadius:4},
        {type:'bar',label:'Cobros',data:cobros,backgroundColor:'rgba(0,196,140,.32)',borderColor:'#00C48C',borderWidth:1.5,borderRadius:4},
        {type:'line',label:'% Eficiencia',data:efic,borderColor:'#FFB800',backgroundColor:'rgba(255,184,0,.08)',borderWidth:2,pointRadius:4,pointBackgroundColor:'#FFB800',fill:true,tension:.35,yAxisID:'y2'}
      ]},
      options:{responsive:true,maintainAspectRatio:false,
        plugins:{legend:{labels:{color:'#4D6E8A',font:{size:11,family:"'DM Mono'"}}},
          tooltip:{backgroundColor:'#112233',borderColor:'rgba(255,255,255,.1)',borderWidth:1,titleColor:'#EDF4FF',bodyColor:'#93B4CC',
            callbacks:{label:c=>c.dataset.label+': '+(c.datasetIndex===2?c.raw+'%':fmtM(c.raw))}}},
        scales:{
          x:{grid:{color:'rgba(255,255,255,.04)'},ticks:{color:'#4D6E8A',font:{size:10,family:"'DM Mono'"}}},
          y:{grid:{color:'rgba(255,255,255,.04)'},ticks:{color:'#4D6E8A',font:{size:10,family:"'DM Mono'"},callback:v=>fmtM(v)}},
          y2:{position:'right',min:0,max:120,grid:{display:false},ticks:{color:'#FFB800',font:{size:10,family:"'DM Mono'"},callback:v=>v+'%'}}
        }}
    });
  }catch(e){console.warn('loadCobros error:',e);}
}

function renderAll(vends, metas){
  if(!vends.length){
    document.getElementById('heroSub').textContent='Sin datos de vendedores para el periodo seleccionado. Prueba otro mes o quita el filtro de vendedor.';
    ['kpiVendedores','kpiBestHoy','kpiTotalHoy','kpiTotalMes','kpiEnMetaHoy','kpiBestMes'].forEach(id=>{ const e=document.getElementById(id); if(e) e.textContent='--'; });
    document.getElementById('scorecardsGrid').innerHTML = '<div class="empty">No hay datos. Revisa filtros (aÃ±o/mes/vendedor) o que el servidor estÃ© en http://localhost:7000</div>';
    document.getElementById('rankingBody').innerHTML = '<tr><td colspan="10" class="empty">Sin registros</td></tr>';
    return;
  }
  const metaDia  = +metas.META_TOTAL_DIARIA||(+metas.META_DIARIA_POR_VENDEDOR||5650)*vends.length;
  const metaMes  = metaDia * (vends[0]?.DIAS_TRANSCURRIDOS||22);
  const totalHoy = vends.reduce((s,r)=>s+(+r.VENTA_HOY||0),0);
  const totalMes = vends.reduce((s,r)=>s+(+r.VENTA_MES||0),0);
  const enMetaHoy = vends.filter(r=>(+r.VENTA_HOY||0)>=(+r.META_DIA||0)).length;
  const enMetaMes = vends.filter(r=>r.STATUS_MES==='OK').length;
  const bestHoy  = [...vends].sort((a,b)=>(+b.VENTA_HOY||0)-(+a.VENTA_HOY||0))[0];
  const bestMes  = [...vends].sort((a,b)=>(+b.VENTA_MES||0)-(+a.VENTA_MES||0))[0];

  document.getElementById('heroSub').textContent = vends.length+' vendedores activos \u2022 Meta diaria equipo '+fmtM(metaDia)+' \u2022 '+Math.round(totalHoy/metaDia*100)+'% cumplimiento hoy';
  document.getElementById('kpiVendedores').textContent = vends.length;
  document.getElementById('kpiBestHoy').textContent = (bestHoy?.NOMBRE||'--').split(' ')[0];
  document.getElementById('kpiBestHoySub').textContent = fmtM(+bestHoy?.VENTA_HOY||0)+' hoy';
  document.getElementById('kpiTotalHoy').textContent = fmtM(totalHoy);
  document.getElementById('kpiMetaHoySub').textContent = 'Meta: '+fmtM(metaDia)+' ('+Math.round(totalHoy/metaDia*100)+'%)';
  document.getElementById('kpiTotalMes').textContent = fmtM(totalMes);
  document.getElementById('kpiMetaMesSub').textContent = 'Meta acum: '+fmtM(metaMes)+' ('+Math.round(totalMes/metaMes*100)+'%)';
  document.getElementById('kpiEnMetaHoy').textContent = enMetaMes+'/'+vends.length;
  document.getElementById('kpiBestMes').textContent = (bestMes?.NOMBRE||'--').split(' ')[0];
  document.getElementById('kpiBestMesSub').textContent = fmtM(+bestMes?.VENTA_MES||0)+' mes';

  renderScorecards(vends);
  renderRanking(vends);
}

function renderScorecards(vends){
  const sorted=[...vends].sort((a,b)=>(+b.VENTA_MES||0)-(+a.VENTA_MES||0));
  const html=sorted.map((v,i)=>{
    const col=COLS[i%COLS.length];
    const metaDia=+v.META_DIA||5650;
    const metaMes=+v.META_MES||(metaDia*(v.DIAS_TRANSCURRIDOS||22));
    const pHoy=Math.min(Math.round((+v.VENTA_HOY||0)/metaDia*100),200);
    const pMes=Math.min(Math.round((+v.VENTA_MES||0)/metaMes*100),200);
    const pYTD=Math.min(Math.round((+v.VENTA_YTD||0)/(metaMes*12)*100),200);
    const st=v.STATUS_MES||'BAJO';
    const stCls=st==='OK'?'status-ok':st==='PARCIAL'?'status-parcial':st==='SIN_META'?'status-sm':'status-bajo';
    const stTxt=st==='OK'?'&#10003; En meta':st==='PARCIAL'?'&#9432; Parcial':'&#9888; Bajo meta';
    const ini=((v.NOMBRE||'V').charAt(0)).toUpperCase();
    return `<div class="sc-card" style="--vc:${col}">
      <div class="sc-header">
        <div class="sc-avatar" style="background:linear-gradient(135deg,${col},${col}44);color:#050B14">${ini}</div>
        <div><div class="sc-name">${v.NOMBRE||'Vendedor '+(i+1)}</div><div class="sc-id">ID: ${v.VENDEDOR_ID||'--'}</div></div>
      </div>
      <div class="sc-kpis">
        <div class="sc-kpi">
          <div class="sc-kpi-lbl">Hoy</div>
          <div class="sc-kpi-val" style="color:${col}">${fmtM(+v.VENTA_HOY||0)}</div>
        </div>
        <div class="sc-kpi">
          <div class="sc-kpi-lbl">Mes</div>
          <div class="sc-kpi-val" style="color:${col}">${fmtM(+v.VENTA_MES||0)}</div>
        </div>
        <div class="sc-kpi">
          <div class="sc-kpi-lbl">YTD</div>
          <div class="sc-kpi-val" style="color:var(--text2)">${fmtM(+v.VENTA_YTD||0)}</div>
        </div>
      </div>
      <div class="sc-kpis" style="margin-top:.5rem;border-top:1px solid var(--border);padding-top:.5rem">
        <div class="sc-kpi">
          <div class="sc-kpi-lbl">Cotiz. mes</div>
          <div class="sc-kpi-val" style="color:var(--yellow);font-size:.78rem">${fmtM(+v.COTI_MES||0)}</div>
          <div style="font-family:'DM Mono',monospace;font-size:.52rem;color:var(--muted)">${v.NUM_COTI_MES||0} docs</div>
        </div>
        <div class="sc-kpi">
          <div class="sc-kpi-lbl">Ticket prom.</div>
          <div class="sc-kpi-val" style="color:var(--text2);font-size:.78rem">${fmtM(+v.TICKET_PROM||0)}</div>
          <div style="font-family:'DM Mono',monospace;font-size:.52rem;color:var(--muted)">${v.FACTURAS_MES||0} facts</div>
        </div>
        <div class="sc-kpi">
          <div class="sc-kpi-lbl">Conv. coti</div>
          <div class="sc-kpi-val" style="color:${(+v.COTI_MES||0)>0&&(+v.VENTA_MES||0)/(+v.COTI_MES)>=0.4?'var(--green)':(+v.COTI_MES||0)>0?'var(--yellow)':'var(--muted)'};font-size:.78rem">${(+v.COTI_MES||0)>0?Math.round((+v.VENTA_MES||0)/(+v.COTI_MES)*100)+'%':'â€”'}</div>
          <div style="font-family:'DM Mono',monospace;font-size:.52rem;color:var(--muted)">vta/coti</div>
        </div>
      </div>
      <div class="sc-meta-row" style="margin-top:.6rem">
        <div class="sc-meta-lbl"><span>Meta D&iacute;a</span><span style="color:${col}">${pHoy}%</span></div>
        <div class="sc-meta-bar"><div class="sc-meta-fill" style="width:${Math.min(pHoy,100)}%;background:${col}"></div></div>
      </div>
      <div class="sc-meta-row" style="margin-top:.4rem">
        <div class="sc-meta-lbl"><span>Meta Mes</span><span style="color:${col}">${pMes}%</span></div>
        <div class="sc-meta-bar"><div class="sc-meta-fill" style="width:${Math.min(pMes,100)}%;background:${col}"></div></div>
      </div>
      <div style="display:flex;align-items:center;justify-content:space-between;margin-top:.6rem">
        <span class="sc-status ${stCls}">${stTxt}</span>
        <span style="font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted)">${pYTD}% YTD</span>
      </div>
    </div>`;
  }).join('');
  document.getElementById('scorecardsGrid').innerHTML = html || '<div class="empty">Sin datos</div>';
}

function renderRanking(vends){
  const sorted=[...vends].sort((a,b)=>(+b.VENTA_MES||0)-(+a.VENTA_MES||0));
  const rows=sorted.map((v,i)=>{
    const col=COLS[i%COLS.length];
    const metaDia=+v.META_DIA||5650;
    const metaMes=+v.META_MES||(metaDia*(v.DIAS_TRANSCURRIDOS||22));
    const pHoy=Math.round((+v.VENTA_HOY||0)/metaDia*100);
    const pMes=Math.round((+v.VENTA_MES||0)/metaMes*100);
    const st=v.STATUS_MES||'BAJO';
    const stCls=st==='OK'?'status-ok':st==='PARCIAL'?'status-parcial':st==='SIN_META'?'status-sm':'status-bajo';
    const stTxt=st==='OK'?'En meta':st==='PARCIAL'?'Parcial':st==='SIN_META'?'Sin meta':'Bajo';
    const rankBg=i===0?'#FFB800':i===1?'#B0BECE':i===2?'#CD7F32':'var(--s3)';
    const rankFg=i<3?'#050B14':'var(--muted)';
    return `<tr>
      <td style="padding:.65rem .75rem"><span style="display:inline-flex;align-items:center;justify-content:center;width:22px;height:22px;border-radius:50%;background:${rankBg};color:${rankFg};font-size:.65rem;font-weight:700;font-family:'DM Mono',monospace">${i+1}</span></td>
      <td style="padding:.65rem .75rem;color:var(--text);font-weight:600">${v.NOMBRE||'Vendedor '+(i+1)}</td>
      <td class="num" style="color:${col}">${fmtM(+v.VENTA_HOY||0)}</td>
      <td class="num">
        <span style="color:${pHoy>=100?'var(--green)':pHoy>=70?'var(--yellow)':'var(--red)'}">${pHoy}%</span>
        <div class="prog-wrap"><div class="prog-fill" style="width:${Math.min(pHoy,100)}%;background:${col}"></div></div>
      </td>
      <td class="num" style="color:${col}">${fmtM(+v.VENTA_MES||0)}</td>
      <td class="num">
        <span style="color:${pMes>=100?'var(--green)':pMes>=70?'var(--yellow)':'var(--red)'}">${pMes}%</span>
        <div class="prog-wrap"><div class="prog-fill" style="width:${Math.min(pMes,100)}%;background:${col}"></div></div>
      </td>
      <td class="num" style="color:var(--text2)">${fmtM(+v.VENTA_YTD||0)}</td>
      <td class="num" style="color:var(--text2)">${+v.FACTURAS_HOY||0}</td>
      <td class="num" style="color:var(--text2)">${+v.FACTURAS_MES||0}</td>
      <td style="padding:.65rem .75rem"><span class="sc-status ${stCls}" style="font-size:.58rem">${stTxt}</span></td>
    </tr>`;
  }).join('');
  document.getElementById('rankingBody').innerHTML=rows||'<tr><td colspan="10" class="empty">Sin datos</td></tr>';
}

function renderChartDiario(dias){
  const MESES = ['Ene','Feb','Mar','Abr','May','Jun','Jul','Ago','Sep','Oct','Nov','Dic'];
  let labels, vals;
  if (dias && dias.length) {
    labels = dias.map(d=>{
      const raw = String(d.FECHA||d.DIA||'').substring(0,10);
      const p = raw.split('-');
      if (p.length===3) return p[2]+'/'+p[1];
      const dt = new Date(raw);
      if (!isNaN(dt.getTime())) return dt.getDate()+'/'+MESES[dt.getMonth()];
      return raw.slice(5);
    });
    vals = dias.map(d=>+(d.TOTAL||d.TOTAL_VENTAS||0));
  } else {
    const today = new Date();
    labels = []; vals = [];
    for (let i = 29; i >= 0; i--) {
      const d = new Date(today); d.setDate(d.getDate() - i);
      labels.push(d.getDate()+'/'+MESES[d.getMonth()]);
      vals.push(0);
    }
  }
  const ctx = document.getElementById('chartDiario');
  if (!ctx) return;
  if (chartDiarioInst) chartDiarioInst.destroy();
  chartDiarioInst = new Chart(ctx, {
    type: 'line',
    data: { labels, datasets: [{ label: 'Venta', data: vals, borderColor: '#F57C00', backgroundColor: 'rgba(245,124,0,.08)', borderWidth: 2, pointRadius: 2, fill: true, tension: .35 }] },
    options: { responsive: true, maintainAspectRatio: false, plugins: { legend: { display: false }, tooltip: { backgroundColor: '#0D1D35', borderColor: 'rgba(255,255,255,.1)', borderWidth: 1, titleColor: '#E8EFF8', bodyColor: '#B0BECE', callbacks: { label: c=>' '+fmtM(c.raw) } } }, scales: { x: { grid: { color: 'rgba(255,255,255,.04)' }, ticks: { color: '#5A7090', font: { size: 10, family: "'DM Mono'" } } }, y: { min: 0, grid: { color: 'rgba(255,255,255,.04)' }, ticks: { color: '#5A7090', font: { size: 10, family: "'DM Mono'" }, callback: v=>fmtM(v) } } } }
  });
}

function renderChartCumpl(vends){
  if(!vends.length)return;
  const sorted=[...vends].sort((a,b)=>(+b.VENTA_MES||0)-(+a.VENTA_MES||0));
  const labels=sorted.map(v=>(v.NOMBRE||'V').split(' ')[0]);
  const pcts=sorted.map(v=>{
    const m=+v.META_MES||(+v.META_DIA||5650)*(v.DIAS_TRANSCURRIDOS||22);
    return Math.round((+v.VENTA_MES||0)/m*100);
  });
  const bgs=sorted.map((v,i)=>{
    const p=pcts[i];
    return p>=100?'rgba(0,229,160,.5)':p>=70?'rgba(255,184,0,.5)':'rgba(255,69,102,.5)';
  });
  const ctx=document.getElementById('chartCumplimiento');
  if(chartCumplInst)chartCumplInst.destroy();
  chartCumplInst=new Chart(ctx,{
    type:'bar',
    data:{labels,datasets:[
      {label:'% Cumplimiento',data:pcts,backgroundColor:bgs,borderColor:bgs.map(c=>c.replace('.5)','.9)')),borderWidth:1,borderRadius:4},
      {type:'line',label:'Meta 100%',data:pcts.map(()=>100),borderColor:'rgba(77,166,255,.5)',borderDash:[4,4],borderWidth:1.5,pointRadius:0,fill:false}
    ]},
    options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{labels:{color:'#5A7090',font:{size:11,family:"'DM Mono'"}}},tooltip:{backgroundColor:'#0D1D35',borderColor:'rgba(255,255,255,.1)',borderWidth:1,callbacks:{label:c=>c.dataset.label+': '+c.raw+(c.datasetIndex===0?'%':'%')}}},scales:{x:{grid:{color:'rgba(255,255,255,.04)'},ticks:{color:'#5A7090',font:{size:10,family:"'DM Mono'"}}},y:{grid:{color:'rgba(255,255,255,.04)'},ticks:{color:'#5A7090',font:{size:10,family:"'DM Mono'"},callback:v=>v+'%'},suggestedMax:120}}}
  });
}

// Carga inicial inmediata
loadData();
initFilters({ containerId:'filter-bar', showVendedor:true, onChange:()=>loadData() });
setInterval(loadData, 60000);
</script>
<script src="nav.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Suminregio Parker &mdash; Inventario</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Mono:ital,wght@0,300;0,400;0,500;1,300&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --bg:#060E1A;--s1:#0C1B29;--s2:#112233;--s3:#172D42;--s4:#1D3654;
  --border:rgba(255,255,255,.06);--border2:rgba(255,255,255,.11);
  --green:#00E5A0;--green-dim:rgba(0,229,160,.1);
  --yellow:#FFB800;--yellow-dim:rgba(255,184,0,.12);
  --orange:#FF8C42;--orange-dim:rgba(255,140,66,.1);
  --red:#FF4566;--red-dim:rgba(255,69,102,.12);
  --blue:#4DA6FF;--blue-dim:rgba(77,166,255,.1);
  --purple:#9B6DFF;--purple-dim:rgba(155,109,255,.1);
  --text:#E8EFF8;--text2:#B0BECE;--muted:#5A7090;--dim:#243348;
}
html{font-size:16px;-webkit-font-smoothing:antialiased}
body{background:var(--bg);color:var(--text);font-family:'Syne',sans-serif;min-height:100vh}
.bg-orbs{position:fixed;inset:0;pointer-events:none;z-index:0;overflow:hidden}
.orb{position:absolute;border-radius:50%;filter:blur(130px);opacity:.12;animation:drift 20s ease-in-out infinite alternate}
.orb-1{width:600px;height:600px;background:radial-gradient(circle,#1E7FD9,transparent 70%);bottom:-100px;right:-100px}
.orb-2{width:500px;height:500px;background:radial-gradient(circle,#FFB800,transparent 70%);top:-100px;left:-100px;animation-delay:-5s}
@keyframes drift{0%{transform:translate(0,0)}100%{transform:translate(15px,15px)}}
header{position:sticky;top:0;z-index:100;background:rgba(5,11,20,.85);backdrop-filter:blur(20px);border-bottom:1px solid var(--border)}
.header-inner{max-width:1600px;margin:0 auto;height:64px;display:flex;align-items:center;justify-content:space-between;gap:1.5rem;padding:0 2rem}
.logo{display:flex;align-items:center;gap:.75rem;text-decoration:none}
.logo-icon{width:36px;height:36px;background:linear-gradient(135deg,var(--orange),var(--yellow));border-radius:10px;display:grid;place-items:center;flex-shrink:0}
.logo-icon svg{width:20px;height:20px;fill:white}
.logo-text{font-size:.9rem;font-weight:700;color:var(--text)}
.logo-sub{font-size:.62rem;font-family:'DM Mono',monospace;color:var(--muted);letter-spacing:.08em;text-transform:uppercase}
nav{display:flex;align-items:center;gap:.25rem}
.nav-link{display:flex;align-items:center;gap:.4rem;padding:.4rem .85rem;border-radius:8px;font-size:.8rem;font-weight:500;color:var(--muted);text-decoration:none;transition:all .2s}
.nav-link:hover{color:var(--text);background:var(--s2)}
.nav-link.active{color:var(--text);background:var(--s3);border:1px solid var(--border2)}
.nav-link svg{width:14px;height:14px;fill:currentColor}
.header-right{display:flex;align-items:center;gap:1rem}
.live-pill{display:flex;align-items:center;gap:.5rem;background:var(--orange-dim);border:1px solid rgba(255,140,66,.2);border-radius:99px;padding:.3rem .75rem;font-family:'DM Mono',monospace;font-size:.68rem;color:var(--orange);letter-spacing:.06em}
.live-dot{width:6px;height:6px;border-radius:50%;background:var(--orange);box-shadow:0 0 8px var(--orange);animation:pulse 2s ease-in-out infinite}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.4}}
.clock{font-family:'DM Mono',monospace;font-size:.78rem;color:var(--muted)}
.page{position:relative;z-index:1;max-width:1600px;margin:0 auto;padding:2rem}
.page-header{margin-bottom:2rem;display:flex;align-items:flex-start;justify-content:space-between;gap:1rem;flex-wrap:wrap}
.page-title{font-size:1.75rem;font-weight:800;background:linear-gradient(135deg,var(--text),var(--orange));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.page-sub{font-size:.85rem;color:var(--muted);margin-top:.35rem}
.kpi-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:1rem;margin-bottom:1.5rem}
@media(max-width:900px){.kpi-grid{grid-template-columns:repeat(2,1fr)}}
.kpi-card{background:var(--s1);border:1px solid var(--border);border-radius:16px;padding:1.25rem 1.5rem;position:relative;overflow:hidden;transition:all .3s}
.kpi-card::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,var(--kpi-c),transparent);opacity:.8}
.kpi-card:hover{border-color:var(--border2);transform:translateY(-2px);box-shadow:0 12px 40px rgba(0,0,0,.3)}
.kpi-card.blue{--kpi-c:var(--blue)}.kpi-card.green{--kpi-c:var(--green)}.kpi-card.yellow{--kpi-c:var(--yellow)}.kpi-card.orange{--kpi-c:var(--orange)}.kpi-card.red{--kpi-c:var(--red)}
.kpi-label{font-family:'DM Mono',monospace;font-size:.62rem;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-bottom:.4rem}
.kpi-value{font-family:'DM Mono',monospace;font-size:1.4rem;font-weight:500;letter-spacing:-.02em;line-height:1}
.kpi-card.blue .kpi-value{color:var(--blue)}.kpi-card.green .kpi-value{color:var(--green)}.kpi-card.yellow .kpi-value{color:var(--yellow)}.kpi-card.orange .kpi-value{color:var(--orange)}.kpi-card.red .kpi-value{color:var(--red)}
.kpi-sub{margin-top:.4rem;font-family:'DM Mono',monospace;font-size:.65rem;color:var(--muted)}
.tag{display:inline-block;padding:.1rem .4rem;border-radius:4px;font-size:.6rem;font-weight:500}
.tag.up{background:var(--green-dim);color:var(--green)}.tag.down{background:var(--red-dim);color:var(--red)}.tag.warn{background:var(--yellow-dim);color:var(--yellow)}
.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:1.25rem;margin-bottom:1.25rem}
@media(max-width:1000px){.grid-2{grid-template-columns:1fr}}
.card{background:var(--s1);border:1px solid var(--border);border-radius:16px;overflow:hidden}
.card-head{padding:1.25rem 1.5rem;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:.5rem}
.card-title{font-size:.88rem;font-weight:600;color:var(--text);display:flex;align-items:center;gap:.6rem}
.card-title svg{width:16px;height:16px;fill:var(--muted)}
.card-badge{font-family:'DM Mono',monospace;font-size:.63rem;padding:.2rem .6rem;border-radius:99px;border:1px solid var(--border2);color:var(--muted)}
.card-body{padding:1.5rem}
.search-wrap{display:flex;gap:.75rem;margin-bottom:1rem}
.search-input{flex:1;background:var(--s2);border:1px solid var(--border2);color:var(--text);padding:.5rem 1rem;border-radius:10px;font-size:.82rem;font-family:'Syne',sans-serif;outline:none}
.search-input:focus{border-color:var(--blue);box-shadow:0 0 0 2px rgba(77,166,255,.15)}
.search-input::placeholder{color:var(--muted)}
.tbl{width:100%;border-collapse:collapse;font-size:.78rem}
.tbl th{font-family:'DM Mono',monospace;font-size:.6rem;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;padding:.6rem 1rem;text-align:left;border-bottom:1px solid var(--border)}
.tbl td{padding:.65rem 1rem;border-bottom:1px solid var(--border);color:var(--text2)}
.tbl tr:last-child td{border:none}.tbl tr:hover td{background:var(--s2);color:var(--text)}
.tbl .num{text-align:right;font-family:'DM Mono',monospace;font-size:.75rem;color:var(--text)}
.tbl .lbl{font-weight:600;color:var(--text)}
.stock-bar-wrap{display:flex;align-items:center;gap:.5rem}
.stock-bar-bg{flex:1;height:6px;background:var(--s3);border-radius:99px;overflow:hidden}
.stock-bar-fill{height:100%;border-radius:99px}
.semanas-tag{display:inline-block;padding:.1rem .4rem;border-radius:4px;font-family:'DM Mono',monospace;font-size:.62rem}
</style>
</head>
<body>
<div class="bg-orbs"><div class="orb orb-1"></div><div class="orb orb-2"></div></div>

<header>
  <div class="header-inner">
    <a href="index.html" class="logo">
      <div class="logo-icon"><svg viewBox="0 0 24 24"><path d="M20 7H4c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V9c0-1.1-.9-2-2-2zm-8 9c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3zM4 3h16v2H4z"/></svg></div>
      <div><div class="logo-text">Suminregio Parker</div><div class="logo-sub">ERP Dashboard</div></div>
    </a>
    <nav>
      <a href="index.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"/></svg>Inicio</a>
      <a href="ventas.html" class="nav-link"><svg viewBox="0 0 24 24"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>Ventas</a>
      <a href="cobradas.html" class="nav-link"><svg viewBox="0 0 24 24"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>Cobradas</a>
      <a href="vendedores.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>Vendedores</a>
      <a href="cxc.html" class="nav-link"><svg viewBox="0 0 24 24"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>CxC</a>
      <a href="clientes.html" class="nav-link"><svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>Clientes</a>
      <a href="inventario.html" class="nav-link active"><svg viewBox="0 0 24 24"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 2 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/></svg>Inventario</a>
      <a href="resultados.html" class="nav-link"><svg viewBox="0 0 24 24"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>Resultados</a>
    </nav>
    <div class="header-right">
      <div class="live-pill"><div class="live-dot"></div>STOCK</div>
      <div class="clock" id="clock">--:--:--</div>
    </div>
  </div>
</header>

<div class="page">
  <div class="page-header">
    <div>
      <div class="page-title">Inventario</div>
      <div class="page-sub" id="page-sub">Cargando datos...</div>
    </div>
  </div>

  <div class="kpi-grid">
    <div class="kpi-card blue"><div class="kpi-label">Total Art&iacute;culos</div><div class="kpi-value" id="kpi-total">&mdash;</div><div class="kpi-sub">Activos en cat&aacute;logo</div></div>
    <div class="kpi-card yellow"><div class="kpi-label">Valor Inventario</div><div class="kpi-value" id="kpi-valor">&mdash;</div><div class="kpi-sub">Existencia × Precio</div></div>
    <div class="kpi-card red"><div class="kpi-label">Bajo M&iacute;nimo</div><div class="kpi-value" id="kpi-minimo">&mdash;</div><div class="kpi-sub">Requieren reorden</div></div>
    <div class="kpi-card orange"><div class="kpi-label">Sin Stock</div><div class="kpi-value" id="kpi-cero">&mdash;</div><div class="kpi-sub">Con m&iacute;nimo definido y existencia 0</div></div>
  </div>

  <!-- B&Uacute;SQUEDA -->
  <div class="card" style="margin-bottom:1.25rem">
    <div class="card-head"><div class="card-title"><svg viewBox="0 0 24 24"><path d="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"/></svg>Buscar Art&iacute;culo</div><div class="card-badge" id="busq-badge">Escribe m&iacute;nimo 2 caracteres</div></div>
    <div class="card-body">
      <div class="search-wrap">
        <input class="search-input" id="busq-input" type="text" placeholder="Nombre o c&oacute;digo de art&iacute;culo..." oninput="doBusqueda(this.value)">
      </div>
      <div id="busq-resultado"></div>
    </div>
  </div>

  <div class="grid-2">
    <div class="card">
      <div class="card-head"><div class="card-title"><svg viewBox="0 0 24 24"><path d="M20 8.69V4h-4.69L12 .69 8.69 4H4v4.69L.69 12 4 15.31V20h4.69L12 23.31 15.31 20H20v-4.69L23.31 12 20 8.69zM12 18c-.89 0-1.74-.2-2.5-.55C11.56 16.5 13 14.42 13 12s-1.44-4.5-3.5-5.45C10.26 6.2 11.11 6 12 6c3.31 0 6 2.69 6 6s-2.69 6-6 6zM6 12c0-1.93.78-3.68 2.04-4.96C8.01 8.32 8 9.15 8 10c0 2.42 1.72 4.44 4 4.9V18c-3.31 0-6-2.69-6-6z"/></svg>Art&iacute;culos Bajo M&iacute;nimo</div><div class="card-badge" id="minimo-badge">&mdash;</div></div>
      <div class="card-body" style="padding:0">
        <table class="tbl" id="tbl-minimo">
          <thead><tr><th>Art&iacute;culo</th><th>Unidad</th><th class="num">Exist.</th><th class="num">M&iacute;nimo</th><th class="num">Faltante</th></tr></thead>
          <tbody><tr><td colspan="5" style="text-align:center;color:var(--muted);padding:1.5rem">Cargando...</td></tr></tbody>
        </table>
      </div>
    </div>

    <div class="card">
      <div class="card-head"><div class="card-title"><svg viewBox="0 0 24 24"><path d="M11.99 2C6.47 2 2 6.48 2 12s4.47 10 9.99 10C17.52 22 22 17.52 22 12S17.52 2 11.99 2zM12 20c-4.42 0-8-3.58-8-8s3.58-8 8-8 8 3.58 8 8-3.58 8-8 8zm.5-13H11v6l5.25 3.15.75-1.23-4.5-2.67V7z"/></svg>Consumo Semanal (stock cr&iacute;tico)</div><div class="card-badge">&Uacute;ltimas 4 semanas</div></div>
      <div class="card-body" style="padding:0">
        <table class="tbl" id="tbl-consumo">
          <thead><tr><th>Art&iacute;culo</th><th class="num">Exist.</th><th class="num">Cons/Sem</th><th class="num">Semanas</th></tr></thead>
          <tbody><tr><td colspan="4" style="text-align:center;color:var(--muted);padding:1.5rem">Cargando...</td></tr></tbody>
        </table>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="card-head"><div class="card-title"><svg viewBox="0 0 24 24"><path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm0 4l6 2.67V11c0 3.94-2.71 7.62-6 8.93-3.29-1.31-6-4.99-6-8.93V7.67L12 5z"/></svg>Top Art&iacute;culos por Valor de Inventario</div><div class="card-badge">Mayor valor en stock</div></div>
    <div class="card-body" style="padding:0">
      <table class="tbl" id="tbl-topstock">
        <thead><tr><th>#</th><th>Art&iacute;culo</th><th>Unidad</th><th class="num">Existencia</th><th class="num">Precio</th><th class="num">Valor Total</th></tr></thead>
        <tbody><tr><td colspan="6" style="text-align:center;color:var(--muted);padding:1.5rem">Cargando...</td></tr></tbody>
      </table>
    </div>
  </div>

  <!-- FORECAST: Consumo + Stock M&iacute;nimo Recomendado + D&iacute;as hasta agotamiento -->
  <div class="card" style="margin-top:1.25rem">
    <div class="card-head">
      <div class="card-title">
        <svg viewBox="0 0 24 24"><path d="M2.5 19h19v2h-19zm19.57-9.36c-.21-.8-1.04-1.28-1.84-1.06L14.92 10l-6.9-6.43-1.93.51 4.14 7.17-4.97 1.33-1.97-1.54-1.45.39 2.59 4.49s7.12-1.9 14.35-3.84c.81-.23 1.28-1.05 1.07-1.86z"/></svg>
        Forecast: Consumo &amp; Reposici&oacute;n (&uacute;ltimos 90 d&iacute;as)
      </div>
      <div class="card-badge" id="consumo-badge">--</div>
    </div>
    <div class="card-body" style="padding:0">
      <table class="tbl" id="tbl-forecast">
        <thead><tr>
          <th>Art&iacute;culo</th><th>Unidad</th>
          <th class="num">Existencia</th><th class="num">Cons/D&iacute;a</th>
          <th class="num">D&iacute;as Stock</th><th class="num">Stock M&iacute;n Rec.</th>
          <th class="num">Reponer</th><th>Alerta</th>
        </tr></thead>
        <tbody id="tbl-forecast-body"><tr><td colspan="8" style="text-align:center;color:var(--muted);padding:1.5rem">Cargando...</td></tr></tbody>
      </table>
    </div>
  </div>

  <!-- Art&iacute;culos sin movimiento -->
  <div class="card" style="margin-top:1.25rem;margin-bottom:1.5rem">
    <div class="card-head">
      <div class="card-title">
        <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="4.93" y1="4.93" x2="19.07" y2="19.07"/></svg>
        Art&iacute;culos Sin Movimiento (&gt;180 d&iacute;as con existencia)
      </div>
      <div class="card-badge" id="sinmov-badge">--</div>
    </div>
    <div class="card-body" style="padding:0">
      <table class="tbl" id="tbl-sinmov">
        <thead><tr>
          <th>Art&iacute;culo</th><th>Unidad</th>
          <th class="num">Existencia</th><th class="num">M&iacute;nimo</th>
          <th class="num">D&iacute;as Sin Venta</th><th class="num">&Uacute;lt. Movimiento</th>
        </tr></thead>
        <tbody id="tbl-sinmov-body"><tr><td colspan="6" style="text-align:center;color:var(--muted);padding:1.5rem">Cargando...</td></tr></tbody>
      </table>
    </div>
  </div>
</div>

<script>
const API = '';
const fmt  = n => isNaN(+n)||n==null?'&mdash;':'$'+Number(n).toLocaleString('es-MX',{minimumFractionDigits:0,maximumFractionDigits:0});
const fmtK = n => { if(isNaN(+n)||n==null) return '&mdash;'; n=+n; if(Math.abs(n)>=1e6) return '$'+(n/1e6).toFixed(1)+'M'; if(Math.abs(n)>=1e3) return '$'+(n/1e3).toFixed(0)+'k'; return '$'+n.toFixed(0); };
const setText = (id,v)=>{ const e=document.getElementById(id); if(e) e.textContent=v; };

setInterval(()=>{ document.getElementById('clock').textContent=new Date().toLocaleTimeString('es-MX'); },1000);

let busqTimer;
function doBusqueda(val) {
  clearTimeout(busqTimer);
  if(val.trim().length < 2) { document.getElementById('busq-resultado').innerHTML=''; setText('busq-badge','Escribe m&iacute;nimo 2 caracteres'); return; }
  busqTimer = setTimeout(async ()=>{
    try {
      const rows = await fetch(`${API}/api/inv/existencias?q=${encodeURIComponent(val)}`).then(r=>r.json());
      setText('busq-badge', `${rows.length} resultados`);
      document.getElementById('busq-resultado').innerHTML = rows.length ?
        `<table class="tbl"><thead><tr><th>Art&iacute;culo</th><th>Unidad</th><th class="num">Existencia</th><th class="num">M&iacute;nimo</th><th class="num">Precio</th></tr></thead><tbody>${
          rows.map(r=>{
            const pct = r.EXISTENCIA_MINIMA>0?Math.min((r.EXISTENCIA/r.EXISTENCIA_MINIMA)*100,100):100;
            const barColor = r.EXISTENCIA<=0?'var(--red)':r.EXISTENCIA<r.EXISTENCIA_MINIMA?'var(--orange)':'var(--green)';
            return `<tr><td class="lbl">${r.DESCRIPCION}</td><td style="color:var(--muted)">${r.UNIDAD}</td>
              <td class="num"><div class="stock-bar-wrap"><div class="stock-bar-bg"><div class="stock-bar-fill" style="width:${pct}%;background:${barColor}"></div></div><span>${r.EXISTENCIA}</span></div></td>
              <td class="num" style="color:var(--muted)">${r.EXISTENCIA_MINIMA}</td>
              <td class="num">${fmt(r.PRECIO_VENTA)}</td></tr>`;
          }).join('')
        }</tbody></table>` :
        '<div style="text-align:center;color:var(--muted);padding:1rem">Sin resultados</div>';
    } catch(e){}
  }, 350);
}

async function loadAll() {
  try {
    const d = await fetch(`${API}/api/inv/resumen`).then(r=>r.json());
    setText('kpi-total', Number(d.TOTAL_ARTICULOS||0).toLocaleString());
    setText('kpi-valor', fmtK(d.VALOR_INVENTARIO));
    setText('kpi-minimo', Number(d.BAJO_MINIMO||0).toLocaleString());
    setText('kpi-cero', Number(d.SIN_STOCK||0).toLocaleString());
  } catch(e){}

  await Promise.all([loadMinimo(), loadConsumo(), loadTopStock(), loadForecast(), loadSinMovimiento()]);
  setText('page-sub', `Actualizado: ${new Date().toLocaleTimeString('es-MX')}`);
}

async function loadMinimo() {
  try {
    const rows = await fetch(`${API}/api/inv/bajo-minimo?limit=50`).then(r=>r.json());
    document.querySelector('#tbl-minimo tbody').innerHTML = rows.map(r=>{
      const pct = r.EXISTENCIA_MINIMA>0?(r.EXISTENCIA/r.EXISTENCIA_MINIMA*100):0;
      const barColor = r.EXISTENCIA<=0?'var(--red)':'var(--orange)';
      return `<tr>
        <td class="lbl">${r.DESCRIPCION}</td>
        <td style="color:var(--muted)">${r.UNIDAD}</td>
        <td class="num"><div class="stock-bar-wrap"><div class="stock-bar-bg"><div class="stock-bar-fill" style="width:${Math.min(pct,100)}%;background:${barColor}"></div></div><span style="color:${barColor}">${r.EXISTENCIA}</span></div></td>
        <td class="num">${r.EXISTENCIA_MINIMA}</td>
        <td class="num"><span style="color:var(--red)">-${r.FALTANTE}</span></td>
      </tr>`;
    }).join('') || '<tr><td colspan="5" style="text-align:center;color:var(--muted)">Sin art&iacute;culos bajo m&iacute;nimo</td></tr>';
    setText('minimo-badge', `${rows.length} art&iacute;culos`);
  } catch(e){}
}

async function loadConsumo() {
  try {
    const rows = await fetch(`${API}/api/inv/consumo-semanal?limit=30`).then(r=>r.json());
    document.querySelector('#tbl-consumo tbody').innerHTML = rows.map(r=>{
      const sem = +(r.SEMANAS_STOCK||0);
      const semColor = sem<1?'var(--red)':sem<2?'var(--orange)':sem<4?'var(--yellow)':'var(--green)';
      const semTag = sem<1?'down':sem<2?'warn':'up';
      return `<tr>
        <td class="lbl">${r.DESCRIPCION}</td>
        <td class="num">${+(r.EXISTENCIA||0).toFixed(0)}</td>
        <td class="num">${+(r.CONSUMO_SEMANAL_PROM||0).toFixed(1)}</td>
        <td class="num"><span class="semanas-tag tag ${semTag}" style="color:${semColor}">${sem>9999?'8':sem.toFixed(1)} sem</span></td>
      </tr>`;
    }).join('') || '<tr><td colspan="4" style="text-align:center;color:var(--muted)">Sin datos de consumo</td></tr>';
  } catch(e){ console.error('consumo',e); }
}

async function loadTopStock() {
  try {
    const rows = await fetch(`${API}/api/inv/top-stock?limit=30`).then(r=>r.json());
    document.querySelector('#tbl-topstock tbody').innerHTML = rows.map((r,i)=>`<tr>
      <td style="font-family:'DM Mono',monospace;font-size:.7rem;color:var(--muted)">${i+1}</td>
      <td class="lbl">${r.DESCRIPCION}</td>
      <td style="color:var(--muted)">${r.UNIDAD}</td>
      <td class="num">${+(r.EXISTENCIA||0).toLocaleString()}</td>
      <td class="num">${fmt(r.PRECIO_VENTA)}</td>
      <td class="num"><span style="color:var(--yellow)">${fmt(r.VALOR_TOTAL)}</span></td>
    </tr>`).join('') || '<tr><td colspan="6" style="text-align:center;color:var(--muted)">Sin datos</td></tr>';
  } catch(e){}
}

async function loadForecast(){
  try{
    const rows = await fetch(`${API}/api/inv/consumo?dias=90&limit=50&lead=15`).then(r=>r.json());
    setText('consumo-badge', rows.length+' art&iacute;culos');
    document.getElementById('tbl-forecast-body').innerHTML = rows.map(r=>{
      const dias = +(r.DIAS_STOCK||0);
      const alerta = (r.ALERTA||'OK').toUpperCase();
      const alertaCol = alerta==='CRITICO'?'var(--red)':alerta==='BAJO'?'var(--orange)':alerta==='BAJO_MINIMO'?'var(--yellow)':'var(--green)';
      const alertaTxt = alerta==='CRITICO'?'&#9888; CR&Iacute;TICO':alerta==='BAJO'?'&#9650; BAJO':alerta==='BAJO_MINIMO'?'&#9432; MIN':'&#10003; OK';
      const reponer = +(r.CANTIDAD_REPONER||0);
      return `<tr>
        <td class="lbl">${r.DESCRIPCION}</td>
        <td style="color:var(--muted)">${r.UNIDAD||''}</td>
        <td class="num">${+(r.EXISTENCIA_ACTUAL||0).toFixed(1)}</td>
        <td class="num" style="color:var(--blue)">${+(r.CONSUMO_DIARIO||0).toFixed(2)}/d&iacute;a</td>
        <td class="num"><span style="color:${alertaCol};font-weight:600">${dias>9000?'&infin;':dias+' d'}</span></td>
        <td class="num">${+(r.STOCK_MINIMO_RECOMENDADO||0)}</td>
        <td class="num" style="color:${reponer>0?'var(--orange)':'var(--muted)'}">${reponer>0?'+'+reponer:'--'}</td>
        <td><span style="font-family:\'DM Mono\',monospace;font-size:.65rem;color:${alertaCol}">${alertaTxt}</span></td>
      </tr>`;
    }).join('') || '<tr><td colspan="8" style="text-align:center;color:var(--muted);padding:1.5rem">Sin datos</td></tr>';
  }catch(e){console.error('forecast',e);}
}

async function loadSinMovimiento(){
  try{
    const rows = await fetch(`${API}/api/inv/sin-movimiento?dias=180&limit=50`).then(r=>r.json());
    setText('sinmov-badge', rows.length+' art&iacute;culos');
    document.getElementById('tbl-sinmov-body').innerHTML = rows.map(r=>{
      const dias = +(r.DIAS_SIN_VENTA||9999);
      const col  = dias>365?'var(--red)':dias>180?'var(--orange)':'var(--yellow)';
      return `<tr>
        <td class="lbl">${r.DESCRIPCION}</td>
        <td style="color:var(--muted)">${r.UNIDAD||''}</td>
        <td class="num" style="color:var(--yellow)">${+(r.EXISTENCIA_ACTUAL||0).toFixed(1)}</td>
        <td class="num">${+(r.MIN_ACTUAL||0)}</td>
        <td class="num"><span style="color:${col};font-weight:600">${dias>9000?'Sin hist.':dias+' d&iacute;as'}</span></td>
        <td class="num" style="color:var(--muted)">${r.ULTIMO_MOVIMIENTO||'Sin registro'}</td>
      </tr>`;
    }).join('') || '<tr><td colspan="6" style="text-align:center;color:var(--muted);padding:1.5rem">Sin art&iacute;culos sin movimiento &#127881;</td></tr>';
  }catch(e){console.error('sin-movimiento',e);}
}

loadAll();
setInterval(loadAll, 120000);
</script>
<script src="nav.js"></script>
</body>
</html>
'use strict';

/**
 * Suminregio Parker â€” API Server v9.0
 * â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
 * FIXES v9 sobre v8:
 *  â€¢ cxcClienteSQL() â€” REVERTIDO a IMPORTES_DOCTOS_CC (user rechazÃ³ SALDOS_CC)
 *                      Ahora suma IMPORTE + COALESCE(IMPUESTO,0) para incluir IVA
 *  â€¢ cxcCargosSQL()  â€” Inner subquery REVERTIDA a IMPORTES_DOCTOS_CC (sin SALDOS_CC)
 *  â€¢ /api/debug/cxc  â€” Enriquecido: distribuciÃ³n TIPO_IMPTE, IMPORTE vs IMPORTE+IVA,
 *                      comparaciÃ³n DOCTOS_CC cargo sin registro en IMPORTES_DOCTOS_CC
 *  â€¢ Inventario: fixed EXISTENCIA_MINIMA â†’ SQL_MINIMO_SUB join, UNIDAD_MEDIDA â†’ UNIDAD_VENTA
 * FIXES v6 sobre v5:
 *  â€¢ CXC Balance: cxcClienteSQL() = saldo neto REAL por cliente (C - R groupado
 *                 por CLIENTE_ID). En Microsip, CARGO y RECIBO tienen diferentes
 *                 DOCTO_CC_ID, por lo que nunca se podÃ­a netear por documento.
 *  â€¢ cxcCargosSQL() = docs cargo de clientes con saldo pendiente (para aging)
 *  â€¢ /api/cxc/top-deudores  â†’ usa cxcClienteSQL + cxcCargosSQL combinados
 *  â€¢ /api/cxc/historial     â†’ usa cxcCargosSQL (CLIENTE_ID directo)
 *  â€¢ /api/cxc/por-condicion â†’ usa cxcCargosSQL + CLIENTES.COND_PAGO_ID
 *  â€¢ /api/director/resumen  â†’ CXC: cxcClienteSQL (saldo) + cxcCargosSQL (aging)
 *  â€¢ Static: express.static(__dirname) con charset=utf-8 (corrige Ã±, Ã¡, etc.)
 * FIXES v5 sobre v4:
 *  â€¢ META_IDEAL corregida a 10% sobre base (antes 30%)
 *  â€¢ CXC Aging: usa VENCIMIENTOS_CARGOS_CC.FECHA_VENCIMIENTO para calcular
 *               dÃ­as vencidos desde la fecha de vencimiento REAL del documento
 *               Fallback: DOCTOS_CC.FECHA + CONDICIONES_PAGO.DIAS_PPAG
 *  â€¢ Ventas: UNION ALL de DOCTOS_VE (Industrial) + DOCTOS_PV (Mostrador)
 *            ParÃ¡metro ?tipo=VE o ?tipo=PV para filtrar por fuente
 *  â€¢ Cotizaciones: solo DOCTOS_VE (PV no maneja cotizaciones)
 *  â€¢ Nuevos endpoints:
 *      /api/ventas/cobradas         Facturas cobradas por vendedor
 *      /api/ventas/margen           Margen por vendedor/mes (precio venta)
 *      /api/cxc/por-condicion       CXC agrupado por condiciÃ³n de pago
 *      /api/ventas/ranking-clientes Ranking clientes con saldo y condiciÃ³n
 *      /api/debug/pv                DiagnÃ³stico DOCTOS_PV
 * â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
 */

require('dotenv').config();

const express  = require('express');
const cors     = require('cors');
const Firebird = require('node-firebird');
const path     = require('path');

const app  = express();
const PORT = process.env.PORT || 7000;

// â”€â”€ Middleware â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
app.use(cors({ origin: process.env.CORS_ORIGIN || '*' }));
app.use(express.json());

// Servir archivos estÃ¡ticos â€” raÃ­z del proyecto Y carpeta public/
const staticOpts = {
  setHeaders: (res, filePath) => {
    if (filePath.endsWith('.html')) res.setHeader('Content-Type', 'text/html; charset=utf-8');
    if (filePath.endsWith('.js'))   res.setHeader('Content-Type', 'application/javascript; charset=utf-8');
    if (filePath.endsWith('.css'))  res.setHeader('Content-Type', 'text/css; charset=utf-8');
  }
};
app.use(express.static(__dirname, staticOpts));                          // raÃ­z
app.use(express.static(path.join(__dirname, 'public'), staticOpts));     // carpeta public/

// â”€â”€ ConfiguraciÃ³n Firebird â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const DB_OPTIONS = {
  host          : process.env.FB_HOST     || '127.0.0.1',
  port          : parseInt(process.env.FB_PORT) || 3050,
  database      : process.env.FB_DATABASE || 'C:/Microsip datos/SUMINREGIO-PARKER.FDB',
  user          : process.env.FB_USER     || 'SYSDBA',
  password      : process.env.FB_PASSWORD || 'masterkey',
  lowercase_keys: false,    // campos en MAYÃšSCULAS
  charset       : 'UTF8',    // Firebird convierte WIN1252â†’UTF8 automÃ¡ticamente
};

console.log('DB path:', DB_OPTIONS.database);

// â”€â”€ Helper: ejecuta query â†’ promesa â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
function query(sql, params = [], timeoutMs = 12000) {
  const queryPromise = new Promise((resolve, reject) => {
    Firebird.attach(DB_OPTIONS, (err, db) => {
      if (err) return reject(err);
      db.query(sql, params, (err2, result) => {
        db.detach();
        if (err2) return reject(err2);
        resolve(result || []);
      });
    });
  });
  const timeoutPromise = new Promise((_, reject) =>
    setTimeout(() => reject(new Error(`Query timeout (${timeoutMs}ms)`)), timeoutMs)
  );
  return Promise.race([queryPromise, timeoutPromise]);
}

// â”€â”€ Helper: ruta GET con manejo de errores â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
function get(routePath, handler) {
  app.get(routePath, async (req, res) => {
    try {
      const data = await handler(req);
      res.json(data);
    } catch (e) {
      console.error(`[ERROR] ${routePath} â†’`, e.message);
      res.status(500).json({ error: e.message, path: routePath });
    }
  });
}

// â”€â”€ Helper: construye clÃ¡usulas WHERE para filtros de fecha/vendedor/cliente â”€
// Soporta: anio, mes, dia, vendedor, cliente, desde (YYYY-MM-DD), hasta (YYYY-MM-DD)
// Retorna { sql, params, lookbackOverride }
// lookbackOverride: dÃ­as desde 'desde' hasta hoy â€” para que los endpoints
//   con ventana fija (CURRENT_DATE - N) no corten los datos pedidos.
function buildFiltros(req, alias = 'd') {
  const conds  = [];
  const params = [];
  const { anio, mes, dia, vendedor, cliente } = req.query;
  let   { desde, hasta } = req.query;

  // Validar formato bÃ¡sico YYYY-MM-DD
  const reDate = /^\d{4}-\d{2}-\d{2}$/;
  if (desde && !reDate.test(desde)) desde = null;
  if (hasta && !reDate.test(hasta)) hasta = null;

  if (desde) { conds.push(`CAST(${alias}.FECHA AS DATE) >= CAST(? AS DATE)`); params.push(desde); }
  if (hasta) { conds.push(`CAST(${alias}.FECHA AS DATE) <= CAST(? AS DATE)`); params.push(hasta); }

  // anio/mes solo si no hay rango explÃ­cito
  if (!desde) {
    if (anio) { conds.push(`EXTRACT(YEAR  FROM ${alias}.FECHA) = ?`); params.push(parseInt(anio)); }
    if (mes)  { conds.push(`EXTRACT(MONTH FROM ${alias}.FECHA) = ?`); params.push(parseInt(mes)); }
  }
  if (dia)      { conds.push(`CAST(${alias}.FECHA AS DATE) = CAST(? AS DATE)`); params.push(dia); }
  if (vendedor) { conds.push(`${alias}.VENDEDOR_ID = ?`);  params.push(parseInt(vendedor)); }
  if (cliente)  { conds.push(`${alias}.CLIENTE_ID  = ?`);  params.push(parseInt(cliente)); }

  // Si hay desde, calcular cuÃ¡ntos dÃ­as de lookback necesitamos
  let lookbackOverride = null;
  if (desde) {
    const daysAgo = Math.ceil((Date.now() - new Date(desde).getTime()) / 86400000);
    lookbackOverride = Math.max(daysAgo + 5, 31);
  }

  return { sql: conds.length ? ' AND ' + conds.join(' AND ') : '', params, lookbackOverride };
}

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  VENTAS â€” MACRO SQL (UNION ALL DOCTOS_VE + DOCTOS_PV)
//  tipo: 'VE'=Industrial, 'PV'=Mostrador, ''=Todos
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

/**
 * Genera sub-SELECT con UNION ALL de DOCTOS_VE + DOCTOS_PV,
 * filtrando solo Facturas/Ventas vÃ¡lidas sin doble-conteo.
 * @param {string} tipo - 'VE', 'PV' o '' (todos)
 */
function ventasSub(tipo = '') {
  const ve = `
    SELECT
      d.FECHA,
      d.IMPORTE_NETO,
      COALESCE(d.VENDEDOR_ID, 0)  AS VENDEDOR_ID,
      COALESCE(d.CLIENTE_ID,  0)  AS CLIENTE_ID,
      d.FOLIO,
      d.TIPO_DOCTO,
      d.ESTATUS,
      'VE' AS TIPO_SRC
    FROM DOCTOS_VE d
    WHERE (
      (d.TIPO_DOCTO = 'F' AND d.ESTATUS <> 'C')
      OR (d.TIPO_DOCTO = 'V' AND d.ESTATUS NOT IN ('C','T'))
    )`;

  const pv = `
    SELECT
      d.FECHA,
      d.IMPORTE_NETO,
      COALESCE(d.VENDEDOR_ID, 0)  AS VENDEDOR_ID,
      COALESCE(d.CLIENTE_ID,  0)  AS CLIENTE_ID,
      d.FOLIO,
      d.TIPO_DOCTO,
      d.ESTATUS,
      'PV' AS TIPO_SRC
    FROM DOCTOS_PV d
    WHERE (
      (d.TIPO_DOCTO = 'F' AND d.ESTATUS <> 'C')
      OR (d.TIPO_DOCTO = 'V' AND d.ESTATUS NOT IN ('C','T'))
    )`;

  if (tipo === 'VE') return `(${ve})`;
  if (tipo === 'PV') return `(${pv})`;
  return `(${ve} UNION ALL ${pv})`;
}

// â”€â”€ Filtro de tipo de ventas desde request â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
function getTipo(req) {
  const t = (req.query.tipo || '').toUpperCase();
  return t === 'VE' ? 'VE' : t === 'PV' ? 'PV' : '';
}

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  CXC â€” MACRO SQL con aging por fecha de vencimiento real
//  Usa VENCIMIENTOS_CARGOS_CC.FECHA_VENCIMIENTO
//  Fallback: DOCTOS_CC.FECHA + CONDICIONES_PAGO.DIAS_PPAG
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

// â”€â”€ v10: Saldo neto por cliente â€” fÃ³rmula alineada al DAX Power BI â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// CARGO  (TIPO_IMPTE='C'): usa IMPORTE (base ex-IVA). El IVA se guarda en IMPUESTO
//                           pero NO se suma al cargo â€” asÃ­ equiparamos con el cobro.
// COBRO  (TIPO_IMPTE='R'): normaliza a base ex-IVA:
//   Â· Si IMPUESTO > 0 el IMPORTE ya es ex-IVA   â†’ usar IMPORTE
//   Â· Si IMPUESTO = 0 el pago incluye IVA 16%   â†’ dividir entre 1.16
// Referencia DAX: Cobro = IF(IMPUESTO>0, IMPORTE, IMPORTE/1.16)
function cxcClienteSQL() {
  return `(
    SELECT
      dc.CLIENTE_ID,
      SUM(CASE
        WHEN i.TIPO_IMPTE = 'C'
          THEN i.IMPORTE
        WHEN i.TIPO_IMPTE = 'R'
          THEN -(CASE WHEN COALESCE(i.IMPUESTO, 0) > 0
                      THEN i.IMPORTE
                      ELSE i.IMPORTE / 1.16 END)
        ELSE 0
      END) AS SALDO
    FROM IMPORTES_DOCTOS_CC i
    JOIN DOCTOS_CC dc ON dc.DOCTO_CC_ID = i.DOCTO_CC_ID
    WHERE COALESCE(i.CANCELADO, 'N') = 'N'
    GROUP BY dc.CLIENTE_ID
    HAVING SUM(CASE
        WHEN i.TIPO_IMPTE = 'C'
          THEN i.IMPORTE
        WHEN i.TIPO_IMPTE = 'R'
          THEN -(CASE WHEN COALESCE(i.IMPUESTO, 0) > 0
                      THEN i.IMPORTE
                      ELSE i.IMPORTE / 1.16 END)
        ELSE 0
      END) > 1
  )`;
}

// â”€â”€ v9: Documentos de CARGO para clientes que aÃºn tienen saldo pendiente â”€â”€â”€â”€â”€
// Para aging y detalles por documento. Solo TIPO_IMPTE='C' y solo para
// clientes cuyo saldo neto (IMPORTES_DOCTOS_CC) > $1.
// NULLIF(DIAS_PPAG,0) evita vencimiento inmediato en crÃ©dito de 0 dÃ­as.
function cxcCargosSQL() {
  return `(
    SELECT
      i.DOCTO_CC_ID,
      dc.CLIENTE_ID,
      dc.FOLIO,
      i.IMPORTE                                                       AS SALDO,
      CAST(COALESCE(
        MIN(vc.FECHA_VENCIMIENTO),
        CAST(dc.FECHA AS DATE) + CAST(COALESCE(NULLIF(cp.DIAS_PPAG, 0), 30) AS INTEGER)
      ) AS DATE)                                                      AS FECHA_VENCIMIENTO,
      (CURRENT_DATE - CAST(COALESCE(
        MIN(vc.FECHA_VENCIMIENTO),
        CAST(dc.FECHA AS DATE) + CAST(COALESCE(NULLIF(cp.DIAS_PPAG, 0), 30) AS INTEGER)
      ) AS DATE))                                                     AS DIAS_VENCIDO
    FROM IMPORTES_DOCTOS_CC i
    JOIN  DOCTOS_CC dc         ON dc.DOCTO_CC_ID  = i.DOCTO_CC_ID
    LEFT JOIN CONDICIONES_PAGO cp ON cp.COND_PAGO_ID = dc.COND_PAGO_ID
    LEFT JOIN VENCIMIENTOS_CARGOS_CC vc ON vc.DOCTO_CC_ID = i.DOCTO_CC_ID
    WHERE i.TIPO_IMPTE = 'C'
      AND COALESCE(i.CANCELADO, 'N') = 'N'
      AND dc.CLIENTE_ID IN (
        SELECT dc2.CLIENTE_ID
        FROM IMPORTES_DOCTOS_CC i2
        JOIN DOCTOS_CC dc2 ON dc2.DOCTO_CC_ID = i2.DOCTO_CC_ID
        WHERE COALESCE(i2.CANCELADO, 'N') = 'N'
        GROUP BY dc2.CLIENTE_ID
        HAVING SUM(CASE
            WHEN i2.TIPO_IMPTE = 'C'
              THEN i2.IMPORTE
            WHEN i2.TIPO_IMPTE = 'R'
              THEN -(CASE WHEN COALESCE(i2.IMPUESTO,0) > 0
                          THEN i2.IMPORTE ELSE i2.IMPORTE/1.16 END)
            ELSE 0
          END) > 0
      )
    GROUP BY i.DOCTO_CC_ID, dc.CLIENTE_ID, dc.FOLIO, dc.FECHA,
             i.IMPORTE, i.IMPUESTO, cp.DIAS_PPAG
  )`;
}

// Deprecated â€” mantener para compatibilidad pero los endpoints usan cxcClienteSQL/cxcCargosSQL
function cxcSaldosSub() { return cxcCargosSQL(); }

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  CONFIG / METAS  âœ… v5: META_IDEAL = 10% sobre base
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

get('/api/config/metas', async () => {
  const rows = await query(`
    SELECT COUNT(DISTINCT VENDEDOR_ID) AS NUM_VENDEDORES
    FROM DOCTOS_VE
    WHERE (
      (TIPO_DOCTO = 'F' AND ESTATUS <> 'C')
      OR (TIPO_DOCTO = 'V' AND ESTATUS NOT IN ('C', 'T'))
    )
    AND EXTRACT(YEAR  FROM FECHA) = EXTRACT(YEAR  FROM CURRENT_DATE)
    AND EXTRACT(MONTH FROM FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)
  `);
  const numV = (rows[0] && rows[0].NUM_VENDEDORES) ? Number(rows[0].NUM_VENDEDORES) : 1;

  const META_DIA_V   = 5650;
  const META_IDEAL_V = 5650 * 1.10;
  const META_DIA_C   = 10000;
  const META_IDEAL_C = 10000 * 1.10;

  return {
    META_DIARIA_POR_VENDEDOR : META_DIA_V,
    META_IDEAL_POR_VENDEDOR  : META_IDEAL_V,
    META_COTI_POR_VENDEDOR   : META_DIA_C,
    META_COTI_IDEAL          : META_IDEAL_C,
    META_TOTAL_DIARIA        : META_DIA_V   * numV,
    META_IDEAL_TOTAL         : META_IDEAL_V * numV,
    META_COTI_TOTAL          : META_DIA_C   * numV,
    META_COTI_IDEAL_TOTAL    : META_IDEAL_C * numV,
    NUM_VENDEDORES           : numV,
    MARGEN_COMISION          : 0.08,
  };
});

get('/api/config/filtros', async () => {
  const [vendedores, clientes, anios] = await Promise.all([
    query(`SELECT VENDEDOR_ID, NOMBRE FROM VENDEDORES WHERE COALESCE(ESTATUS,'A') NOT IN ('I','B','0','N') ORDER BY NOMBRE`)
      .catch(() => query(`SELECT VENDEDOR_ID, NOMBRE FROM VENDEDORES ORDER BY NOMBRE`)),
    query(`
      SELECT FIRST 500 d.CLIENTE_ID, c.NOMBRE
      FROM (
        SELECT DISTINCT CLIENTE_ID FROM DOCTOS_VE
        WHERE ((TIPO_DOCTO='F' AND ESTATUS<>'C') OR (TIPO_DOCTO='V' AND ESTATUS NOT IN ('C','T')))
          AND FECHA >= (CURRENT_DATE - 365)
      ) d
      JOIN CLIENTES c ON c.CLIENTE_ID = d.CLIENTE_ID
      ORDER BY c.NOMBRE
    `),
    query(`
      SELECT DISTINCT EXTRACT(YEAR FROM FECHA) AS ANIO
      FROM DOCTOS_VE
      WHERE (TIPO_DOCTO='F' OR TIPO_DOCTO='V') AND ESTATUS <> 'C'
      ORDER BY ANIO DESC
    `),
  ]);
  return { vendedores, clientes, anios };
});
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  VENTAS â€” RESÃšMENES
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

get('/api/ventas/resumen', async (req) => {
  const f    = buildFiltros(req, 'd');
  const tipo = getTipo(req);
  const rows = await query(`
    SELECT
      SUM(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE
               THEN d.IMPORTE_NETO ELSE 0 END)                      AS HOY,
      SUM(CASE WHEN EXTRACT(YEAR  FROM d.FECHA) = EXTRACT(YEAR  FROM CURRENT_DATE)
               AND  EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)
               THEN d.IMPORTE_NETO ELSE 0 END)                      AS MES_ACTUAL,
      COUNT(CASE WHEN EXTRACT(YEAR  FROM d.FECHA) = EXTRACT(YEAR  FROM CURRENT_DATE)
                 AND  EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)
                 THEN 1 END)                                         AS FACTURAS_MES,
      SUM(CASE WHEN EXTRACT(YEAR  FROM d.FECHA) = EXTRACT(YEAR  FROM CURRENT_DATE)
               AND  EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)
               AND  CAST(d.FECHA AS DATE) <= CURRENT_DATE - 1
               THEN d.IMPORTE_NETO ELSE 0 END)                      AS HASTA_AYER_MES
    FROM ${ventasSub(tipo)} d
    WHERE 1=1 ${f.sql}
  `, f.params).catch(() => []);
  return rows[0] || {};
});

get('/api/ventas/cotizaciones/resumen', async (req) => {
  const f = buildFiltros(req, 'd');
  const rows = await query(`
    SELECT
      SUM(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE
               THEN d.IMPORTE_NETO ELSE 0 END)                      AS HOY,
      SUM(CASE WHEN EXTRACT(YEAR  FROM d.FECHA) = EXTRACT(YEAR  FROM CURRENT_DATE)
               AND  EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)
               THEN d.IMPORTE_NETO ELSE 0 END)                      AS MES_ACTUAL,
      COUNT(CASE WHEN EXTRACT(YEAR  FROM d.FECHA) = EXTRACT(YEAR  FROM CURRENT_DATE)
                 AND  EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)
                 THEN 1 END)                                         AS COTIZACIONES_MES,
      COUNT(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE THEN 1 END) AS COTIZACIONES_HOY
    FROM DOCTOS_VE d
    WHERE d.TIPO_DOCTO = 'C' AND d.ESTATUS <> 'C'
      ${f.sql}
  `, f.params).catch(() => []);
  return rows[0] || {};
});

get('/api/ventas/diarias', async (req) => {
  const tipo = getTipo(req);
  const dias = Math.min(parseInt(req.query.dias) || 30, 366);
  const sql = tipo === ''
    ? `
    SELECT CAST(d.FECHA AS DATE) AS DIA,
      COALESCE(SUM(CASE WHEN d.TIPO_SRC = 'VE' THEN d.IMPORTE_NETO ELSE 0 END), 0) AS VENTAS_VE,
      COALESCE(SUM(CASE WHEN d.TIPO_SRC = 'PV' THEN d.IMPORTE_NETO ELSE 0 END), 0) AS VENTAS_PV,
      COALESCE(SUM(d.IMPORTE_NETO), 0) AS TOTAL_VENTAS
    FROM ${ventasSub()} d
    WHERE CAST(d.FECHA AS DATE) >= CURRENT_DATE - ?
    GROUP BY CAST(d.FECHA AS DATE) ORDER BY 1
    `
    : `
    SELECT CAST(d.FECHA AS DATE) AS DIA, COUNT(*) AS FACTURAS, COALESCE(SUM(d.IMPORTE_NETO), 0) AS TOTAL_VENTAS
    FROM ${ventasSub(tipo)} d
    WHERE CAST(d.FECHA AS DATE) >= CURRENT_DATE - ?
    GROUP BY CAST(d.FECHA AS DATE) ORDER BY 1
    `;
  const rows = await query(sql, [dias]).catch(() => []);
  if (tipo !== '') return (rows || []).map(r => ({ DIA: r.DIA, VENTAS_VE: tipo === 'VE' ? (r.TOTAL_VENTAS || 0) : 0, VENTAS_PV: tipo === 'PV' ? (r.TOTAL_VENTAS || 0) : 0, TOTAL_VENTAS: r.TOTAL_VENTAS || 0 }));
  return rows || [];
});

get('/api/ventas/semanales', async (req) => {
  const f = buildFiltros(req, 'd');
  const tipo = getTipo(req);
  return query(`
    SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(WEEK FROM d.FECHA) AS SEMANA,
      COUNT(*) AS FACTURAS, COALESCE(SUM(d.IMPORTE_NETO),0) AS TOTAL
    FROM ${ventasSub(tipo)} d WHERE 1=1 ${f.sql}
    GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(WEEK FROM d.FECHA) ORDER BY 1, 2
  `, f.params).catch(() => []);
});

get('/api/ventas/mensuales', async (req) => {
  const mesesN = Math.min(Math.max(parseInt(req.query.meses) || 12, 1), 24);
  const desde = new Date();
  desde.setMonth(desde.getMonth() - mesesN);
  const desdeStr = desde.getFullYear() + '-' + String(desde.getMonth() + 1).padStart(2, '0') + '-01';
  const f = { sql: ' AND CAST(d.FECHA AS DATE) >= CAST(? AS DATE)', params: [desdeStr] };
  const tipo = getTipo(req);
  if (tipo === '') {
    const rows = await query(`
      SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(MONTH FROM d.FECHA) AS MES,
        COUNT(*) AS FACTURAS,
        COALESCE(SUM(CASE WHEN d.TIPO_SRC = 'VE' THEN d.IMPORTE_NETO ELSE 0 END), 0) AS VENTAS_VE,
        COALESCE(SUM(CASE WHEN d.TIPO_SRC = 'PV' THEN d.IMPORTE_NETO ELSE 0 END), 0) AS VENTAS_PV,
        COALESCE(SUM(d.IMPORTE_NETO), 0) AS TOTAL
      FROM ${ventasSub()} d WHERE 1=1 ${f.sql}
      GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(MONTH FROM d.FECHA) ORDER BY 1, 2
    `, f.params).catch(() => []);
    return (rows || []).map(r => ({ ANIO: r.ANIO, MES: r.MES, FACTURAS: r.FACTURAS, VENTAS_VE: r.VENTAS_VE, VENTAS_PV: r.VENTAS_PV, TOTAL: r.TOTAL }));
  }
  return query(`
    SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(MONTH FROM d.FECHA) AS MES,
      COUNT(*) AS FACTURAS, COALESCE(SUM(d.IMPORTE_NETO),0) AS TOTAL
    FROM ${ventasSub(tipo)} d WHERE 1=1 ${f.sql}
    GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(MONTH FROM d.FECHA) ORDER BY 1, 2
  `, f.params).catch(() => []).then(rows => (rows || []).map(r => ({ ANIO: r.ANIO, MES: r.MES, FACTURAS: r.FACTURAS, VENTAS_VE: tipo === 'VE' ? r.TOTAL : 0, VENTAS_PV: tipo === 'PV' ? r.TOTAL : 0, TOTAL: r.TOTAL })));
});

get('/api/ventas/cotizaciones/diarias', async (req) => {
  const dias = Math.min(parseInt(req.query.dias) || 30, 366);
  const rows = await query(`
    SELECT CAST(d.FECHA AS DATE) AS DIA, COUNT(*) AS COTIZACIONES, COALESCE(SUM(d.IMPORTE_NETO),0) AS TOTAL_COTIZACIONES
    FROM DOCTOS_VE d WHERE d.TIPO_DOCTO = 'C' AND d.ESTATUS <> 'C'
      AND CAST(d.FECHA AS DATE) >= CURRENT_DATE - ?
    GROUP BY CAST(d.FECHA AS DATE) ORDER BY 1
  `, [dias]).catch(() => []);
  return (rows || []).map(r => ({ DIA: r.DIA, COTIZACIONES: r.COTIZACIONES, TOTAL_COTIZACIONES: r.TOTAL_COTIZACIONES || 0 }));
});

get('/api/ventas/cotizaciones/semanales', async (req) => {
  const f = buildFiltros(req, 'd');
  return query(`
    SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(WEEK FROM d.FECHA) AS SEMANA,
      COUNT(*) AS COTIZACIONES, COALESCE(SUM(d.IMPORTE_NETO),0) AS TOTAL
    FROM DOCTOS_VE d WHERE d.TIPO_DOCTO = 'C' AND d.ESTATUS <> 'C' ${f.sql}
    GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(WEEK FROM d.FECHA) ORDER BY 1, 2
  `, f.params).catch(() => []);
});

get('/api/ventas/cotizaciones/mensuales', async (req) => {
  const f = buildFiltros(req, 'd');
  return query(`
    SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(MONTH FROM d.FECHA) AS MES,
      COUNT(*) AS COTIZACIONES, COALESCE(SUM(d.IMPORTE_NETO),0) AS TOTAL
    FROM DOCTOS_VE d WHERE d.TIPO_DOCTO = 'C' AND d.ESTATUS <> 'C' ${f.sql}
    GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(MONTH FROM d.FECHA) ORDER BY 1, 2
  `, f.params).catch(() => []);
});

get('/api/ventas/top-clientes', async (req) => {
  const f = buildFiltros(req, 'd');
  const tipo = getTipo(req);
  const limit = Math.min(parseInt(req.query.limit) || 20, 100);
  return query(`
    SELECT FIRST ${limit}
      d.CLIENTE_ID, c.NOMBRE AS CLIENTE, COUNT(*) AS FACTURAS, COALESCE(SUM(d.IMPORTE_NETO),0) AS TOTAL
    FROM ${ventasSub(tipo)} d
    LEFT JOIN CLIENTES c ON c.CLIENTE_ID = d.CLIENTE_ID
    WHERE d.CLIENTE_ID > 0 ${f.sql}
    GROUP BY d.CLIENTE_ID, c.NOMBRE ORDER BY TOTAL DESC
  `, f.params).catch(() => []);
});

// ventas (4).html y vendedores esperan: VENDEDOR, VENTAS_HOY, VENTAS_MES, VENTAS_MES_VE, VENTAS_MES_PV, FACTURAS_HOY, FACTURAS_MES
get('/api/ventas/por-vendedor', async (req) => {
  const f = buildFiltros(req, 'd');
  const tipo = getTipo(req);
  return query(`
    SELECT d.VENDEDOR_ID,
      COALESCE(v.NOMBRE, 'Vendedor ' || CAST(d.VENDEDOR_ID AS VARCHAR(10))) AS VENDEDOR,
      SUM(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE THEN d.IMPORTE_NETO ELSE 0 END) AS VENTAS_HOY,
      SUM(CASE WHEN EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE) THEN d.IMPORTE_NETO ELSE 0 END) AS VENTAS_MES,
      SUM(CASE WHEN EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE) AND d.TIPO_SRC = 'VE' THEN d.IMPORTE_NETO ELSE 0 END) AS VENTAS_MES_VE,
      SUM(CASE WHEN EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE) AND d.TIPO_SRC = 'PV' THEN d.IMPORTE_NETO ELSE 0 END) AS VENTAS_MES_PV,
      SUM(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE THEN 1 ELSE 0 END) AS FACTURAS_HOY,
      SUM(CASE WHEN EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE) THEN 1 ELSE 0 END) AS FACTURAS_MES
    FROM ${ventasSub(tipo)} d
    LEFT JOIN VENDEDORES v ON v.VENDEDOR_ID = d.VENDEDOR_ID
    WHERE EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE) AND d.VENDEDOR_ID > 0 ${f.sql}
    GROUP BY d.VENDEDOR_ID, v.NOMBRE ORDER BY VENTAS_MES DESC
  `, f.params).catch(() => []);
});

get('/api/ventas/por-vendedor/cotizaciones', async (req) => {
  const f = buildFiltros(req, 'd');
  const anio = req.query.anio ? parseInt(req.query.anio) : null;
  const mes = req.query.mes ? parseInt(req.query.mes) : null;
  const y = anio || new Date().getFullYear();
  const m = mes || (new Date().getMonth() + 1);
  const condPeriodo = `EXTRACT(YEAR FROM d.FECHA) = ${y} AND EXTRACT(MONTH FROM d.FECHA) = ${m}`;
  const condVend = req.query.vendedor ? ` AND d.VENDEDOR_ID = ${parseInt(req.query.vendedor)}` : '';
  return query(`
    SELECT v.NOMBRE AS VENDEDOR, d.VENDEDOR_ID,
      SUM(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE THEN d.IMPORTE_NETO ELSE 0 END) AS COTIZACIONES_HOY,
      SUM(CASE WHEN ${condPeriodo} THEN d.IMPORTE_NETO ELSE 0 END) AS COTIZACIONES_MES,
      COUNT(CASE WHEN ${condPeriodo} THEN 1 END) AS NUM_COTI_MES
    FROM DOCTOS_VE d
    JOIN VENDEDORES v ON v.VENDEDOR_ID = d.VENDEDOR_ID
    WHERE d.TIPO_DOCTO = 'C' AND d.ESTATUS <> 'C' AND ${condPeriodo} ${condVend}
    GROUP BY v.NOMBRE, d.VENDEDOR_ID ORDER BY COTIZACIONES_MES DESC
  `).catch(() => []);
});

get('/api/ventas/recientes', async (req) => {
  const f = buildFiltros(req, 'd');
  const tipo = getTipo(req);
  const limit = Math.min(parseInt(req.query.limit) || 30, 100);
  return query(`
    SELECT FIRST ${limit} d.FECHA, d.FOLIO, d.TIPO_DOCTO, d.TIPO_SRC, d.IMPORTE_NETO AS TOTAL, d.CLIENTE_ID,
      COALESCE(c.NOMBRE, 'Sin cliente') AS CLIENTE,
      COALESCE(v.NOMBRE, 'Sin vendedor') AS VENDEDOR
    FROM ${ventasSub(tipo)} d
    LEFT JOIN CLIENTES c ON c.CLIENTE_ID = d.CLIENTE_ID
    LEFT JOIN VENDEDORES v ON v.VENDEDOR_ID = d.VENDEDOR_ID
    WHERE 1=1 ${f.sql}
    ORDER BY d.FECHA DESC, d.FOLIO DESC
  `, f.params).catch(() => []);
});

get('/api/ventas/vs-cotizaciones', async (req) => {
  const mesesN = Math.min(parseInt(req.query.meses) || 6, 24);
  const desde = new Date();
  desde.setMonth(desde.getMonth() - mesesN);
  const desdeStr = desde.getFullYear() + '-' + String(desde.getMonth() + 1).padStart(2, '0') + '-01';
  const [ventasMes, cotizMes] = await Promise.all([
    query(`
      SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(MONTH FROM d.FECHA) AS MES,
        COALESCE(SUM(d.IMPORTE_NETO), 0) AS TOTAL_VENTAS, COUNT(*) AS NUM_DOCS
      FROM ${ventasSub()} d WHERE CAST(d.FECHA AS DATE) >= CAST(? AS DATE)
      GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(MONTH FROM d.FECHA) ORDER BY 1, 2
    `, [desdeStr]).catch(() => []),
    query(`
      SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(MONTH FROM d.FECHA) AS MES,
        COALESCE(SUM(d.IMPORTE_NETO), 0) AS TOTAL_COTI, COUNT(*) AS NUM_COTI
      FROM DOCTOS_VE d WHERE d.TIPO_DOCTO = 'C' AND d.ESTATUS <> 'C' AND CAST(d.FECHA AS DATE) >= CAST(? AS DATE)
      GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(MONTH FROM d.FECHA) ORDER BY 1, 2
    `, [desdeStr]).catch(() => []),
  ]);
  return { ventas: ventasMes || [], cotizaciones: cotizMes || [] };
});

get('/api/ventas/ranking-clientes', async (req) => {
  const f = buildFiltros(req, 'd');
  const tipo = getTipo(req);
  return query(`
    SELECT d.CLIENTE_ID, c.NOMBRE, COALESCE(SUM(d.IMPORTE_NETO),0) AS VENTA
    FROM ${ventasSub(tipo)} d
    LEFT JOIN CLIENTES c ON c.CLIENTE_ID = d.CLIENTE_ID
    WHERE d.CLIENTE_ID > 0 ${f.sql}
    GROUP BY d.CLIENTE_ID, c.NOMBRE ORDER BY VENTA DESC
  `, f.params).catch(() => []);
});

// Cobradas: por defecto mes actual (no depender de filtros anio/mes para que la pagina siempre muestre datos)
get('/api/ventas/cobradas', async (req) => {
  const tipo = getTipo(req);
  const vendedorQ = req.query.vendedor ? ` AND d.VENDEDOR_ID = ${parseInt(req.query.vendedor)}` : '';
  const dcVendedorQ = ''; // cobros no tienen vendedor directo
  const [rows, cobrosRow] = await Promise.all([
    query(`
      SELECT d.VENDEDOR_ID, v.NOMBRE AS VENDEDOR, COUNT(DISTINCT d.FOLIO) AS NUM_FACTURAS, COALESCE(SUM(d.IMPORTE_NETO),0) AS TOTAL_VENTA
      FROM ${ventasSub(tipo)} d
      LEFT JOIN VENDEDORES v ON v.VENDEDOR_ID = d.VENDEDOR_ID
      WHERE d.VENDEDOR_ID > 0
        AND EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE)
        AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)
        ${vendedorQ}
      GROUP BY d.VENDEDOR_ID, v.NOMBRE
    `).catch(() => []),
    query(`
      SELECT COALESCE(SUM(CASE WHEN COALESCE(i.IMPUESTO,0) > 0 THEN i.IMPORTE ELSE i.IMPORTE / 1.16 END), 0) AS TOTAL_COBRADO
      FROM IMPORTES_DOCTOS_CC i
      JOIN DOCTOS_CC dc ON dc.DOCTO_CC_ID = i.DOCTO_CC_ID
      WHERE i.TIPO_IMPTE = 'R' AND COALESCE(i.CANCELADO, 'N') = 'N'
        AND EXTRACT(YEAR FROM dc.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE)
        AND EXTRACT(MONTH FROM dc.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)
    `).catch(() => [{ TOTAL_COBRADO: 0 }]),
  ]);
  const totalCobradoReal = +(cobrosRow && cobrosRow[0] && cobrosRow[0].TOTAL_COBRADO) || 0;
  const totalFacturado = (rows || []).reduce((s, r) => s + (+r.TOTAL_VENTA || 0), 0);
  const mapped = (rows || []).map(r => ({
    VENDEDOR_ID: r.VENDEDOR_ID,
    VENDEDOR: r.VENDEDOR,
    NOMBRE: r.VENDEDOR,
    NUM_FACTURAS: r.NUM_FACTURAS,
    FACTURAS_COBRADAS: r.NUM_FACTURAS,
    TOTAL_VENTA: +r.TOTAL_VENTA || 0,
    TOTAL_COBRADO: totalCobradoReal > 0 && totalFacturado > 0 ? Math.round((+r.TOTAL_VENTA || 0) / totalFacturado * totalCobradoReal * 100) / 100 : +r.TOTAL_VENTA || 0,
  }));
  return { vendedores: mapped, totalFacturado, totalCobrado: totalCobradoReal };
});

get('/api/ventas/margen', async (req) => {
  const f = buildFiltros(req, 'd');
  return query(`
    SELECT d.VENDEDOR_ID, v.NOMBRE, EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(MONTH FROM d.FECHA) AS MES,
      COALESCE(SUM(det.PRECIO_TOTAL - COALESCE(det.COSTO_TOTAL, 0)), 0) AS MARGEN,
      COALESCE(SUM(det.PRECIO_TOTAL), 0) AS VENTA
    FROM DOCTOS_VE d
    JOIN DOCTOS_VE_DET det ON det.DOCTO_VE_ID = d.DOCTO_VE_ID
    LEFT JOIN VENDEDORES v ON v.VENDEDOR_ID = d.VENDEDOR_ID
    WHERE (d.TIPO_DOCTO = 'F' OR d.TIPO_DOCTO = 'V') AND d.ESTATUS <> 'C' ${f.sql}
    GROUP BY d.VENDEDOR_ID, v.NOMBRE, EXTRACT(YEAR FROM d.FECHA), EXTRACT(MONTH FROM d.FECHA)
  `, f.params).catch(() => []);
});

get('/api/ventas/margen-articulos', async (req) => {
  const f = buildFiltros(req, 'd');
  const limit = Math.min(parseInt(req.query.limit) || 50, 200);
  return query(`
    SELECT a.ARTICULO_ID, a.DESCRIPCION, SUM(det.PRECIO_TOTAL - COALESCE(det.COSTO_TOTAL, 0)) AS MARGEN, SUM(det.PRECIO_TOTAL) AS VENTA
    FROM DOCTOS_VE d
    JOIN DOCTOS_VE_DET det ON det.DOCTO_VE_ID = d.DOCTO_VE_ID
    LEFT JOIN ARTICULOS a ON a.ARTICULO_ID = det.ARTICULO_ID
    WHERE (d.TIPO_DOCTO = 'F' OR d.TIPO_DOCTO = 'V') AND d.ESTATUS <> 'C' ${f.sql}
    GROUP BY a.ARTICULO_ID, a.DESCRIPCION ORDER BY MARGEN DESC
  `, f.params).catch(() => []);
});

get('/api/ventas/cotizaciones', async (req) => {
  const f = buildFiltros(req, 'd');
  const limit = Math.min(parseInt(req.query.limit) || 50, 200);
  return query(`
    SELECT d.DOCTO_VE_ID, d.FECHA, d.FOLIO, d.IMPORTE_NETO, d.CLIENTE_ID, c.NOMBRE AS CLIENTE, d.VENDEDOR_ID
    FROM DOCTOS_VE d
    LEFT JOIN CLIENTES c ON c.CLIENTE_ID = d.CLIENTE_ID
    WHERE d.TIPO_DOCTO = 'C' AND d.ESTATUS <> 'C' ${f.sql}
    ORDER BY d.FECHA DESC
  `, f.params).catch(() => []);
});

get('/api/ventas/vendedores', async () => {
  return query(`SELECT VENDEDOR_ID, NOMBRE FROM VENDEDORES WHERE COALESCE(ESTATUS,'A') NOT IN ('I','B','0','N') ORDER BY NOMBRE`).catch(() => []);
});

get('/api/ventas/diario', async (req) => {
  const f = buildFiltros(req, 'd');
  const tipo = getTipo(req);
  return query(`
    SELECT CAST(d.FECHA AS DATE) AS FECHA, COUNT(*) AS FACTURAS, COALESCE(SUM(d.IMPORTE_NETO),0) AS TOTAL
    FROM ${ventasSub(tipo)} d WHERE 1=1 ${f.sql}
    GROUP BY CAST(d.FECHA AS DATE) ORDER BY 1
  `, f.params).catch(() => []);
});

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  VENTAS â€” CUMPLIMIENTO (con filtros anio, mes, vendedor)
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

get('/api/ventas/cumplimiento', async (req) => {
  const f = buildFiltros(req, 'd');
  const anioQ = req.query.anio ? parseInt(req.query.anio) : null;
  const mesQ = req.query.mes ? parseInt(req.query.mes) : null;
  const vendedorQ = req.query.vendedor ? parseInt(req.query.vendedor) : null;
  const desde = req.query.desde;
  const hasta = req.query.hasta;

  const [metas] = await query(`SELECT COALESCE(MAX(META_DIARIA_POR_VENDEDOR),0) AS META_DIA, COALESCE(MAX(META_IDEAL_POR_VENDEDOR),0) AS META_IDEAL FROM CONFIGURACIONES_GEN`).catch(() => [{ META_DIA: 5650, META_IDEAL: 6500 }]);
  const metaDia = +(metas && metas.META_DIA) || 5650;
  const metaIdeal = +(metas && metas.META_IDEAL) || 6500;

  let condAnioMes = 'EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)';
  let diasTranscurridos = 22;
  if (desde && hasta && /^\d{4}-\d{2}-\d{2}$/.test(desde) && /^\d{4}-\d{2}-\d{2}$/.test(hasta)) {
    condAnioMes = `CAST(d.FECHA AS DATE) >= CAST('${desde}' AS DATE) AND CAST(d.FECHA AS DATE) <= CAST('${hasta}' AS DATE)`;
    diasTranscurridos = Math.ceil((new Date(hasta) - new Date(desde)) / 86400000) + 1;
    diasTranscurridos = Math.min(Math.max(diasTranscurridos, 1), 31);
  } else if (anioQ) {
    condAnioMes = `EXTRACT(YEAR FROM d.FECHA) = ${anioQ}`;
    if (mesQ) {
      condAnioMes += ` AND EXTRACT(MONTH FROM d.FECHA) = ${mesQ}`;
      const daysInMonth = new Date(anioQ, mesQ, 0).getDate();
      const today = new Date();
      diasTranscurridos = (anioQ === today.getFullYear() && mesQ === today.getMonth() + 1) ? today.getDate() : daysInMonth;
    } else {
      const today = new Date();
      diasTranscurridos = (anioQ === today.getFullYear()) ? Math.ceil((today - new Date(anioQ, 0, 1)) / 86400000) + 1 : 365;
    }
  }

  const condVendedor = vendedorQ ? ` AND d.VENDEDOR_ID = ${vendedorQ}` : '';
  const anioExpr = anioQ || 'EXTRACT(YEAR FROM CURRENT_DATE)';

  const ventas = await query(`
    SELECT d.VENDEDOR_ID,
      SUM(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE THEN d.IMPORTE_NETO ELSE 0 END) AS VENTA_HOY,
      SUM(CASE WHEN ${condAnioMes} THEN d.IMPORTE_NETO ELSE 0 END) AS VENTA_MES,
      SUM(CASE WHEN EXTRACT(YEAR FROM d.FECHA) = ${anioExpr} THEN d.IMPORTE_NETO ELSE 0 END) AS VENTA_YTD,
      SUM(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE THEN 1 ELSE 0 END) AS FACTURAS_HOY,
      SUM(CASE WHEN ${condAnioMes} THEN 1 ELSE 0 END) AS FACTURAS_MES
    FROM ${ventasSub()} d
    WHERE ${condAnioMes} AND d.VENDEDOR_ID > 0 ${condVendedor}
    GROUP BY d.VENDEDOR_ID
  `).catch(() => []);

  const ventaMap = {};
  (ventas || []).forEach(v => { ventaMap[v.VENDEDOR_ID] = v; });

  const rows = await query(`
    SELECT DISTINCT d.VENDEDOR_ID, COALESCE(v.NOMBRE, 'Vendedor ' || CAST(d.VENDEDOR_ID AS VARCHAR(10))) AS NOMBRE
    FROM ${ventasSub()} d
    LEFT JOIN VENDEDORES v ON v.VENDEDOR_ID = d.VENDEDOR_ID
    WHERE ${condAnioMes} AND d.VENDEDOR_ID > 0 ${condVendedor}
    ORDER BY 2
  `).catch(() => []);

  const metaMes = metaDia * Math.max(diasTranscurridos, 1);
  const rowsMapped = (rows || []).map(v => {
    const d = ventaMap[v.VENDEDOR_ID] || {};
    return {
      NOMBRE: v.NOMBRE,
      VENDEDOR_ID: v.VENDEDOR_ID,
      VENTA_HOY: +d.VENTA_HOY || 0,
      VENTA_MES: +d.VENTA_MES || 0,
      VENTA_YTD: +d.VENTA_YTD || 0,
      FACTURAS_HOY: +d.FACTURAS_HOY || 0,
      FACTURAS_MES: +d.FACTURAS_MES || 0,
    };
  }).sort((a, b) => b.VENTA_MES - a.VENTA_MES);

  return rowsMapped.map(r => ({
    ...r,
    META_DIA: metaDia,
    META_MES: metaMes,
    META_IDEAL: metaIdeal,
    PCT_HOY: metaDia > 0 ? Math.round(+r.VENTA_HOY / metaDia * 100) : 0,
    PCT_MES: metaMes > 0 ? Math.round(+r.VENTA_MES / metaMes * 100) : 0,
    DIAS_TRANSCURRIDOS: diasTranscurridos,
    STATUS_HOY: metaDia > 0 ? (+r.VENTA_HOY >= metaDia ? 'OK' : +r.VENTA_HOY >= metaDia * 0.7 ? 'PARCIAL' : 'BAJO') : 'SIN_META',
    STATUS_MES: metaMes > 0 ? (+r.VENTA_MES >= metaMes ? 'OK' : +r.VENTA_MES >= metaMes * 0.7 ? 'PARCIAL' : 'BAJO') : 'SIN_META',
  }));
});

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  DIRECTOR
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

// director.html espera: dir.ventas (HOY, MES_ACTUAL, FACTURAS_MES, MES_VE, MES_PV), dir.cxc, dir.cotizaciones
get('/api/director/resumen', async () => {
  const [vRow, cxcSaldos, cxcAging, coRow] = await Promise.all([
    query(`
      SELECT
        SUM(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE THEN d.IMPORTE_NETO ELSE 0 END) AS HOY,
        SUM(CASE WHEN EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE) THEN d.IMPORTE_NETO ELSE 0 END) AS MES_ACTUAL,
        COUNT(CASE WHEN EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE) THEN 1 END) AS FACTURAS_MES,
        SUM(CASE WHEN EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE) AND d.TIPO_SRC = 'VE' THEN d.IMPORTE_NETO ELSE 0 END) AS MES_VE,
        SUM(CASE WHEN EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE) AND d.TIPO_SRC = 'PV' THEN d.IMPORTE_NETO ELSE 0 END) AS MES_PV
      FROM ${ventasSub()} d
    `).catch(() => [{}]),
    query(`SELECT cs.CLIENTE_ID, cs.SALDO FROM ${cxcClienteSQL()} cs`).catch(() => []),
    query(`SELECT cd.CLIENTE_ID, SUM(cd.SALDO) AS TOTAL_C, SUM(CASE WHEN cd.DIAS_VENCIDO > 0 THEN cd.SALDO ELSE 0 END) AS VENC_C FROM ${cxcCargosSQL()} cd GROUP BY cd.CLIENTE_ID`).catch(() => []),
    query(`
      SELECT SUM(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE THEN d.IMPORTE_NETO ELSE 0 END) AS IMPORTE_COTI_HOY, SUM(CASE WHEN EXTRACT(YEAR FROM d.FECHA)=EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA)=EXTRACT(MONTH FROM CURRENT_DATE) THEN d.IMPORTE_NETO ELSE 0 END) AS IMPORTE_COTI_MES, SUM(CASE WHEN CAST(d.FECHA AS DATE) = CURRENT_DATE THEN 1 ELSE 0 END) AS COTI_HOY, SUM(CASE WHEN EXTRACT(YEAR FROM d.FECHA)=EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA)=EXTRACT(MONTH FROM CURRENT_DATE) THEN 1 ELSE 0 END) AS COTI_MES FROM DOCTOS_VE d WHERE d.TIPO_DOCTO = 'C' AND d.ESTATUS <> 'C'
    `).catch(() => [{}]),
  ]);
  const agMap = {};
  (cxcAging || []).forEach(r => { agMap[r.CLIENTE_ID] = r; });
  let saldoTotal = 0, vencido = 0, porVencer = 0;
  (cxcSaldos || []).forEach(r => {
    const saldo = +r.SALDO || 0;
    saldoTotal += saldo;
    const ag = agMap[r.CLIENTE_ID];
    if (ag && +ag.TOTAL_C > 0) {
      const pct = Math.min(+ag.VENC_C / +ag.TOTAL_C, 1);
      vencido += saldo * pct;
      porVencer += saldo * (1 - pct);
    } else porVencer += saldo;
  });
  const v = vRow[0] || {};
  const co = coRow[0] || {};
  return {
    ventas: { HOY: +(v.HOY||0), MES_ACTUAL: +(v.MES_ACTUAL||0), FACTURAS_MES: +(v.FACTURAS_MES||0), MES_VE: +(v.MES_VE||0), MES_PV: +(v.MES_PV||0) },
    cxc: { SALDO_TOTAL: Math.round(saldoTotal*100)/100, NUM_CLIENTES: (cxcSaldos||[]).length, VENCIDO: Math.round(vencido*100)/100, POR_VENCER: Math.round(porVencer*100)/100 },
    cotizaciones: { COTI_HOY: +(co.COTI_HOY||0), IMPORTE_COTI_HOY: +(co.IMPORTE_COTI_HOY||0), IMPORTE_COTI_MES: +(co.IMPORTE_COTI_MES||0), COTI_MES: +(co.COTI_MES||0) },
  };
});

get('/api/director/ventas-diarias', async (req) => {
  const dias = Math.min(parseInt(req.query.dias) || 30, 366);
  return query(`
    SELECT CAST(d.FECHA AS DATE) AS DIA,
      COALESCE(SUM(CASE WHEN d.TIPO_SRC = 'VE' THEN d.IMPORTE_NETO ELSE 0 END), 0) AS VENTAS_VE,
      COALESCE(SUM(CASE WHEN d.TIPO_SRC = 'PV' THEN d.IMPORTE_NETO ELSE 0 END), 0) AS VENTAS_PV,
      COALESCE(SUM(d.IMPORTE_NETO), 0) AS TOTAL_VENTAS
    FROM ${ventasSub()} d
    WHERE CAST(d.FECHA AS DATE) >= CURRENT_DATE - ?
    GROUP BY CAST(d.FECHA AS DATE) ORDER BY 1
  `, [dias]).catch(() => []);
});

// director.html espera CLIENTE, TOTAL_VENTAS, NUM_FACTURAS
get('/api/director/top-clientes', async (req) => {
  const limit = Math.min(parseInt(req.query.limit) || 15, 50);
  return query(`
    SELECT FIRST ${limit} d.CLIENTE_ID, COALESCE(c.NOMBRE, 'Sin nombre') AS CLIENTE,
      COALESCE(SUM(d.IMPORTE_NETO),0) AS TOTAL_VENTAS, COUNT(*) AS NUM_FACTURAS
    FROM ${ventasSub()} d
    LEFT JOIN CLIENTES c ON c.CLIENTE_ID = d.CLIENTE_ID
    WHERE d.CLIENTE_ID > 0 AND EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)
    GROUP BY d.CLIENTE_ID, c.NOMBRE ORDER BY TOTAL_VENTAS DESC
  `).catch(() => []);
});

// director.html usa r.VENDEDOR y r.VENTAS_MES
get('/api/director/vendedores', async () => {
  const rows = await query(`
    SELECT d.VENDEDOR_ID, COALESCE(v.NOMBRE, 'Vendedor ' || CAST(d.VENDEDOR_ID AS VARCHAR(10))) AS VENDEDOR,
      COUNT(*) AS FACTURAS_MES, COALESCE(SUM(d.IMPORTE_NETO),0) AS VENTAS_MES
    FROM ${ventasSub()} d
    LEFT JOIN VENDEDORES v ON v.VENDEDOR_ID = d.VENDEDOR_ID
    WHERE d.VENDEDOR_ID > 0 AND EXTRACT(YEAR FROM d.FECHA) = EXTRACT(YEAR FROM CURRENT_DATE) AND EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)
    GROUP BY d.VENDEDOR_ID, v.NOMBRE ORDER BY VENTAS_MES DESC
  `).catch(() => []);
  return rows;
});

// director.html espera FOLIO, TIPO_SRC, CLIENTE, VENDEDOR, TOTAL, FECHA
get('/api/director/recientes', async (req) => {
  const limit = Math.min(parseInt(req.query.limit) || 20, 100);
  return query(`
    SELECT FIRST ${limit} d.FOLIO, d.TIPO_SRC, COALESCE(c.NOMBRE, 'Sin cliente') AS CLIENTE, COALESCE(v.NOMBRE, 'Sin vendedor') AS VENDEDOR, d.IMPORTE_NETO AS TOTAL, d.FECHA
    FROM ${ventasSub()} d
    LEFT JOIN CLIENTES c ON c.CLIENTE_ID = d.CLIENTE_ID
    LEFT JOIN VENDEDORES v ON v.VENDEDOR_ID = d.VENDEDOR_ID
    ORDER BY d.FECHA DESC, d.FOLIO DESC
  `).catch(() => []);
});

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  CXC
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

// Resumen CxC: saldo real (ya considera cobros). Vencido = parte proporcional del saldo
// segÃºn cargos vencidos (CONDICIONES_PAGO en cxcCargosSQL para FECHA_VENCIMIENTO).
get('/api/cxc/resumen', async (req) => {
  const cf = req.query.cliente ? parseInt(req.query.cliente) : null;
  const cfSql  = cf ? ` AND cs.CLIENTE_ID = ${cf}` : '';
  const cfSql2 = cf ? ` AND cd.CLIENTE_ID = ${cf}` : '';

  const [saldosRows, agingRows] = await Promise.all([
    query(`SELECT cs.CLIENTE_ID, cs.SALDO FROM ${cxcClienteSQL()} cs WHERE 1=1 ${cfSql}`).catch(() => []),
    query(`
      SELECT cd.CLIENTE_ID,
        SUM(cd.SALDO) AS TOTAL_C,
        SUM(CASE WHEN cd.DIAS_VENCIDO > 0 THEN cd.SALDO ELSE 0 END) AS VENC_C
      FROM ${cxcCargosSQL()} cd WHERE 1=1 ${cfSql2}
      GROUP BY cd.CLIENTE_ID
    `).catch(() => []),
  ]);

  const agMap = {};
  agingRows.forEach(r => { agMap[r.CLIENTE_ID] = r; });

  let saldoTotal = 0, numClientes = 0, vencido = 0, porVencer = 0;
  saldosRows.forEach(r => {
    const saldo = +r.SALDO || 0;
    saldoTotal += saldo;
    numClientes++;
    const ag = agMap[r.CLIENTE_ID];
    if (ag && +ag.TOTAL_C > 0) {
      const pct = Math.min(+ag.VENC_C / +ag.TOTAL_C, 1);
      vencido   += saldo * pct;
      porVencer += saldo * (1 - pct);
    } else {
      porVencer += saldo;
    }
  });

  return {
    SALDO_TOTAL  : Math.round(saldoTotal * 100) / 100,
    NUM_CLIENTES : numClientes,
    VENCIDO      : Math.round(vencido   * 100) / 100,
    POR_VENCER   : Math.round(porVencer * 100) / 100,
  };
});

// Aging proporcional al saldo neto (cobros ya descontados). Buckets suman = SALDO_TOTAL.
get('/api/cxc/aging', async (req) => {
  const cf = req.query.cliente ? ` AND cd.CLIENTE_ID = ${parseInt(req.query.cliente)}` : '';
  const [cargos, saldos] = await Promise.all([
    query(`
      SELECT cd.CLIENTE_ID,
        SUM(cd.SALDO) AS TOTAL_CARGO,
        SUM(CASE WHEN cd.DIAS_VENCIDO <= 0               THEN cd.SALDO ELSE 0 END) AS CORRIENTE,
        SUM(CASE WHEN cd.DIAS_VENCIDO BETWEEN  1 AND  30 THEN cd.SALDO ELSE 0 END) AS DIAS_1_30,
        SUM(CASE WHEN cd.DIAS_VENCIDO BETWEEN 31 AND  60 THEN cd.SALDO ELSE 0 END) AS DIAS_31_60,
        SUM(CASE WHEN cd.DIAS_VENCIDO BETWEEN 61 AND  90 THEN cd.SALDO ELSE 0 END) AS DIAS_61_90,
        SUM(CASE WHEN cd.DIAS_VENCIDO > 90               THEN cd.SALDO ELSE 0 END) AS DIAS_MAS_90
      FROM ${cxcCargosSQL()} cd WHERE 1=1 ${cf}
      GROUP BY cd.CLIENTE_ID
    `).catch(() => []),
    query(`SELECT cs.CLIENTE_ID, cs.SALDO FROM ${cxcClienteSQL()} cs`).catch(() => []),
  ]);
  const saldoMap = {};
  saldos.forEach(s => { saldoMap[s.CLIENTE_ID] = +s.SALDO || 0; });
  const res = { CORRIENTE: 0, DIAS_1_30: 0, DIAS_31_60: 0, DIAS_61_90: 0, DIAS_MAS_90: 0 };
  cargos.forEach(c => {
    const netSaldo = saldoMap[c.CLIENTE_ID] || 0;
    const totalCargo = +c.TOTAL_CARGO || 0;
    if (totalCargo <= 0) return;
    const ratio = Math.min(netSaldo / totalCargo, 1);
    res.CORRIENTE   += (+c.CORRIENTE   || 0) * ratio;
    res.DIAS_1_30   += (+c.DIAS_1_30   || 0) * ratio;
    res.DIAS_31_60  += (+c.DIAS_31_60  || 0) * ratio;
    res.DIAS_61_90  += (+c.DIAS_61_90  || 0) * ratio;
    res.DIAS_MAS_90 += (+c.DIAS_MAS_90 || 0) * ratio;
  });
  return {
    CORRIENTE  : Math.round(res.CORRIENTE   * 100) / 100,
    DIAS_1_30  : Math.round(res.DIAS_1_30   * 100) / 100,
    DIAS_31_60 : Math.round(res.DIAS_31_60  * 100) / 100,
    DIAS_61_90 : Math.round(res.DIAS_61_90  * 100) / 100,
    DIAS_MAS_90: Math.round(res.DIAS_MAS_90 * 100) / 100,
  };
});

get('/api/cxc/vencidas', async (req) => {
  const limit = Math.min(parseInt(req.query.limit) || 100, 500);
  const cf = req.query.cliente ? ` AND cd.CLIENTE_ID = ${parseInt(req.query.cliente)}` : '';
  return query(`
    SELECT FIRST ${limit}
      cd.FOLIO,
      c.NOMBRE AS CLIENTE,
      COALESCE(cp.NOMBRE, 'S/D') AS CONDICION_PAGO,
      cd.SALDO,
      cd.DIAS_VENCIDO AS ATRASO,
      cd.DIAS_VENCIDO AS DIAS_ATRASO,
      cd.FECHA_VENCIMIENTO
    FROM ${cxcCargosSQL()} cd
    JOIN CLIENTES c ON c.CLIENTE_ID = cd.CLIENTE_ID
    LEFT JOIN DOCTOS_CC dc ON dc.DOCTO_CC_ID = cd.DOCTO_CC_ID
    LEFT JOIN CONDICIONES_PAGO cp ON cp.COND_PAGO_ID = dc.COND_PAGO_ID
    WHERE cd.DIAS_VENCIDO > 0 ${cf}
    ORDER BY cd.DIAS_VENCIDO DESC, cd.SALDO DESC
  `).catch(() => []);
});

// Top Deudores: saldo neto + condiciÃ³n de pago + vencido/atraso/docs (front: CLIENTE, CONDICION_PAGO, SALDO_TOTAL, VENCIDO, MAX_DIAS_ATRASO, NUM_DOCUMENTOS)
get('/api/cxc/top-deudores', async (req) => {
  const limit = Math.min(parseInt(req.query.limit) || 20, 100);
  const [saldos, aging] = await Promise.all([
    query(`
      SELECT s.CLIENTE_ID, s.SALDO
      FROM ${cxcClienteSQL()} s
    `).catch(() => []),
    query(`
      SELECT cd.CLIENTE_ID,
        SUM(CASE WHEN cd.DIAS_VENCIDO > 0 THEN cd.SALDO ELSE 0 END) AS VENCIDO,
        MAX(CASE WHEN cd.DIAS_VENCIDO > 0 THEN cd.DIAS_VENCIDO ELSE 0 END) AS MAX_DIAS,
        COUNT(*) AS NUM_DOCS
      FROM ${cxcCargosSQL()} cd
      GROUP BY cd.CLIENTE_ID
    `).catch(() => []),
  ]);
  const agingMap = {};
  aging.forEach(a => { agingMap[a.CLIENTE_ID] = a; });
  const clienteIds = saldos.map(s => s.CLIENTE_ID).filter(Boolean);
  if (!clienteIds.length) return [];
  const clientes = await query(`
    SELECT cl.CLIENTE_ID, cl.NOMBRE AS CLIENTE, COALESCE(cp.NOMBRE, 'S/D') AS CONDICION_PAGO
    FROM CLIENTES cl
    LEFT JOIN CONDICIONES_PAGO cp ON cp.COND_PAGO_ID = cl.COND_PAGO_ID
    WHERE cl.CLIENTE_ID IN (${clienteIds.join(',')})
  `).catch(() => []);
  const clMap = {};
  clientes.forEach(c => { clMap[c.CLIENTE_ID] = c; });
  const result = saldos
    .map(s => {
      const cl = clMap[s.CLIENTE_ID] || {};
      const ag = agingMap[s.CLIENTE_ID] || {};
      return {
        CLIENTE_ID    : s.CLIENTE_ID,
        CLIENTE       : cl.CLIENTE || ('Cliente ' + s.CLIENTE_ID),
        CONDICION_PAGO: cl.CONDICION_PAGO || 'S/D',
        SALDO_TOTAL   : +s.SALDO || 0,
        VENCIDO       : Math.min(+ag.VENCIDO || 0, Math.max(0, +s.SALDO || 0)),
        MAX_DIAS_ATRASO: +ag.MAX_DIAS || 0,
        NUM_DOCUMENTOS : +ag.NUM_DOCS || 0,
      };
    })
    .sort((a, b) => b.SALDO_TOTAL - a.SALDO_TOTAL)
    .slice(0, limit);
  return result;
});

get('/api/cxc/historial', async (req) => {
  const cliente = req.query.cliente ? parseInt(req.query.cliente) : null;
  if (!cliente) return [];
  return query(`
    SELECT cd.CLIENTE_ID, cd.FOLIO, cd.FECHA_VENCIMIENTO, cd.DIAS_VENCIDO, cd.SALDO
    FROM ${cxcCargosSQL()} cd WHERE cd.CLIENTE_ID = ?
    ORDER BY cd.FECHA_VENCIMIENTO
  `, [cliente]).catch(() => []);
});

// Por CondiciÃ³n: front espera CONDICION_PAGO, NUM_CLIENTES, NUM_DOCUMENTOS, DIAS_CREDITO, SALDO_TOTAL, VENCIDO, CORRIENTE
get('/api/cxc/por-condicion', async () => {
  return query(`
    SELECT
      COALESCE(cp.NOMBRE, 'Sin condiciÃ³n')    AS CONDICION_PAGO,
      COALESCE(cp.DIAS_PPAG, 0)              AS DIAS_CREDITO,
      COUNT(DISTINCT cd.CLIENTE_ID)          AS NUM_CLIENTES,
      COUNT(DISTINCT cd.DOCTO_CC_ID)         AS NUM_DOCUMENTOS,
      SUM(cd.SALDO)                          AS SALDO_TOTAL,
      SUM(CASE WHEN cd.DIAS_VENCIDO > 0 THEN cd.SALDO ELSE 0 END)   AS VENCIDO,
      SUM(CASE WHEN cd.DIAS_VENCIDO <= 0 THEN cd.SALDO ELSE 0 END)  AS CORRIENTE
    FROM ${cxcCargosSQL()} cd
    JOIN CLIENTES c ON c.CLIENTE_ID = cd.CLIENTE_ID
    LEFT JOIN CONDICIONES_PAGO cp ON cp.COND_PAGO_ID = c.COND_PAGO_ID
    GROUP BY cp.NOMBRE, cp.DIAS_PPAG
    ORDER BY SALDO_TOTAL DESC
  `).catch(() => []);
});

// Calendario Pagos / Buro: por documento, con CLIENTE, ANIO, MES_EMISION, saldo restante, fechas. Sin ?cliente= devuelve todos.
// Si ?saldos_actuales=1 devuelve { rows, saldosPorCliente } para que el front muestre deuda actual sin depender del filtro meses.
get('/api/cxc/historial-pagos', async (req) => {
  const limit = Math.min(parseInt(req.query.limit) || 300, 500);
  const meses = Math.min(parseInt(req.query.meses) || 12, 24);
  const saldosActuales = req.query.saldos_actuales === '1' || req.query.saldos_actuales === 'true';
  const clienteFiltro = req.query.cliente ? ` AND cl.CLIENTE_ID = ${parseInt(req.query.cliente)}` : '';
  const rows = await query(`
    SELECT FIRST ${limit}
      dc.DOCTO_CC_ID,
      dc.FOLIO,
      cl.NOMBRE                                                         AS CLIENTE,
      cl.CLIENTE_ID,
      COALESCE(cp.NOMBRE, 'S/D')                                        AS CONDICION_PAGO,
      CAST(dc.FECHA AS DATE)                                            AS FECHA_EMISION,
      CAST(COALESCE(MIN(vc.FECHA_VENCIMIENTO), CAST(dc.FECHA AS DATE) + CAST(COALESCE(NULLIF(cp.DIAS_PPAG, 0), 30) AS INTEGER)) AS DATE) AS FECHA_VENCIMIENTO,
      SUM(CASE WHEN i.TIPO_IMPTE = 'C' THEN i.IMPORTE ELSE 0 END)       AS CARGO_ORIGINAL,
      SUM(CASE WHEN i.TIPO_IMPTE = 'R' THEN i.IMPORTE ELSE 0 END)       AS TOTAL_COBRADO,
      SUM(CASE WHEN i.TIPO_IMPTE = 'C' THEN i.IMPORTE WHEN i.TIPO_IMPTE = 'R' THEN -i.IMPORTE ELSE 0 END) AS SALDO_RESTANTE,
      MAX(CASE WHEN i.TIPO_IMPTE = 'R' THEN CAST(i.FECHA AS DATE) END)   AS FECHA_ULTIMO_PAGO,
      EXTRACT(YEAR FROM dc.FECHA)                                       AS ANIO,
      EXTRACT(MONTH FROM dc.FECHA)                                      AS MES_EMISION
    FROM IMPORTES_DOCTOS_CC i
    JOIN DOCTOS_CC dc ON dc.DOCTO_CC_ID = i.DOCTO_CC_ID
    JOIN CLIENTES cl ON cl.CLIENTE_ID = dc.CLIENTE_ID
    LEFT JOIN CONDICIONES_PAGO cp ON cp.COND_PAGO_ID = dc.COND_PAGO_ID
    LEFT JOIN VENCIMIENTOS_CARGOS_CC vc ON vc.DOCTO_CC_ID = i.DOCTO_CC_ID
    WHERE COALESCE(i.CANCELADO, 'N') = 'N'
      AND dc.FECHA >= (CURRENT_DATE - ${meses * 31})
      ${clienteFiltro}
    GROUP BY dc.DOCTO_CC_ID, dc.FOLIO, cl.NOMBRE, cl.CLIENTE_ID, cp.NOMBRE, cp.DIAS_PPAG, dc.FECHA
    HAVING SUM(CASE WHEN i.TIPO_IMPTE = 'C' THEN i.IMPORTE ELSE 0 END) > 0
    ORDER BY dc.FECHA DESC
  `).catch(() => []);
  if (!saldosActuales || !rows || !rows.length) return rows || [];
  const ids = [...new Set((rows || []).map(r => r.CLIENTE_ID).filter(Boolean))];
  if (!ids.length) return { rows, saldosPorCliente: {} };
  let saldosPorCliente = {};
  try {
    const saldosRows = await query(`SELECT cs.CLIENTE_ID, cs.SALDO FROM ${cxcClienteSQL()} cs WHERE cs.CLIENTE_ID IN (${ids.join(',')})`).catch(() => []);
    (saldosRows || []).forEach(r => { saldosPorCliente[r.CLIENTE_ID] = +r.SALDO || 0; });
  } catch (_) { /* si falla saldos, igual devolver rows */ }
  return { rows, saldosPorCliente };
});

get('/api/cxc/comportamiento-pago', async (req) => {
  const cliente = req.query.cliente ? parseInt(req.query.cliente) : null;
  if (!cliente) return [];
  return query(`
    SELECT cd.CLIENTE_ID, AVG(cd.DIAS_VENCIDO) AS PROMEDIO_DIAS_VENCIDO, COUNT(*) AS DOCS_VENCIDOS
    FROM ${cxcCargosSQL()} cd WHERE cd.CLIENTE_ID = ? AND cd.DIAS_VENCIDO > 0
    GROUP BY cd.CLIENTE_ID
  `, [cliente]).catch(() => []);
});

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  INVENTARIO â€” Microsip: SALDOS_IN, NIVELES_ARTICULOS, PRECIOS_ARTICULOS
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
const SQL_EXIST_SUB = `( SELECT ARTICULO_ID, SUM(ENTRADAS_UNIDADES - SALIDAS_UNIDADES) AS EXISTENCIA FROM SALDOS_IN GROUP BY ARTICULO_ID )`;
const SQL_MINIMO_SUB = `( SELECT ARTICULO_ID, MAX(INVENTARIO_MINIMO) AS INVENTARIO_MINIMO FROM NIVELES_ARTICULOS WHERE INVENTARIO_MINIMO > 0 GROUP BY ARTICULO_ID )`;
const SQL_PRECIO_SUB = `( SELECT ARTICULO_ID, MIN(PRECIO) AS PRECIO1 FROM PRECIOS_ARTICULOS WHERE MONEDA_ID = 1 AND PRECIO > 0 GROUP BY ARTICULO_ID )`;

// SIN_STOCK = solo articulos con minimo definido y existencia 0 (alerta real). No contar todo el catalogo en cero.
get('/api/inv/resumen', async () => {
  const rows = await query(`
    SELECT
      COUNT(DISTINCT a.ARTICULO_ID) AS TOTAL_ARTICULOS,
      SUM(CASE WHEN COALESCE(s.EXISTENCIA, 0) < COALESCE(n.INVENTARIO_MINIMO, 0) AND COALESCE(n.INVENTARIO_MINIMO, 0) > 0 THEN 1 ELSE 0 END) AS BAJO_MINIMO,
      SUM(COALESCE(s.EXISTENCIA, 0) * COALESCE(pr.PRECIO1, 0)) AS VALOR_INVENTARIO,
      SUM(CASE WHEN COALESCE(n.INVENTARIO_MINIMO, 0) > 0 AND COALESCE(s.EXISTENCIA, 0) <= 0 THEN 1 ELSE 0 END) AS SIN_STOCK
    FROM ARTICULOS a
    LEFT JOIN ${SQL_EXIST_SUB} s ON s.ARTICULO_ID = a.ARTICULO_ID
    LEFT JOIN ${SQL_MINIMO_SUB} n ON n.ARTICULO_ID = a.ARTICULO_ID
    LEFT JOIN ${SQL_PRECIO_SUB} pr ON pr.ARTICULO_ID = a.ARTICULO_ID
    WHERE COALESCE(a.ESTATUS, 'A') = 'A'
  `).catch(() => [{}]);
  const r = rows[0] || {};
  return { TOTAL_ARTICULOS: +(r.TOTAL_ARTICULOS||0), VALOR_INVENTARIO: +(r.VALOR_INVENTARIO||0), BAJO_MINIMO: +(r.BAJO_MINIMO||0), SIN_STOCK: +(r.SIN_STOCK||0) };
});

get('/api/inv/bajo-minimo', async (req) => {
  const limit = Math.min(parseInt(req.query.limit) || 50, 200);
  const q = (req.query.q || '').trim().toUpperCase();
  const extra = q.length >= 2 ? ` AND UPPER(a.NOMBRE) LIKE '%${q.replace(/'/g, "''")}%'` : '';
  return query(`
    SELECT FIRST ${limit} a.ARTICULO_ID, a.NOMBRE AS DESCRIPCION, COALESCE(a.UNIDAD_VENTA, 'PZA') AS UNIDAD,
      COALESCE(s.EXISTENCIA, 0) AS EXISTENCIA, n.INVENTARIO_MINIMO AS EXISTENCIA_MINIMA,
      (n.INVENTARIO_MINIMO - COALESCE(s.EXISTENCIA, 0)) AS FALTANTE
    FROM ARTICULOS a
    JOIN ${SQL_MINIMO_SUB} n ON n.ARTICULO_ID = a.ARTICULO_ID
    LEFT JOIN ${SQL_EXIST_SUB} s ON s.ARTICULO_ID = a.ARTICULO_ID
    WHERE COALESCE(a.ESTATUS, 'A') = 'A' AND COALESCE(s.EXISTENCIA, 0) < n.INVENTARIO_MINIMO ${extra}
    ORDER BY FALTANTE DESC
  `).catch(() => []);
});

get('/api/inv/existencias', async (req) => {
  const q = (req.query.q || '').trim();
  if (q.length < 2) return [];
  const like = `%${q.toUpperCase().replace(/'/g, "''")}%`;
  return query(`
    SELECT FIRST 50 a.ARTICULO_ID, a.NOMBRE AS DESCRIPCION, COALESCE(a.UNIDAD_VENTA, 'PZA') AS UNIDAD,
      COALESCE(s.EXISTENCIA, 0) AS EXISTENCIA, COALESCE(n.INVENTARIO_MINIMO, 0) AS EXISTENCIA_MINIMA, COALESCE(pr.PRECIO1, 0) AS PRECIO_VENTA
    FROM ARTICULOS a
    LEFT JOIN ${SQL_EXIST_SUB} s ON s.ARTICULO_ID = a.ARTICULO_ID
    LEFT JOIN ${SQL_MINIMO_SUB} n ON n.ARTICULO_ID = a.ARTICULO_ID
    LEFT JOIN ${SQL_PRECIO_SUB} pr ON pr.ARTICULO_ID = a.ARTICULO_ID
    WHERE COALESCE(a.ESTATUS, 'A') = 'A' AND (UPPER(a.NOMBRE) LIKE ? OR UPPER(CAST(a.ARTICULO_ID AS VARCHAR(50))) LIKE ?)
    ORDER BY a.NOMBRE
  `, [like, like]).catch(() => []);
});

get('/api/inv/top-stock', async (req) => {
  const limit = Math.min(parseInt(req.query.limit) || 30, 100);
  return query(`
    SELECT FIRST ${limit} a.ARTICULO_ID, a.NOMBRE AS DESCRIPCION, COALESCE(a.UNIDAD_VENTA, 'PZA') AS UNIDAD,
      COALESCE(s.EXISTENCIA, 0) AS EXISTENCIA, COALESCE(s.EXISTENCIA, 0) * COALESCE(pr.PRECIO1, 0) AS VALOR_TOTAL, COALESCE(pr.PRECIO1, 0) AS PRECIO_VENTA
    FROM ARTICULOS a
    LEFT JOIN ${SQL_EXIST_SUB} s ON s.ARTICULO_ID = a.ARTICULO_ID
    LEFT JOIN ${SQL_PRECIO_SUB} pr ON pr.ARTICULO_ID = a.ARTICULO_ID
    WHERE COALESCE(a.ESTATUS, 'A') = 'A' AND COALESCE(s.EXISTENCIA, 0) > 0
    ORDER BY VALOR_TOTAL DESC
  `).catch(() => []);
});

// Consumo semanal desde ventas (DOCTOS_VE_DET) â€” inventario.html espera DESCRIPCION, EXISTENCIA, CONSUMO_SEMANAL_PROM, SEMANAS_STOCK
get('/api/inv/consumo-semanal', async (req) => {
  const limit = Math.min(parseInt(req.query.limit) || 30, 100);
  return query(`
    SELECT FIRST ${limit} a.NOMBRE AS DESCRIPCION, a.ARTICULO_ID, COALESCE(s.EXISTENCIA, 0) AS EXISTENCIA,
      SUM(det.UNIDADES) / 4.0 AS CONSUMO_SEMANAL_PROM,
      CASE WHEN SUM(det.UNIDADES) > 0 THEN COALESCE(s.EXISTENCIA, 0) / (SUM(det.UNIDADES) / 4.0) ELSE 9999 END AS SEMANAS_STOCK
    FROM DOCTOS_VE d
    JOIN DOCTOS_VE_DET det ON det.DOCTO_VE_ID = d.DOCTO_VE_ID
    JOIN ARTICULOS a ON a.ARTICULO_ID = det.ARTICULO_ID
    LEFT JOIN ${SQL_EXIST_SUB} s ON s.ARTICULO_ID = a.ARTICULO_ID
    WHERE (d.TIPO_DOCTO = 'F' AND d.ESTATUS <> 'C') OR (d.TIPO_DOCTO = 'V' AND d.ESTATUS NOT IN ('C','T'))
      AND d.FECHA >= (CURRENT_DATE - 28)
    GROUP BY a.NOMBRE, a.ARTICULO_ID, s.EXISTENCIA
    HAVING SUM(det.UNIDADES) > 0
    ORDER BY SEMANAS_STOCK ASC
  `).catch(() => []);
});

// Forecast consumo â€” inventario.html espera DESCRIPCION, UNIDAD, EXISTENCIA_ACTUAL, CONSUMO_DIARIO, DIAS_STOCK, STOCK_MINIMO_RECOMENDADO, ALERTA, CANTIDAD_REPONER
get('/api/inv/consumo', async (req) => {
  const dias = Math.min(parseInt(req.query.dias) || 90, 365);
  const limit = Math.min(parseInt(req.query.limit) || 50, 200);
  const lead = Math.min(parseInt(req.query.lead) || 15, 60);
  const rows = await query(`
    SELECT FIRST ${limit} a.NOMBRE AS DESCRIPCION, a.ARTICULO_ID, COALESCE(a.UNIDAD_VENTA, 'PZA') AS UNIDAD,
      COALESCE(ex.EXISTENCIA, 0) AS EXISTENCIA_ACTUAL, COALESCE(mn.INVENTARIO_MINIMO, 0) AS MIN_ACTUAL,
      SUM(det.UNIDADES) AS CONSUMO_PERIODO, SUM(det.UNIDADES) / ${dias}.0 AS CONSUMO_DIARIO,
      CASE WHEN SUM(det.UNIDADES) > 0 THEN CAST(COALESCE(ex.EXISTENCIA, 0) / (SUM(det.UNIDADES) / ${dias}.0) AS INTEGER) ELSE 9999 END AS DIAS_STOCK,
      CASE WHEN SUM(det.UNIDADES) > 0 THEN CAST(SUM(det.UNIDADES) / ${dias}.0 * ${lead} + 0.9999 AS INTEGER) ELSE 0 END AS STOCK_MINIMO_RECOMENDADO
    FROM DOCTOS_VE d
    JOIN DOCTOS_VE_DET det ON det.DOCTO_VE_ID = d.DOCTO_VE_ID
    JOIN ARTICULOS a ON a.ARTICULO_ID = det.ARTICULO_ID
    LEFT JOIN ${SQL_EXIST_SUB} ex ON ex.ARTICULO_ID = a.ARTICULO_ID
    LEFT JOIN ${SQL_MINIMO_SUB} mn ON mn.ARTICULO_ID = a.ARTICULO_ID
    WHERE (d.TIPO_DOCTO = 'F' AND d.ESTATUS <> 'C') AND d.FECHA >= (CURRENT_DATE - ${dias}) AND COALESCE(a.ESTATUS, 'A') = 'A'
    GROUP BY a.NOMBRE, a.ARTICULO_ID, a.UNIDAD_VENTA, ex.EXISTENCIA, mn.INVENTARIO_MINIMO
    HAVING SUM(det.UNIDADES) > 0
    ORDER BY DIAS_STOCK ASC
  `).catch(() => []);
  return rows.map(r => ({
    ...r,
    ALERTA: +r.DIAS_STOCK < lead ? 'CRITICO' : +r.DIAS_STOCK < lead * 2 ? 'BAJO' : +r.EXISTENCIA_ACTUAL <= +r.MIN_ACTUAL ? 'BAJO_MINIMO' : 'OK',
    NECESITA_REPONER: +r.EXISTENCIA_ACTUAL < +r.STOCK_MINIMO_RECOMENDADO,
    CANTIDAD_REPONER: Math.max(0, +r.STOCK_MINIMO_RECOMENDADO - +r.EXISTENCIA_ACTUAL),
  }));
});

get('/api/inv/sin-movimiento', async (req) => {
  const dias = Math.min(parseInt(req.query.dias) || 180, 730);
  const limit = Math.min(parseInt(req.query.limit) || 50, 200);
  return query(`
    SELECT FIRST ${limit} a.NOMBRE AS DESCRIPCION, a.ARTICULO_ID, COALESCE(a.UNIDAD_VENTA, 'PZA') AS UNIDAD,
      COALESCE(ex.EXISTENCIA, 0) AS EXISTENCIA_ACTUAL, COALESCE(mn.INVENTARIO_MINIMO, 0) AS MIN_ACTUAL,
      MAX(CAST(d.FECHA AS DATE)) AS ULTIMO_MOVIMIENTO, (CURRENT_DATE - MAX(CAST(d.FECHA AS DATE))) AS DIAS_SIN_VENTA
    FROM ARTICULOS a
    LEFT JOIN ${SQL_EXIST_SUB} ex ON ex.ARTICULO_ID = a.ARTICULO_ID
    LEFT JOIN ${SQL_MINIMO_SUB} mn ON mn.ARTICULO_ID = a.ARTICULO_ID
    LEFT JOIN DOCTOS_VE_DET det ON det.ARTICULO_ID = a.ARTICULO_ID
    LEFT JOIN DOCTOS_VE d ON d.DOCTO_VE_ID = det.DOCTO_VE_ID AND (d.TIPO_DOCTO = 'F' AND d.ESTATUS <> 'C')
    WHERE COALESCE(a.ESTATUS, 'A') = 'A' AND COALESCE(ex.EXISTENCIA, 0) > 0
    GROUP BY a.NOMBRE, a.ARTICULO_ID, a.UNIDAD_VENTA, ex.EXISTENCIA, mn.INVENTARIO_MINIMO
    HAVING (MAX(CAST(d.FECHA AS DATE)) IS NULL) OR ((CURRENT_DATE - MAX(CAST(d.FECHA AS DATE))) > ${dias})
    ORDER BY 7 DESC NULLS FIRST, ex.EXISTENCIA DESC
  `).catch(() => []);
});

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  CLIENTES (riesgo, inactivos, resumen-riesgo)
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

get('/api/clientes/riesgo', async (req) => {
  const limit = Math.min(parseInt(req.query.limit) || 100, 500);
  return query(`
    SELECT cd.CLIENTE_ID, c.NOMBRE, SUM(cd.SALDO) AS SALDO, MAX(cd.DIAS_VENCIDO) AS MAX_DIAS_VENCIDO
    FROM ${cxcCargosSQL()} cd
    LEFT JOIN CLIENTES c ON c.CLIENTE_ID = cd.CLIENTE_ID
    WHERE cd.DIAS_VENCIDO > 0
    GROUP BY cd.CLIENTE_ID, c.NOMBRE ORDER BY MAX_DIAS_VENCIDO DESC
  `).catch(() => []);
});

get('/api/clientes/inactivos', async (req) => {
  const meses = Math.min(parseInt(req.query.meses) || 12, 24);
  return query(`
    SELECT c.CLIENTE_ID, c.NOMBRE
    FROM CLIENTES c
    WHERE NOT EXISTS (
      SELECT 1 FROM DOCTOS_VE d
      WHERE d.CLIENTE_ID = c.CLIENTE_ID AND d.FECHA >= (CURRENT_DATE - ?)
    )
  `, [meses * 31]).catch(() => []);
});

get('/api/clientes/resumen-riesgo', async () => {
  const defaultRes = { TOTAL_EN_RIESGO: 0, MONTO_CRITICO: 0, MONTO_ALTO: 0, MONTO_MEDIO: 0, MONTO_LEVE: 0 };
  try {
    const [totales] = await query(`
      SELECT COUNT(DISTINCT cd.CLIENTE_ID) AS TOTAL_EN_RIESGO,
        SUM(CASE WHEN cd.DIAS_VENCIDO > 90 THEN cd.SALDO ELSE 0 END) AS MONTO_CRITICO,
        SUM(CASE WHEN cd.DIAS_VENCIDO > 60 AND cd.DIAS_VENCIDO <= 90 THEN cd.SALDO ELSE 0 END) AS MONTO_ALTO,
        SUM(CASE WHEN cd.DIAS_VENCIDO > 30 AND cd.DIAS_VENCIDO <= 60 THEN cd.SALDO ELSE 0 END) AS MONTO_MEDIO,
        SUM(CASE WHEN cd.DIAS_VENCIDO <= 30 THEN cd.SALDO ELSE 0 END) AS MONTO_LEVE
      FROM ${cxcCargosSQL()} cd WHERE cd.DIAS_VENCIDO > 0
    `).catch(() => [null]);
    return { ...defaultRes, ...(totales || {}) };
  } catch (e) {
    return defaultRes;
  }
});

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  RESULTADOS (P&L) â€” resultados.html espera meses[], totales{}, tiene_costo
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

get('/api/resultados/pnl', async (req) => {
  const mesesN = Math.min(Math.max(parseInt(req.query.meses) || 3, 1), 24);
  const desde = new Date();
  desde.setMonth(desde.getMonth() - mesesN);
  const desdeStr = desde.getFullYear() + '-' + String(desde.getMonth() + 1).padStart(2, '0') + '-01';

  const [ventasMes, costosVEMes, costosINMes, costosINDirect, cobrosMes] = await Promise.all([
    query(`
      SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(MONTH FROM d.FECHA) AS MES,
        COALESCE(SUM(d.IMPORTE_NETO), 0) AS VENTAS_NETAS,
        COALESCE(SUM(CASE WHEN d.TIPO_SRC = 'VE' THEN d.IMPORTE_NETO ELSE 0 END), 0) AS VENTAS_VE,
        COALESCE(SUM(CASE WHEN d.TIPO_SRC = 'PV' THEN d.IMPORTE_NETO ELSE 0 END), 0) AS VENTAS_PV,
        COUNT(*) AS NUM_FACTURAS
      FROM ${ventasSub()} d
      WHERE CAST(d.FECHA AS DATE) >= CAST(? AS DATE)
      GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(MONTH FROM d.FECHA) ORDER BY 1, 2
    `, [desdeStr]).catch(() => []),
    query(`
      SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(MONTH FROM d.FECHA) AS MES,
        COALESCE(SUM(det.COSTO_TOTAL), 0) AS COSTO_VENTAS
      FROM DOCTOS_VE d
      JOIN DOCTOS_VE_DET det ON det.DOCTO_VE_ID = d.DOCTO_VE_ID
      WHERE (d.TIPO_DOCTO = 'F' OR d.TIPO_DOCTO = 'V') AND d.ESTATUS <> 'C'
        AND CAST(d.FECHA AS DATE) >= CAST(? AS DATE)
      GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(MONTH FROM d.FECHA) ORDER BY 1, 2
    `, [desdeStr]).catch(() => []),
    query(`
      SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(MONTH FROM d.FECHA) AS MES,
        COALESCE(SUM(det.CANTIDAD * COALESCE(det.COSTO_UNITARIO, det.PRECIO_UNITARIO, 0)), 0) AS COSTO_VENTAS
      FROM DOCTOS_IN d
      JOIN DOCTOS_IN_DET det ON det.DOCTO_IN_ID = d.DOCTO_IN_ID
      WHERE d.TIPO_MOV = 'S' AND CAST(d.FECHA AS DATE) >= CAST(? AS DATE)
      GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(MONTH FROM d.FECHA) ORDER BY 1, 2
    `, [desdeStr]).catch(() => []),
    query(`
      SELECT EXTRACT(YEAR FROM d.FECHA) AS ANIO, EXTRACT(MONTH FROM d.FECHA) AS MES,
        COALESCE(SUM(COALESCE(d.IMPORTE, d.PRECIO_UNITARIO * d.UNIDADES, 0)), 0) AS COSTO_VENTAS
      FROM DOCTOS_IN d
      WHERE d.TIPO_DOCTO STARTING WITH 'S' AND CAST(d.FECHA AS DATE) >= CAST(? AS DATE) AND COALESCE(d.UNIDADES, 0) > 0
      GROUP BY EXTRACT(YEAR FROM d.FECHA), EXTRACT(MONTH FROM d.FECHA) ORDER BY 1, 2
    `, [desdeStr]).catch(() => []),
    query(`
      SELECT EXTRACT(YEAR FROM dc.FECHA) AS ANIO, EXTRACT(MONTH FROM dc.FECHA) AS MES,
        SUM(CASE WHEN COALESCE(i.IMPUESTO, 0) > 0 THEN i.IMPORTE ELSE i.IMPORTE / 1.16 END) AS COBROS
      FROM IMPORTES_DOCTOS_CC i
      JOIN DOCTOS_CC dc ON dc.DOCTO_CC_ID = i.DOCTO_CC_ID
      WHERE i.TIPO_IMPTE = 'R' AND COALESCE(i.CANCELADO, 'N') = 'N'
        AND dc.FECHA >= CAST(? AS DATE)
      GROUP BY EXTRACT(YEAR FROM dc.FECHA), EXTRACT(MONTH FROM dc.FECHA) ORDER BY 1, 2
    `, [desdeStr]).catch(() => []),
  ]);

  const key = (a, m) => `${a}-${m}`;
  const costMap = {};
  (costosVEMes || []).forEach(r => { costMap[key(r.ANIO, r.MES)] = (costMap[key(r.ANIO, r.MES)] || 0) + (+r.COSTO_VENTAS || 0); });
  (costosINMes || []).forEach(r => { costMap[key(r.ANIO, r.MES)] = (costMap[key(r.ANIO, r.MES)] || 0) + (+r.COSTO_VENTAS || 0); });
  (costosINDirect || []).forEach(r => { costMap[key(r.ANIO, r.MES)] = (costMap[key(r.ANIO, r.MES)] || 0) + (+r.COSTO_VENTAS || 0); });
  const cobMap = {}; (cobrosMes || []).forEach(r => { cobMap[key(r.ANIO, r.MES)] = +r.COBROS || 0; });

  const meses = (ventasMes || []).map(r => {
    const ventas = +r.VENTAS_NETAS || 0;
    const costo = costMap[key(r.ANIO, r.MES)] || 0;
    const cobros = cobMap[key(r.ANIO, r.MES)] || 0;
    const util = ventas - costo;
    const margenPct = ventas > 0 ? Math.round((util / ventas) * 1000) / 10 : 0;
    return {
      ANIO: r.ANIO,
      MES: r.MES,
      VENTAS_NETAS: ventas,
      VENTAS_VE: +r.VENTAS_VE || 0,
      VENTAS_PV: +r.VENTAS_PV || 0,
      COSTO_VENTAS: costo,
      UTILIDAD_BRUTA: util,
      MARGEN_BRUTO_PCT: margenPct,
      COBROS: cobros,
      NUM_FACTURAS: +r.NUM_FACTURAS || 0,
    };
  });

  const totales = meses.reduce((acc, m) => {
    acc.VENTAS_NETAS += m.VENTAS_NETAS;
    acc.VENTAS_VE += m.VENTAS_VE;
    acc.VENTAS_PV += m.VENTAS_PV;
    acc.COSTO_VENTAS += m.COSTO_VENTAS;
    acc.UTILIDAD_BRUTA += m.UTILIDAD_BRUTA;
    acc.COBROS += m.COBROS;
    acc.NUM_FACTURAS += m.NUM_FACTURAS;
    return acc;
  }, { VENTAS_NETAS: 0, VENTAS_VE: 0, VENTAS_PV: 0, COSTO_VENTAS: 0, UTILIDAD_BRUTA: 0, COBROS: 0, NUM_FACTURAS: 0 });
  totales.MARGEN_BRUTO_PCT = totales.VENTAS_NETAS > 0
    ? Math.round((totales.UTILIDAD_BRUTA / totales.VENTAS_NETAS) * 1000) / 10 : 0;

  const tiene_costo = totales.COSTO_VENTAS > 0;

  return { meses, totales, tiene_costo };
});

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  DEBUG
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

get('/api/debug/cxc', async () => {
  const [docs, importes, clientes] = await Promise.all([
    query(`SELECT COUNT(*) AS N FROM DOCTOS_CC`).catch(() => [{ N: 0 }]),
    query(`SELECT COUNT(*) AS N FROM IMPORTES_DOCTOS_CC`).catch(() => [{ N: 0 }]),
    query(`SELECT COUNT(*) AS N FROM CLIENTES`).catch(() => [{ N: 0 }]),
  ]);
  return { doctos_cc: docs[0].N, importes_cc: importes[0].N, clientes: clientes[0].N };
});

get('/api/debug/ventas', async () => {
  const [ve, pv] = await Promise.all([
    query(`SELECT COUNT(*) AS N FROM DOCTOS_VE WHERE (TIPO_DOCTO = 'F' OR TIPO_DOCTO = 'V') AND ESTATUS <> 'C'`).catch(() => [{ N: 0 }]),
    query(`SELECT COUNT(*) AS N FROM DOCTOS_PV WHERE (TIPO_DOCTO = 'F' OR TIPO_DOCTO = 'V') AND ESTATUS <> 'C'`).catch(() => [{ N: 0 }]),
  ]);
  return { doctos_ve: ve[0].N, doctos_pv: pv[0].N };
});

get('/api/debug/pv', async () => {
  const rows = await query(`SELECT COUNT(*) AS N FROM DOCTOS_PV`).catch(() => [{ N: 0 }]);
  return { doctos_pv: rows[0].N };
});

get('/api/debug/cumplimiento', async () => {
  const rows = await query(`SELECT FIRST 1 * FROM CONFIGURACIONES_GEN`).catch(() => []);
  return { config: rows[0] || null };
});

get('/api/debug/inv', async () => {
  const [art, lines] = await Promise.all([
    query(`SELECT COUNT(*) AS N FROM ARTICULOS`).catch(() => [{ N: 0 }]),
    query(`SELECT COUNT(*) AS N FROM LINES_EXISTENCIA`).catch(() => [{ N: 0 }]),
  ]);
  return { articulos: art[0].N, lines_existencia: lines[0].N };
});

get('/api/debug/schema', async () => {
  const tables = ['DOCTOS_VE', 'DOCTOS_CC', 'IMPORTES_DOCTOS_CC', 'CLIENTES', 'VENDEDORES'];
  const out = {};
  for (const t of tables) {
    try {
      const r = await query(`SELECT FIRST 1 * FROM ${t}`);
      out[t] = r[0] ? Object.keys(r[0]) : [];
    } catch (e) {
      out[t] = e.message;
    }
  }
  return out;
});

get('/api/debug/costo', async () => {
  const rows = await query(`
    SELECT FIRST 5
      d.DOCTO_VE_ID, det.ARTICULO_ID, det.PRECIO_TOTAL, det.COSTO_TOTAL
    FROM DOCTOS_VE d JOIN DOCTOS_VE_DET det ON det.DOCTO_VE_ID = d.DOCTO_VE_ID
    WHERE (d.TIPO_DOCTO = 'F' OR d.TIPO_DOCTO = 'V') AND d.ESTATUS <> 'C'
  `).catch(() => []);
  return { sample: rows };
});

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  EMAIL (preview, enviar, cron)
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

function generarReporteHTML(data) {
  if (!data) data = {};
  return `<!DOCTYPE html><html><head><meta charset="utf-8"><title>Reporte</title></head><body>
  <h1>Reporte Suminregio</h1>
  <p>Generado: ${new Date().toISOString()}</p>
  <pre>${JSON.stringify(data, null, 2)}</pre>
  </body></html>`;
}

get('/api/email/preview', async (req) => {
  const [ventas, cxc] = await Promise.all([
    query(`SELECT COALESCE(SUM(d.IMPORTE_NETO),0) AS T FROM ${ventasSub()} d WHERE EXTRACT(MONTH FROM d.FECHA) = EXTRACT(MONTH FROM CURRENT_DATE)`).catch(() => [{ T: 0 }]),
    query(`SELECT COALESCE(SUM(s.SALDO),0) AS T FROM ${cxcClienteSQL()} s`).catch(() => [{ T: 0 }]),
  ]);
  const html = generarReporteHTML({
    ventas_mes: +(ventas[0] && ventas[0].T) || 0,
    cxc_saldo: +(cxc[0] && cxc[0].T) || 0,
  });
  return { html };
});

app.post('/api/email/enviar', async (req, res) => {
  try {
    const { destinos, asunto, cuerpo } = req.body || {};
    const html = cuerpo || generarReporteHTML({ mensaje: 'Sin datos' });
    // Stub: no envÃ­a correo real sin nodemailer configurado
    res.json({ ok: true, mensaje: 'EnvÃ­o simulado (configurar nodemailer para envÃ­o real)' });
  } catch (e) {
    console.error('[ERROR] /api/email/enviar', e.message);
    res.status(500).json({ error: e.message });
  }
});

function iniciarCronEmail() {
  // Stub: no programa tareas sin cron configurado
  try {
    if (typeof setInterval !== 'undefined') setInterval(() => {}, 86400000);
  } catch (e) {}
}

// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
//  PING & SERVER
// â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•

app.get('/api/ping', (req, res) => {
  res.json({
    ok: true,
    ts: new Date().toISOString(),
    version: '9.0',
    empresa: process.env.EMPRESA_NOMBRE || 'SUMINREGIO PARKER',
  });
});

iniciarCronEmail();

app.listen(PORT, () => {
  console.log(`Suminregio API escuchando en http://localhost:${PORT}`);
});
