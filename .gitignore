node_modules/
# Carpeta generada por scripts/export-suminregio-deploy.ps1 (regenerar antes de publicar)
suminregio-microsip-deploy/
.env
env
*.log
.DS_Store
Thumbs.db

# Binario local (descargar de ngrok.com); el arranque vía PM2 está en ecosystem-ngrok.config.cjs
ngrok.exe
backup/
.claude/

# Métricas de uso (pueden contener IPs / rutas sensibles)
data/usage-metrics.jsonl
microsip-api_Beto/
sistema-cotizacion-web/

# Archivos temporales / de sesión que no deben commitearse
tmp_*
*.tmp
CODIGO_PARA_CLAUDE*.txt
PASTE_PARA_CLAUDE*.md
*.bak
*~
*.swp
leer_demo.py
ver-estatus*.js

# Backups históricos en repo (ya migrados a Backups_handoff_*)
*.LOCAL_VENTAS_*.js
*_resto.js
server_cumplimiento_*.js
