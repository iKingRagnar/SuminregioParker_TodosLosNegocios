node_modules/
# Carpeta generada por scripts/export-suminregio-deploy.ps1 (regenerar antes de publicar)
suminregio-microsip-deploy/
.env
env
*.log
.DS_Store
Thumbs.db

# Binario local (descargar de ngrok.com); el arranque vía PM2 está en ecosystem-ngrok.config.cjs
ngrok.exe

# ── Evita "Filename too long" en Windows y basura que no debe ir a GitHub ──
.cursor/*
!.cursor/rules/
!.cursor/rules/**
Cerebro Cursor/
ARCHIVO__referencia_pbix/
ARCHIVO__principal_viejo/
Recordatorio para Cursor/
**/*_extracted/
bitacora_fila_pegar.txt

# Repo anidado (si no quieres versionarlo aquí; quita esta línea si sí)
# microsip-api_Beto/
